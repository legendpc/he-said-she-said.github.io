Is there a way I could download and install a package listed on npm manually? Something's wrong with the way promised-io is getting served up -- it's redirecting instead of giving out JSON, and npm can't download it -- and as a result, I can't install bogart or couchdb...