maxogden: I understand. Do you have any idea how I could modify knox-stream.js write() so that instead of doing emit('data') I could actually call the destination pipe write() and get the return value?