maxogden: I modified formidable to handle pause/resume correctly depending on the return value from the write()... I just need to make sure that the knox write stream will return the underlying stream.write result