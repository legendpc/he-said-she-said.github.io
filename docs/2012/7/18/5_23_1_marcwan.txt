domino14:  worst case, you can just have a method that processes all incoming requests and parses the body by hand.  i used to multipart file handling like that before the frameworks got support for it.