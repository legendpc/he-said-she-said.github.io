denom: basically, repls don't get access to the global namespace, or the script's namespace. So, in order to have variables accessible in the repl, you have to set repl.context = { a: 'a', b: function () { return 'b'; }} or whatever