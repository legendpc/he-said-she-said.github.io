I know its not super complicated, but is there an existing lib to compare one json document against a 'template' json document and remove from it all the keys that are not present in the template document?