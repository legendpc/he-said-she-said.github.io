Hi. I'm trying to use cookie auth (using CouchDB and nano). I can login and set the cookie. Now I want to reuse it like so: var nano = require('nano')({ url : 'http://localhost:5984/alice', cookie: 'AuthSession=' + auth }). This works but leads to require('nano') all over the place..