i'm writing a database driver, and had a question. when writing a random script that isn't run by a webserver or anything, i pass the get function a callback, but usually the node.js event loop ends before the callback completes. is this fine?