There are good articles that explain how node is 'non blocking' - to keep my answer short, ALL code is blocking; a processor can only handle one task at a time. Since string.match isn't waiting for anything (the processor is given definite steps), it's blocking from that perspective as well.