I'm trying to install node.js on my redhat box. By default it works and installs in /usr/local/bin/ which means that when I try to install express globally it fails and then when I do sudo npm install -g express, I get that there is no command called npm. Help?