enmityxero: Just trying to write good code. I see a lot of modules that do not use callbacks. Seems like pattern matching could be cpu intensive. I am not clear if I/O is disk access or any memory location.