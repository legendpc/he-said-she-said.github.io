function getDataFromDb(collection, id, callback) { db.collection(collection).find().toArray(callback) } - then call with getDataFromDb(myCollection, anID, function (err, results) { console.log('BAM!') })