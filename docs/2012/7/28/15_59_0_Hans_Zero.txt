okay manually installing the latest version is not helping but there is a pull request for node-gyp on github I'll try to use that (which won't solve my node-waf problem but will install the package hopefully)