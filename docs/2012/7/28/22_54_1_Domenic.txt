owen1: To some extent. Promises have a lot more guarantees than a naively-built solution; they try to provide a parallel to sync code, which is why they can be used as a basis for coroutines (which almost by definition look like sync code).