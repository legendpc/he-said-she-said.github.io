Has anyone found a good framework for abstracting the fact that every module you want to use must be required into your current file? In a .NET project all the classes in your project are available without using them. In JS every single little file has to be required in.