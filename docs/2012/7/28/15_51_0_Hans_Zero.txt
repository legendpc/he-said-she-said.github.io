coredarity, node-stringprep and node-expat (2 packages that show this error) are actually only dependencies of other packages and therefore I thought npm would just deploy the most recent available packages