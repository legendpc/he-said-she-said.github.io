well, there are word lists available on the Web (not JSON, you have to do some parsing first), but if you want to understand sentences' meaning - that's hardly possible and will fail in half of the cases :P