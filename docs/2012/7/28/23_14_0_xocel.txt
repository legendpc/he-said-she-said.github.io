its been a while since i used node-mysql (swapped to mongodb) but as mscdex suggested, put each statement into a separate function, then call them as you would any other function that uses callbacks and just pass the dbclient to each of the functions. 