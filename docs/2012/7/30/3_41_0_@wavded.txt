basically don't want the object to be gc'd at all until its explicitly closed (its for a long running socket connection) but in js, if the object has no more references it is calling the destructor in c++ land, anyone come across this?