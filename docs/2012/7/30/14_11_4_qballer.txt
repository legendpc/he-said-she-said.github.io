I plane on writing a distributed system which will use node.js and the browser. I wonder which data structure (generic) as possible can I use. I have ideas based on Java land where I come from but they might be wrong. Any suggestions?