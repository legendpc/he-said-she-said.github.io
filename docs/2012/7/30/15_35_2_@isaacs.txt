cryptopsy: also, the 'utf8' arg is super wrong there.  you're reading it as a buffer, converting to a string, then converting BACK to a buffer to send it down the wire, but you never take advantage of its string-ness, so there's no benefit.