like the example that has "static v8::Persistent<v8::Function> constructor;" in the header, and instantiates it in "void MyObject::Init()" with "constructor = Persistent<Function>::New(tpl->GetFunction());"