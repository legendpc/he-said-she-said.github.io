SubStack yes, it's was hard to normalize it across all the transports, so we decided to take the easy route and use the remoteAddress that we receive from the handshake request as that is either a JSONP or XHR request