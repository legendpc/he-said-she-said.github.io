anyone here use sequelizejs? If i pass it my primary key directly, it's as a string, and it won't do a save properly. How can I push that into the model to automatically do parseint or the like? for some reason the isInt passes even though typeof tells me it's a string!