i use fs.createFileStream to open up a stream ad use stream.write() to write to it. it's supposed to write ~a few lines per seconds, but after a while it falls behind. im writing a log and its now logging events from ~two hours ago. is that normal? isnt it performing too slow?