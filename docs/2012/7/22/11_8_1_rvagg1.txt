something like this: function timeoutCb(cb, timeout) { var called = false; setTimeout(function () { if (!called) cb('NOT CALLED!'); }, timeout); return function () { called = true; cb.apply(cb, arguments); }; }