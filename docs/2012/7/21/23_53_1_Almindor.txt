I'm trying to fix compatibility issues with our node.js app (currently works on 0.6.x). What's the best way to get two versions of node.js running on the same machine? (Mac OS X) Can I just get the tar.gz, unpack locally and call it directly?