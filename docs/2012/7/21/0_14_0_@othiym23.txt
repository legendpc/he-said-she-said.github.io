use that + the debugger statement in your test code so you can continue the test case, reload the inspector view, and then browse all the source scripts (which node-inspector doesn't know about until they've been required) so you can interactively set breakpoints