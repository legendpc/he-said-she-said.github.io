hello, hackers, i have a question about node.js and mysql: is there a point in holding more than 1 open connection to the mysql server per process, and balance the (heavy) load between them somehow? will that be any good? e.g. it would allow to *send* more than 1 query simulatiously