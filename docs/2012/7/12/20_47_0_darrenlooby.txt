I wonder if anyone can help me. I want to create a server. It only needs a root dir. The whole purpose is to take the POST array, process the data (filter and modify it), store it to a DB and then send it to Pusher.com...