guys, what scope can this construct access?  function(){}  // I'm confused, because it seems to be able to see "global scope" variables just fine, but local variables inside the function where it's instantnated are invisible to it