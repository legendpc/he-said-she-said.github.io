TooTallNate: As far as I know, you can't easily call a .Net DLL from a native C/C++ program. You need to create a COM wrapper around the .Net DLL and then in the native program you use COM to call the wrapper. So it's Native-to-COM-to-.Net.