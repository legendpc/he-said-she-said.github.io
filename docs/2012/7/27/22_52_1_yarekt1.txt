DTrejo: basically, client is drawing the data, and that takes a good few milliseconds. If i send an empty 'done' message from the server, that just gets executed before the drawing is complete client side