so how would you define a closure? a piece of code that is running from within a function after that function as already finished executing and that piece of code that lives on has access to the variables within the function that finished executing?