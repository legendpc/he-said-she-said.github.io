if I want to encapsulate (put in a self contained function) code that makes an asynchronous call, am I correct in thinking I have to use a synchronisation library in node since otherwise, the call is likely to return after the function itself has returned?