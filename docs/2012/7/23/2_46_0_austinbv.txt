so, on rails/jasmine projects (what I have the most experience with) jasmine includes the whole asset pipeline so you say expect(model.something).toEqual(something) without building out this structure