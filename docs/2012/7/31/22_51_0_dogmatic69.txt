ok, figured it out. I have bind-address as <server-ip> so that I can access from other pc's. not sure why, but I have had to put host: '<server-ip>' in the config, even though node is running on the same box