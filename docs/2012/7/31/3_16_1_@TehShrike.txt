SubStack: I haven't used SoX any way other than the command-line.  For that matter, I haven't written a Node.js module in C, either.  How complicated do you think it would be to fashion a module that would do reencoding of audio using SoX?