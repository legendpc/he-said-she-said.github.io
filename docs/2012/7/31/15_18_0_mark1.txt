i already stubbed the worker process that crawls a website which has solved the connection issue to the webserver (in one stub i send a lot of data back to the master process, in the other stub i initiated a lot of new workers, both worked fine)