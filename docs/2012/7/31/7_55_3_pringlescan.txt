ljharb, people host common libraries on a cdn to avoid multiple requests, we make the assumption they've already visited a site that also hosts it on the popular cdn so that all the sites don't waste their bandwidth, http requests or time.