var temp = fs.createReadStream( PATH + '/eZFw', { start: 90, end: 99 } );  console.log(temp); i am a bit new to all of this, so, this returned an object, but nothing i can find on the contents itself ?