Let's say I grab some data from mysql, but I need to grab data from a csv file, and then parse a webpage, and then after that call a function that processes all that data… what's the best way to keep the chunks of data I'm getting so I can deliver it to the end function for processing