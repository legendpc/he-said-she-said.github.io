Damn, this is messing with my mind. I set up a small script that just uses the conect module and mysql to save a row in the db for every request. Used siege to make loads of requests (9k +) and mysql had 1k rows.