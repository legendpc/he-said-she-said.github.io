pringlescan: bind is used to preserve a context, and also curry arguments. you don't need it for a closure as that's already created just by defining a function that refers to variables in the function's enclosing scope.