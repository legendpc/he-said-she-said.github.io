in my app.js I want to set a GLOBAL.BASE_DIR = ? so in in any file in any directory I can do a require(BASE_DIR + '/models/user_model.js');  what is the proper syntax for the project root, is it __dirname?