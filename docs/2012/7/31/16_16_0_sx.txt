ok. so I've found where my issue could be. I have a JSON object called "obj". If I assign: x = obj[key]; console.log(x);  i get 'undefined'. the compiler seems to be trying to print to console before x is completely initiated.