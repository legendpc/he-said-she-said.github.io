tuhoojabotti: OK, well close() that server, io.flashPolicyServer.close(); and io.sockets.clients().forEach(function(socket) { socket.disconnect(); }); where "io" is the result of the socket.io listen call.