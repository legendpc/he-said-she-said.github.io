node-ben basically creates a `done` closure that you wrap the request in when you get it and when the request sends it's response you call the done method that was passed to you and then the time of the entire request will be passed to callback you passed to *.async()