jmwhittaker: yah. `npm install .` will install it locally, keeping everything in that module folder. Then you can just execute the bin/inca in that folder. So either type the full path to it, or add that bin directory to your PATH