or that as well. My experience is that require load the file, and since JSON for the most part is just an object with properties. So you could bind it to another object but that would be a bit redundant.