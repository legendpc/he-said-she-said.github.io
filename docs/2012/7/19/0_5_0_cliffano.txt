is there a nice module that allows checking whether a process/worker is running? i want to spawn a process that writes a file, but i want to monitor whether it's still running or not, to avoid multiple processes streaming to the same file