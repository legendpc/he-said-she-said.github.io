so... I started creating an API with restify. But how do I both use that from my own node code, and also do unit testing? Should I be writing all the functions separately and then have restify be a wrapper to call stuff (adding in permissions) ?