in semver, if x.y.z  -- if Y has been incremented, doesn't that mean that old things will keep working the same? official semver seems to specify that only new functionality, not breaking changes, should have Y incremented.