i mean, in the existing require, it just says require("routes") and then routes.index works.. in routs/index.js, export.index = function() {} .. i have the same for routes/list.js, i.e. exports.list = function() {}; .. 