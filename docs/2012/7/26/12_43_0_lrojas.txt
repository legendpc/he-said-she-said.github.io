I'm trying to wrap my head around Node.js, is node.js equivalent to Apache or it's it more like Django, Rails ? also, if i understand correctly, it does not have a database engine, so could i run it against MySQL, PostgreSQL or even better, CouchDB, MongoDB ?