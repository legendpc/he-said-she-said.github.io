so with node+websockets, Getting couple of thousand rows and sending them to clients. Is it better the send the whole array in one go, and let websockets chunk it. Or send it in batches of say 100. Or send each row as an individual websocket message ?