rektide: thanks, I know I need to handle it, I'm just not sure how.. for example if I had this line: var req = request.get('http://localhost:5984/db/_design/designdoc/_view/doc_by_version?limit=20').pipe(parser).pipe(logger);