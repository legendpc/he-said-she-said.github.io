ljharb: i thought of having node running on the same server as the ruby server. i also thought that node can handle big file uploads nicer, since i don't have to load the file to memory, but save it chunk by chunk into harddrive, and than process it row by row.