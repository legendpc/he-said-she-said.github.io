Looking for advice about implementing a daemon that runs as root, but that is capable of invoking scripts as other UIDs and feeding data to them via STDIN. I believe if I drop privileges with setuid(), I can't regain them back right?