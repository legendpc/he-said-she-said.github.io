so if i am following along, instead of developing my app in python/django or ruby/rails, i would write it in JavaScript, then instead of creating a virtual host in apache and setting up some sort of "enviroment" i would write a server in javascript also and serve the application from there?