mscdex: it works with http:// thanks.  do u need to redirect back or something similar? i want the user to stay in the original form. the node service is suppose to take care of the file upload + processing without causing the browser to freeze.