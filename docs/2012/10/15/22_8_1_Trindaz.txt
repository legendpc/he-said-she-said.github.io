othiym23 little help: how do I make sense of a handle objects? Some are obvious, with values reffering to "jsdom" so that's obviously where the outstanding task comes from, but others have much more vanilla looking keys