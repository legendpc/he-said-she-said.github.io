superjoe: its a function of the app, it will check for an installed NPM (all private) and install it if the version differs, i tried it this way but I suppose I could spawn a child process to run the console commands