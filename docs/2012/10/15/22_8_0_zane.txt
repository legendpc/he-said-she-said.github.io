Is doing a var tmp = require('my_module'); within code (not at the top of the file, but rather scoped inside a function) a bad practice, eg, does it increase memory overhead by re-importing the module etc.?