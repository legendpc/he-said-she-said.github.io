Aria: It explicitly doesn't have the same semantics, global===GLOBAL===root===_ represents the variables accessible in the top scope. This is different than a single variable scoped to the current process, which is similar to the request variable scoped to a single HTTP request