Aria: Yeah, variable scopes are only created inside functions, so that makes sense. But you can check out the spec for yourself. It's why you can "return" inside a file, without being inside an explicit function(){} block