mkdir -p ${HOME}/local/node/versions/0.8.12; curl http://nodejs.org/dist/latest/node-v0.8.12-linux-x86.tar.gz | tar xzf -C ${HOME}/local/node/versions/0.8.12; ln -s ${HOME}/local/node/versions/0.8.12 ${HOME}/local/node/current; echo 'PATH="${HOME}/local/node/current/bin:${PATH}"' >> ~/.profile; . ~/.profile;