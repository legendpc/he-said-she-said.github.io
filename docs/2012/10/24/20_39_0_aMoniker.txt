DTrejo: I have a closure with some variables, and then sub closures that start setIntervals, and in those intervals various functions can be called that need to modify the original variables I mentioned.