 node -e "console.log('Hello World');" # terminates                node -e "var cluster = require('cluster'); if (cluster.isMaster) { cluster.fork(); } console.log('Hello World');" # doesn't terminate            why is that?