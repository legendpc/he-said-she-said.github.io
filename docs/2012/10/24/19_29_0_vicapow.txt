I wrote a module that uses another module (the canvas module in particular.) how can I make sure my module and project are using the same instance of the canvas module? is the best solution to just pass my projects "canvas" instance to the other module?