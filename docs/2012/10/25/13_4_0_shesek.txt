Hey all. Does anyone know of an ORM that properly handles fetching data with relations? For example, one that can create JOINs to BelongsTo/HasOne relations, and expose that data as objects of the correct type?