I've got a silly question, pls don't laugh. Why would you use cookies for user sessions & authentication?  Since the only point of a cookie in this scenario is to keep some identifying Key, why not just store it in a js var on client? (Apart from keeping user logged in after browser closes.)