sshaginyan: How continuous is continuously? The naeive way would be to just run a loop, but that will eat your CPU. A slightly better method would be to set an interval. A potentially better or maybe way worse method would be to monkey patch the array.push method or something.