yummo: new Buffer() is basically like C's malloc(). it just makes the memory available, but doesn't set it to any default. That's one of those things that you could spend the CPU cycles on, but there's no reason to, since you'll most likely be overwriting the data anyway when you need it