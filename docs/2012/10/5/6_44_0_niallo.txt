it registers a handler for messages on a zmq pull socket and is supposed to stay alive forever processing those as they arrive. however it always exits after a number of seconds. processes messages just fine while it's alive.