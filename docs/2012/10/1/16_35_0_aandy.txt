metal55: i haven't used the symlink function so i'd suggest you make a proof of concept first. make a test dir where you create a symlink (like you would without node, like you created the others), and then delete it with node