So I was looking, i can get a buffer from an array of bytes and encode it tostring with base64 encoding, and now i need to do the oposite, I have a string base64 encoded and need to get the byte array from it into a buffer, is it possible?