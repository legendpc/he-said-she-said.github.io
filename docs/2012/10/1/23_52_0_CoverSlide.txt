just like the server doesn't emit a 'connect' event to the client, the transport internally connects, and uses the eventemitter semantics  to emit a 'connect' event on the client so it knows when it's connected