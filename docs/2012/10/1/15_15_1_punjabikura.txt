so I'm making net.Socket.write() calls and the response is coming back in chunks (multiple callback callbacks). When a bunch of commands get sent to the server, the callback needs to know what was the command sent if there is more data coming in, any idea how I can go about doing this?