it depends on the scope of the functions - you could either define them all within a scope that has access to the request (bad way) or .bind() your callback to a scope that has access to the request (kind of better way)