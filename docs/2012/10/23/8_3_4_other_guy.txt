technically it's the index name, you can create indexes that have no relationship to buckets if you want, by by default when you enable indexing on a bucket (rather than completely manually manipulating the index yourself) the index == the bucket name