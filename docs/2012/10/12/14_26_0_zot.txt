semi-newb question: working on a library to be shared - the library code is in 'lib', test code in 'test'.  when i want to write code that require()s identically as a client would, what's the smart way to set that up in my dev env?