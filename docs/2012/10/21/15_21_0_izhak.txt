mbalho: It seems that's not the exact thing I need. I have a csv parser which accepts a stream as it's constructor's argument. Like this csv.createCsvStreamReader(process.openStdin()); So a need to path some stream made out of an http response to it. Can you suggest how I do it please?