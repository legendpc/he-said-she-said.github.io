zezikaro: As tempting as having things like 'the project root' be environmental is, just easy to conjure out of thin air, it's actually much better (For testing, later code re-use, etc) to inject those things -- have the calling module tell it explicitly.