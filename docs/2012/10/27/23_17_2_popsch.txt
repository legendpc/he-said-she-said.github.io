I have the data (a csv file) on the server side but I need to pass it to the browser. How can I convert the string data into an expression that I can insert in the web page? (it should become: var data = '...' )