it works fine when i instantiate 'Game' by itself, but when i try to instantiate an extended class from it, it fails with "ssertion failed: (handle->InternalFieldCount() > 0), function Wrap, file ../src/node_object_wrap.h, line 71."