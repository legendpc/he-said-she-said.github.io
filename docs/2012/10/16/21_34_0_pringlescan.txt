I have a weird question. I was looking for modules to do an HTTP proxy in node, but not a reverse proxy, an HTTP proxy in the normal sense, that I'd use with my browser. I need to do some testing and analyze requests.