dazld_: the best way to figure all that out is really just to start building up a simple web app from the built-in http module, and start trying to solve problems like "callback hell" and routing and setting up DB connections for use by more than one route handler by yourself