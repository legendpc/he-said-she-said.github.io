I don't know who to tell this: I have a system that can (with small improvements) not only grab an website's CSS and make colors variable (say, have a colorpicker that changes the looks on the entire site), but also turn image-based gradients into CSS gradients