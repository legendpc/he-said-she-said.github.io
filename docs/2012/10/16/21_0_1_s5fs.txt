eMarek: if I, a rogue hacker, am able to upload a file to your server, it will immediately be put into production after the node process restarts. that's the security concern, someone can deploy a file and it will go right into production.