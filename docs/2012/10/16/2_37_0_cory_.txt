now that the "binary" encoding is deprecated for Buffers, what's the best way to write binary data out to something like base64 and then read it back into binary format? I've been trying to get zlib compressed strings in base64 to decompress... but the header check fails every time.