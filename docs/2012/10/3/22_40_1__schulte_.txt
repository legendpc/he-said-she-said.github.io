hi, how can I get npm to install my package so that it can be used with `require'?  The package consists solely of c++ bindings, which work locally with "require('./path/to/foo')" -- https://github.com/eschulte/node-gpg