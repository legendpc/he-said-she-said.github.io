mmalecki: the value adding of OO in ECMAScript 6 comes w/ the module system - you're forced into using the same OO system the module uses (which could be incompatible w/ another module you use) w/ ECMASCript 3 prototype chains vs. alternatives