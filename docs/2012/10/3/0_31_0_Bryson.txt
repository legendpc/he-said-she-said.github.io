i have a question on dealing with req.on('end', ...), JSON.parse() inside that listener, catching errors for bad JSON (try/catch?), and putting valid JSON in a variable outside the listener. is somebody available to help?