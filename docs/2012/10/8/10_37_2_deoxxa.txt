pixie79: original question though, it looks like you're confusing how callbacks work. you don't get the result as a return value from the function, but rather the callback you give to the callback-accepting function gets called (back) with the result when it's ready