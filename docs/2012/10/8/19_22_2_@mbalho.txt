dogbone: a) there is a channel for javascript and b) getUserMedia is very new and limited right now, the best you could do is buffer audio into a binary container format and use the also new saveAs API