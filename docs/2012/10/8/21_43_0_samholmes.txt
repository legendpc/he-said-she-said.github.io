Right now, I haven't set up a session store for my app, it's still using MemoryStore. So, all my session data is local state. If I set up a redis store for my sessions, wouldn't the redis database data just be loaded into memory when the app loads up, then all reads are from local state?