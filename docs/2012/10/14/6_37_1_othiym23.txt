Mian: so you write a data access layer as a plugin, and then you wrap the listen call for the Express app with whatever bootstraps the data access layer, and you pass the DAL API object into your router and then everything has a connection to the DB