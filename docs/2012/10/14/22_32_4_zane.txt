at the beginning of the request (in express middleware) I assign a user object to the request, eg, req.user, and I think this is what is being GCd, so I'm explicitly null'ing the object at the end of the request...