chapel: same problem domains are meant to solve -- incoming HTTP request, call out to a database in the handler, call out to a cache in the database callback -- need to make all of that part of the same transaction cycle in the New Relic transaction trace