I'm writing an irc meeting bot and want to reload my app.js file when I make a change to it, without needing to restart the app (and wait for it to reconnect to irc). What's a good way to handle that?