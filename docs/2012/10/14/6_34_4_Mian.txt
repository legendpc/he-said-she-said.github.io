Chapel:  What does it mean by most of the time?  But the thing is whenever the new request comes in, it runs through the same code path that has DB instantiation, right?  I am just wondering if i need to do something like a Singleton for DB.