I have a question about setting up API server with Node.js.  If i instantiate a connection to mysql from node.js.  Does it reuse the same connection for different request?  OR does it re-establish a new connection?