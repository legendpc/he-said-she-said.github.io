I have a project that has some local modules installed in node_modules.  I've installed grunt globally and it sees it fine.  I've also installed grunt-contrib-less globally (but want to keep it out of the local project as I'll use it for multiple) but grunt can never find it.