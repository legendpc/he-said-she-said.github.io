either way.... you have to install the shit before running tests on jenkins ALL the time anyway.... otherwise are you going to manyally update the jenkins server every time you change package.json for a project?