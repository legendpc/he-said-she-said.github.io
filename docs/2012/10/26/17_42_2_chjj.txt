isaacs: yeah, i took a look at it. markdown could definitely use a spec. writing marked, i was testing against 5 different markdown implementations, and every single one of them is different in behavior (not just features).