So for starters: do I need to set up two separate servers, each with its own app=express(), then each with its own require("socket.io").listen(srv) ?   (http has socket.io plugged to provide socket.io.js; https has socket.io to do the job)   Seems like overkill.