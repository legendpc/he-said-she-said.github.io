Well, I've already set up my server on Apache, so I think I'll stick with that. And why would I want to proxy Apache? Apache can serve .htm & .js, while running web app can connect directly to node thru websockets, right?