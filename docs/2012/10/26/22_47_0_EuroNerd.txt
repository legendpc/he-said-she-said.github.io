konobi, right, so you suggest that node becomes file server (for my .htm & .js files)? But I've heard node shouldn't be directly exposed. So thru reverse-proxied nginx? But I've head nginx doesn't deal well with websockets over SSL. So I've got Apache as the safe choice.