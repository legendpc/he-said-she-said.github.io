estliberitas: I'm starting process like this, var shim_proc  = childProcess.spawn(worker_file); And then later in the script I would like to send message to the php script stdin (after particular nodjs event). How would I do it?