Anyone know whether you can use restify's authorization handler, but only for _some requests_? Or am I better off not using it with the app.user(handler) paradigm, and instead just wrap calls around my own auth func?