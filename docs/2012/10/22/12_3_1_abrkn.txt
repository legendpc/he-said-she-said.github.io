with browserify, is there a way to require one file for backend and one for frontend, assuming that: if(process.browser)require('browser.js');else require('node.js'); would cause browserify to pick up both