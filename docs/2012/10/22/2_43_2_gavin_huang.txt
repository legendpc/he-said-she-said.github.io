is there a tool that replaces, say 'require ("./foo")' with the actual content of "./foo.js" and assigning exported vars so the file can be used in browser? I tried projects like browserify, that actually implements a client-side require, which would be overkill for my case.