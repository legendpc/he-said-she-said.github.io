Raynos: like a global variable? or some kind of included file? marked doesn't do any kind of IO so the latter isnt possible, and neither is the first. however, you could hook into the parser and always add something to the `.links` object if you really wanted.