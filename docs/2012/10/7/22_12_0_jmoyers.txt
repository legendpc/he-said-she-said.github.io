im thinking views/* would serve a consolidated js file with all views, named and registered as available partials, then views/specificView would give just the views for that part of the directory structure.