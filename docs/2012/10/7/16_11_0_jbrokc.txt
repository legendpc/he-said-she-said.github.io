hey - i just did `brew upgrade node` which symlinked npm and all. then did `npm install -g <lib>`. how come going into a node shell and doing `require('<lib>')` gives me a module cannot be found error (if it was installed globally)?