Thought experiment: JSON doesn't deserialize to classes.  To get around this, someone scans a JSON for the key "type" and instantiates a class based on that.  I have great reservations with this approach.