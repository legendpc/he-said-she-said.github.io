each of my files includes dependencies using require and specifies the files it depends on using relative paths like so: require('./../../file') and this varies based on the depth of the current file in the directory hierarchy