can't find docs on this anywhere - if I want to prompt for a password and then read it using process.stdin.once, is there a way I can set the console up so that it won't echo the password typed by the user?