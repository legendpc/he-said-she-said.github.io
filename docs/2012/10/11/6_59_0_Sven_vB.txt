so i understand nextTick() will fire asap. what about other code happening in between? for my understanding of async, nextTick(cb) should push cb to the _end_ of the list of immediately due tasks, but the API docs and some websites sound like it would actually place it on top of the queue