hdon: even given the ability to run queries on connection to the database, it would still help to have a way to make the tests transactional. because we depending on how we choose to run the tests, it's possible that only one database connection will be used among multiple tests and the results on one test should not depend on the data created by another