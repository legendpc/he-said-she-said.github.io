what is it with process.nextTick()? what makes it different from setTimeout(..., 0)? some sites sound like nextTick() would avoid giving other code a chance to run in between, but wouldn't that be against NodeJS's async nature?