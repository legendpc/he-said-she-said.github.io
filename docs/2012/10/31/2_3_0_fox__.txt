google didn't help so i created a new VM installed the latest debian, included sit sources and installed nodejs through apt-get, then canvas through npm, created a test.js with only require('canvas') and still the same error apperars