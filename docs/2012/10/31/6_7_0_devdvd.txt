Aria, ill do that.  Regarding the arguments to listen(), the script works just fine without the cluster stuff.  I am just using it as an example for learning clustering since that is a pretty data intensive script