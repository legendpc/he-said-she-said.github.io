antgel you could do it that way, or use a pooling system object etc, var clients = []; function (socket) {clients.push(socket);socket.on('end',function () {clients.splice(clients.indexOf(socket), 1)});} //or similar if you want to roll your own