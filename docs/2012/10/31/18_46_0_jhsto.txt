I have a basic JS question - I have a event which triggers the browser's native alert popup, which displays everything fine, but when I call the event again, the alert box comes up twice. Any suggestion what might cause this?