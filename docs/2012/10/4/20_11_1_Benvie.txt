I also have basic support for Enum types (basically integers that automatically translate to and from a key -> index table, and then Bitfield types for translating from an integer to a given set of bits