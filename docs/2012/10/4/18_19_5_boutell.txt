a gripe about npm: it supports installing things via git rather than as downloaded packages, but it does it pretty badly. npm update can't do anything with them because it doesn't really check them out, it exports them.