however, if the connection is very slow, it will be better to have a copy of the repo on the server (in a not webserved directory), sync that (ideally with only one large file "pack" being transferred) and do the pull on the server without sshfs