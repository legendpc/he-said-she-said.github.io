there's no local one. i think. npm always install the libs you need in each project folder. if you install it with -g, will make the package command line available, not that if you do -g will be available everywhere