I just had a process die on dns.reverse(socket.remoteAddress, ...). dns.revers it threw "Error: getHostByAddr ENOTIMP", and it seems that the cause was remoteAddress being set to "-" instead of an IP address.