does anyone have any thoughts on the best way to serve a limited number of large-ish static files? currently I'm doing it by loading the files in Buffers and then calling request.end(buffer) for each request