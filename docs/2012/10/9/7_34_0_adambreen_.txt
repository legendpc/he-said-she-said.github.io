@chovy - ok, so I think the answer to your question is "no" - you don't need to return checkPermission.  You're following the Express continuation-passing-style convention, so you should just return next() in your checkPermission function.