does somebody know a utility library that allows me to defer tasks using something like .later(this, 'methodName', arg1, arg2) instead of .later(this.methodName.bind(this), arg1, arg2), without the double occurence of "this"?