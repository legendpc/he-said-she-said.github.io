electricsheep_: node is useful for lots of things that arent web app servers, it just also happens to be possible to write web app servers with it. so make sure to not just learn web app dev because most web frameworks in node are their own thing