with async.foreach, it will run each item of the array against an iterator function.  the callback will be called immediately if any of the items passes err to the callback.  is there an async function that is the opposite of foreach?