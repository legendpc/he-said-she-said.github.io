mdedetrich: there's a getTimezoneOffset() function which might be of interest. you could extend the prototype to include a .normalize = function () { return moment(this).add(this.getTimezoneOffset()); }