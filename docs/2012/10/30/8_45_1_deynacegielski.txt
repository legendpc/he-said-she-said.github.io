chovy: Ye, if you're familar with Java/Maven its sort of a multi module project with an aggregator package.json that pulls all the dependencies together into a final distributable. However we need to build the sub-modules first and publish them.