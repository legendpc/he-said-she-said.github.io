yes, the messages wil be of different types, but the message handler can for instance pick up a message, process it, store it in sql, and then publish a new message to redid which then publishes it to socket.io showing a realtime update in the browser.