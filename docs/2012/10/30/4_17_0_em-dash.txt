I'm floundering with an http proxy issue (making a request through a proxy), and my Node/express app is the only piece (of several) that chokes when HTTP_PROXY=http://example:80 (it works fine with HTTP_PROXY=http://example)