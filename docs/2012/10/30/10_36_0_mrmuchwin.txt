hello nom question here: why is there a node_modules in a dependency folder? I reckon its for its dependencies, but what if two dependencies have the same version of another dependencies, say dependency A, does this mean that dependency A gets its code in both dependency 1 and 2.