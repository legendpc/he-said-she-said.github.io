With HANDLEBARS in your main layout page that is not a template how do you insert {{helper}} in it because I can do so inside a template by doing Template.templatename.helper but that's only within a template