TehShrike: but, if you write very boring JavaScript, which pretends that it's typed, and always uses new Ctor(), and always has the same exact fields of the same types, then you get a faster program out the other end.