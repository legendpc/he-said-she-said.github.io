chovy: the best way to deal with it is to wrap JSON.parse in a try / catch clause because it's synchronous and then either retry or pass the error back downstream, depending on what makes more sense for your app