i was wondering if i could add a reference to the dependency checked out locally to my package.json's dependencies or devDependencies so that developers could do npm install --dev to configure their environment to get working