Is there a recommended place to write/read sockets? For instance, with cluster-master, tweak the socket location with the process.env.CLUSTER_MASTER_REPL to a specific location. Or is it best oto just keep it in the default directory/