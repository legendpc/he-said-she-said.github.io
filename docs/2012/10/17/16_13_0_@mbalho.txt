shoshy: so you have some options. incrase the heap size in v8 (temporary solution), store your data as Buffers instead of strings, use redis, or write your program in a way that doesnt require the whole thign to be in memory