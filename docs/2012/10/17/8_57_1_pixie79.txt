interfacing with and the results  returned. our daemon in the background launches a connection to the dameon and creates two fifo's connecting stdin for the  application to one and stdout to the other. 