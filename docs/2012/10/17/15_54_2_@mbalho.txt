shoshy: as i said earlier, node doesnt have access to all of the ram on your machine. it isnt designed for using lots of memory. the only thing that it can do is put Buffers in your system memory, but all other js objects are stored in a much smaller heap