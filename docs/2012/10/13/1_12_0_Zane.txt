Okay this is freaking retarded... when I set up node on my servers, I needed to install it automatically when my ubuntu servers booted. So I found a RPM...  did a "wget http://nodejs.tchol.org/repocfg/el/nodejs-stable-release.noarch.rpm"