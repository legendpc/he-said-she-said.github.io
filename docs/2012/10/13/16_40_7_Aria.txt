V8 devs are pretty conservative. They'll implement some things if they're behind a flag, and are interesting to the runtime, but they're not actually into changing the language, much, just the runtime.