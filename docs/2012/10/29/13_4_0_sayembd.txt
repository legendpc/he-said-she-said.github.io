yes, but the "requestHandler.start" function that I've mentioned earlier, is defined in another file, which I am importing as a module to my main file. So, probably I won't be able to access foo in that file, right ?