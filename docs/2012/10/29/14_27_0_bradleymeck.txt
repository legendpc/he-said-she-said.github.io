deyna if your private couch replicates npm, it will replicate all the modules in npm by default, you will want to check if a module of the same name and version is in the public registry, couchdb ppl can help with replicating without getting all the docs