gildean: only that you never have to process the "require()" statement if you don't use a cluster. and I assumed that was important since they don't just do "var cluster = require('cluster')" at the top.