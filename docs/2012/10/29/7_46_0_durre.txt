I'm trying to setup an integration test with node and mocha. I include my app in the test like so: var app = require('./main'); … then call app.client.send() … client is defined in main.js, but is undefined in the test. am I using require wrong?