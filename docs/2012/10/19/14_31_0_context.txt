hemanth: you need to put your script IN a package, THAT package needs to have an entry in package.json for said command script, THAT package needs to depend on x, you npm -g install THAT package, and npm will install x INSIDE that package as it depends on it