for example: postgres supports 64bit integers, but javascript does not. so if you have huge numbers in your database you can override the int parsing to return ints as a string so you can handle 64bit ints (sorta, can't do math with them or anything)