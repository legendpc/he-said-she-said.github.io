so, what happens when my 10-second long callback fires?  is it ran in a different thread?  i thought node.js was pretty much a straight path of execution as so many tutorials state which is why callbacks are used ?