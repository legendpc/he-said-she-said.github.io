when i'm edigin a geddy .ejs template for the view the browser will automatically upen as soon as i save it; problem, however: the browser is pointed to the template file itself (file://path/to/soemthing.html.ejs) instead of the actual url (http://127.0.0.1:4000/something); why is that?