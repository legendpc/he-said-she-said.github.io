Hmm when documentation says run "grunt" obviously that is no a exe (or a.out) there is grunt.js but it won't run on its own, and "node grunt.js" doesn't so anything useful, so how do you run "grunt" ?