basically collect all the module file names and check all instances in code that match some regex to makes sure they match said key .... its horrible code to write but the test can be more informative as well 