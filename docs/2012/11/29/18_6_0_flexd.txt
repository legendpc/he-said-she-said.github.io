To better explain my point of view: I feel that instead of giving the main program a new instance of CardReader, it would be better for CardReader to get a new instance of reader (which is the spawned process).