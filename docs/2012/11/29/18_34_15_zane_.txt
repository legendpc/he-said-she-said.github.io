tnorris: makes sense,  though, doesn't it seem likely that the memory that causes the crash is only alive for ~5 seconds or less? I mean, take for example JSON stringify -- the command should run in a matter ms....