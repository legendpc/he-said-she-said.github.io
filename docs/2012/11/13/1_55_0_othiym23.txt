and I guess my original point is that node-tap's test suite is a ragged mess, its documentation is pretty halfassed, and the code has random commented-out bits all over the place, but it works great and you can read the whole module in like an hour