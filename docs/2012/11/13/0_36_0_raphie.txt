well, lemme describe my situation. I've got tasks stored in mongodb by the time they should be executed. using node-cron, I have a worker query mongo every minute, and tasks that match the date get added to a redis task queue