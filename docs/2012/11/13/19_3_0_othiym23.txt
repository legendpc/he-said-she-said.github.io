it looks like it shouldn't be super tough to put together an EAX module, it's a protocol for using cryptographic primitives to produce AEAD (authenticated encryption with associated data), which is a NIST thing