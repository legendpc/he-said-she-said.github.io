whomp_, if you call out to the system using the process API it's a bit slower because it has to start up that second process to do it, the rename method is effectively the same thing has mv but doesn't need to manage a separate process just a thread