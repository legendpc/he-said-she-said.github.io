using node-ffi, I need to call a function which takes a pointer-to-pointer-to-struct, the routine deals with allocation of the structure and sticks it in that, pretty common in C -- the problem is I can't figure out how to do this, I don't think it's actually happening as expectedd