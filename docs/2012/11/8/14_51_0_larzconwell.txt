Okay here's the problem, on line 11 of your layout template you're calling "-if(session.user)" which is assuming there's a session object, but you're not including one(even if the user isn't signed in, just include an empty user object so it doesn't error out). 