so I wrote a crazy shell script to scrape my pidgin logs (all of them) remove the html, split into words, remove symbols, etc, and sort by frequency, then some coffeescript to remove anything that wasn't in /etc/dictionaries-common/words 