Yes it would but if "req.session.user" is undefined, you'll need to send an empty object so the Jade compiler doesn't freak out. So what you can do is "res.render('search/index', {session:req.session.user || {}});"