Anyone here use knox? I know the second parameter to the deleteFile callback (res) is the HTTP response object, but how is it used; is it useful? Can there be an error that isn't captured in the first parameter in the callback (err)?