larzconwell: No, the layout.jade file contains the code only for the buttons on the top... The search box and the drop down menu are in a file called index.jade, which in turn is in a folder called search... That's why on top, it says extends ../layout on the top...