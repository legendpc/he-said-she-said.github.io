in S3 terms, there is not such thing as a directory (just keys) but if you know your "directory" path, then you'll have to do a list using the "path/to/dir/" prefix to get all the keys, then delete those