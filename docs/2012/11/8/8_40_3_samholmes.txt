JasonWoof: About what you said about it not being accessible form HTTP. Git repos aren't necessarily accessible from HTTP anyway. I'm using ssh:// protocol to add them as a remote, and even then, you'll need the username/password to access it.