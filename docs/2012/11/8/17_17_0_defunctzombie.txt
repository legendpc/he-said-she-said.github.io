@isaacs hey, what do you think about pulling inherits into a module in core? instead of pushing everything into 'utils' pull out some of the more widely used modules. It would still ship with node, but just be a separate module