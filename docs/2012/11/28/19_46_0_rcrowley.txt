Hi y'all.  I'm confused by the cluster module and how it passes a newly-opened file descriptor from a parent process to a child process in a way that allows the child process to accept(2) and not get EBADF.  Is this documented anywhere?  Does it use the CLONE_FILES flag to clone(2) (on Linux)?