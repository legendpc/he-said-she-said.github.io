Distributions like ubuntu are already packaging it with this alternate name, but it would be best if we could have that be official and supported upstream (in part so that documentation exceptions can be avoided)