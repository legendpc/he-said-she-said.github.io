sgallagh: because there is not as much of a reliance on version numbers, and version number flexibility (all packages using "node" must use the *same* "node"), there is no way for a project name to change its meaning