Right there's definite uses for using a relative name versus a path lookup. Though I do think defining a particular method of doing a path lookup, e.g. "look inside node_modules/" is somewhat out of the scope for Node.js