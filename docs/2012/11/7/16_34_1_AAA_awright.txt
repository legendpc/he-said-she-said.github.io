russfrank: Now there is a use-case for having multiple versions of a package installed, but it's with applications, e.g. I have multiple versions of Unison installed because portage is awesome like that