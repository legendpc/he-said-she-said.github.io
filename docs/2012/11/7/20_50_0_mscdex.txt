linuxR: erm, not quite. for network I/O there is a backlog buffer at the kernel level, so node keeps calling accept() while there's still something to accept. every time it accepts the connection, it raises events in javascript land