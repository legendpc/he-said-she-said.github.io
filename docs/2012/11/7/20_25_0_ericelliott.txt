here's an odd question, maybe.. I want to get the version of my app into JS that gets delivered to browsers. I'm thinking that grunt could look at package.json and build a browserify module that my browser bundle can then require (using browserify)