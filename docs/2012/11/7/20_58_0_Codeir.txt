I have a folder full of tests (binder full of tests, lol). When I run vows lib/test/* it flips out and says it can't find the module. I imagine I need either a package.json or an index.js that requires all of them?