isaacs: You can currently wrap a Buffer in a typed array view (e.g. new Uint8Array(new Buffer())) and it works great, and super fast. but utf8 string encoding with any typed array views just isn't possible.