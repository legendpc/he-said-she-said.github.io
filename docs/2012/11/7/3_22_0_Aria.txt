yug: If the data is text-dominant, I'd quote the binary and embed in json. If the data is characters JSON can't do dominant, I'd send binary with json embedded for other parts; I'd use netstrings, probably, or some simple delimiter to break the pieces apart.