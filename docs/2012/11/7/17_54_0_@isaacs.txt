AAA_awright: npm 0.3 set things up this way so that it would not need shims, but still used them.  npm 1.0 got rid of the shims entirely.  npm 1.1 started using the built-in gzip and javascript tar implementation