Hey. So I'm trying to parse some data and same the inserts to a ram disk, so I can import into SQL. But.. it looks like it's going to fast for the writestream and I get fails on the insert. So, I should re-queue these?  I couldn't find any articles about this.