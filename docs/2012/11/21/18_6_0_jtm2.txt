Why is my "get" failing: redis_client.set('socket_' + client.id, public_hash);  then client.get('socket_' + client.id, function(err, reply) { ... });   both err and reply are null. If I look up the key in redis, it is there and correct.