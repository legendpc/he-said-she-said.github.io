hi. npm install -g quest; node #drops into console;  >require 'quest'    ... no module found. how can this be if 'npm list -g' lists module 'quest' ? the node console responds positively to require 'http' ..although that's built in i suppose. help?