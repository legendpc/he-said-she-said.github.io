tnorris: going to style the commit view a bit more, make it a bit more readable. then make the different activity lists have a "more" button to load more than 1 page of data. then i'll try working on that