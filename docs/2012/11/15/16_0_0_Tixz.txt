Input validation specifically :) Was looking for something I could use cross server/browser, but there seems to be validation built into mongoose, so I just need something to do validation inside my backbone models