and the only reason to write unit tests against models would be if you have tricky custom logic in validators, or some special callbacks you've added to do things like joins, that you want to validate