I mean, at its root file operations are being handled by Node's super secret thread pool (which is used only to make file operations asynchronous and do DNS lookup), and those threads are just making plain read calls