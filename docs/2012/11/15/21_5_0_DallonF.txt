AndreasMadsen, so out of curiosity, if you call domain.dispose() inside of a domain, it exits the domain - what happens if you call domain.dispose() outside of a domain? (i.e. in a parallel async task) Would it kill the domain and abort its processing?