Here's my problem. I have a localhost build of my site. The mysql server for this build is also on localhost. Both the application and server share the same timezone. Date object generated by node-mysql from a DATE column get a 7 hours subtracted from the actual date. Why?