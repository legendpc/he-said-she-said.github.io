TooTallNate: I've noticed that the DataView `get`/`set` methods are about 2x's as fast as Buffer's current `read`/`write` methods. you think it would be worth investigating reimplementing them with DataView's methods?