when I run `npm install` with both email-templates and email-templates-windows in my package.json's deps, I get a compile error, as expected, but the windows module isn't installed because of the error.