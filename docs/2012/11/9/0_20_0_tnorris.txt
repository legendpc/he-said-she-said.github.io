isaacs: so, fwiw, don't think that moving to typed arrays in place of buffers should happen. the spec states that they need to be zero filled when instantiated, and that is a major performance hit over the current buffer implementation.