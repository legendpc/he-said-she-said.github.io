MitchW: well, I have a for loop (0 to 3) { print index }  right before my real for loop that uses stream.write, the simple for loop manage to print 0, 1, 2   but my real for loop only managed to execute once when it should have executed at least 3 times.