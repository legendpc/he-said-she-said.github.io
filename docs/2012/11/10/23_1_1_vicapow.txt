squeakytoy: in the browser, or in node (aka, any JS implementation) there exists functions that, when called, can take a callback. those functions can perform native IO operations, and when finished, are added to the top of the JS event loop