nb-ben: there is an unnecessary memcpy of the `value` data that could be bypassed with a has() but I'm not sure it's worth it, perhaps if you needed to do it really often and your values were quite large