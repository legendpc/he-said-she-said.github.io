so with that example, if you try to go to example.com you'll get the cert /path/to/cert, if you go to example.net you'll get /path/to/cert2, if you go to example.org you'll get selfsigned.crt (it's a default if there's no match)