require('./blah'); would make it relative to the path the require is called in i suppose? -- Since I am using 'ember-runner' (for emberJS) and was trying the 'require' but it just seems to include empty files.