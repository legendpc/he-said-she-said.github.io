TehShrike: Storing all the data from different calls in different variables, somehow get a notification that all processes(threads, actions, what do you call them?) are done and then dispatch to some sort of template layer?