TehShrike: Say I fetch "latest news" from an rss, I look up two items in a database, retrieves some info about the current user and so. I'd have like five different async calls, eventually finishing, each one with it's own block of html code