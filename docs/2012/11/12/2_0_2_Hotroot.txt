The common thing to do is some sort of static file grabbing for pages.  Does this get cached? Otherwise isn't it /way/ faster to just preload the page into a var (or however into memory) and then serve from there?