EyePulp: Thanks for the link. What if the stream is continuous, meaning that child process constantly sending messages to the parent process. We cant use end event for message separators as I understand.