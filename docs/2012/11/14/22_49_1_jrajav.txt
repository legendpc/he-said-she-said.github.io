adlwalrus: Note that you can require things that are install without -g (and which go into node_modules in the current directory), but you can't call them on the command line without using their path; and vice versa for -g