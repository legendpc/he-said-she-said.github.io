Doumie: it seems fairly obvious if you read the docs, acl.middleware() is the function you put in the route as a middleware, it checks req.url and sees if the person requesting it has permissions to use it