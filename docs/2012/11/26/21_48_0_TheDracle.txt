I'm trying to use a Future from the fibers package, and having some strange issues. I'm wrapping a function that expects two arguments an a callback: sync_create_session = Fiber.wrap(api.createSession); sync_create_session.call(api, 'arg1', 'arg2').wait();