ohnoimdead: I think you can choose to make a Debian package containing all the deps, a set of Debian packages that each contain a single dep, or do the whole thing via npm, but be careful of combining the two packaging systems