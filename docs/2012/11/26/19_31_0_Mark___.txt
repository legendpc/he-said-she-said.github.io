@isaacs my path is git+ssh://git@github.com:mycompany/module-name.git#v0.1.1" and it tries to get the package from the npm registry, if I remove the "+ssh" then it tries to do a git clone, but with the wrong params