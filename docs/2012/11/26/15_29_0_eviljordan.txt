Ok, more succinct question: one a client connection, i need to do a db lookup via node-mysql to make sure the namespace is authorized. this apparently takes too long and since it's asynchronous, causes problems. ideas? 