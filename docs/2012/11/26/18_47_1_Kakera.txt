Steam allows you to send private messages and chat messages. Should I have two separate events for each message type, or one for both and let the user figure it out from the 'source' argument if he wants to?