deoxxa: i was wrong. nginx had nothing to do with it. whenever "password:" comes up during npm login, the login works fine, but when it is cached (i guess _auth in .npmrc), the login fails… meaning that publish never works