styol: just to clarify, you just need to make requests until 1e6 is reached. (e.g. 1000 calls 1000 large) but here's a snippet to create what you want: `var buf = new Buffer(1e6+1); buf.fill(97); buf.toString()`