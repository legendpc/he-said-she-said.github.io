let's say i create a stream with fs.createWriteStream("/tmp/foo"), write a bunch of data to it with s.write(), and call s.end().  Since those are async calls, I know that I can't immediately open the file and expect to be able to read all the data back from it. When can I do that?