OK that was poorly phrased. Restart: I'm building node.js from source, on linux, in debug. It takes ages. But I'm really only interested in playing with some .js modules. I wonder if I can alter the Makefile somehow in order to skip all the .c code, and only build the .js file in /lib folder.