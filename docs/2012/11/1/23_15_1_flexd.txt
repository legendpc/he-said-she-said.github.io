and for an actual bigger system doing something important with more than 1 "client" per time I would rather have server processes either communicate via some kind of network instead of doing it like that