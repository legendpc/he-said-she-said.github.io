question: i have a web page, and it has a number of computationally intensive divs that need rendering based on server-side HTTP requests to 3rd parties. is there a best way to render the initial page and then render the divs as soon as they finish?