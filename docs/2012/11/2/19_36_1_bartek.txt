Hey there. If I'm just doing a http.get for some JSON data, is there a nice pattern for getting all the chunks into a single variable? I feel weird doing var data = ""; res.on('data', function(chunk) { data += chunk; } ..