Nopik: If it where not an HTTP sever but a TCP server, the definition of socket-done would be when the end event emits (FIN package send). Cluster dosn't do anything http specific in only handles the port sharing.