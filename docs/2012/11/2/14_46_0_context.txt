dschatz: how bout this: store it in memory (which is prolly faster than shared without having to worry about locking etc) and use redis with pubsub so all processes get PUSHED the new value every time it changes. now you get fast AND single communication.