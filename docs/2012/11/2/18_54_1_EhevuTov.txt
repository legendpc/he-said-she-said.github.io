slajax, I know it's a long shot, but try to run it on Windows. Node handles file descriptors differently on Windows IIRC, and if you don't have the error on Win, then you'd be on to something possibly with Node itself