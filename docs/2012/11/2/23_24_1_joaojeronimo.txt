EhevuTov, cool! So one final thing, just to make sure I got it,    making: obj.prototype.foo = function () { /*etc*/}; obj.prototype.bar = function () { /*etc*/} would be exactly the same as.... (wait for it...)