i'm trying to write a unit test that checks that when my process is killed it kills it's children - this happens at the command line with ctrl-c but in my test which uses child.kill to simulate it it doesn't work :(