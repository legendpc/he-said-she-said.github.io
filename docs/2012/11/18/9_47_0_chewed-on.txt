Guys, when my server starts a Unix socket, it works, the server keeps running. However, after I kill the server using Ctrl + C in my shell, trying to run the same server.js file again ends up giving an error. It seems like the Unix socket that was created was never released/destroyed/free