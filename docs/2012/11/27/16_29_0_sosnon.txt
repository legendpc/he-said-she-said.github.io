while we're at module setup, are there node-specific patterns on setting up 'classes'/instanciable objects (well, prototypes...) and multiple inheritance in node/#node.js or do all the basic js patterns/styles still apply in the node universe?