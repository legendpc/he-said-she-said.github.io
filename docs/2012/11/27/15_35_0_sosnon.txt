also: running it in a terminal emulator and exiting it using "Esc", it will clutter up the terminal; this is common mistake with ncurses, but as far as i can see ultra-repl does not use it; unpleasant anyway