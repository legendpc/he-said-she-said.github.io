let me rephrase that question: what is a correct way of defining a module else than creating a new file? can there be multiple modules in one file then? can there be (multiple) modules in the repl? i mean it should work, right? no matter how bad this design is...