niggler: amm we are talking about C++ dll (.SO in Linux land) which is exposed by an IDL. I see no reason to write bindings when it's already 'exposed' the thing is -- Do you know of a node package that supports that.