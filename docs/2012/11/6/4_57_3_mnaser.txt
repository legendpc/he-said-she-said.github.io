othiym23: i have a task that runs inside setInterval, let's say that it loading a config file, connects to the servers that it's not connected to, that way, i can change the config file and it'll automatically see those changes and connect to it