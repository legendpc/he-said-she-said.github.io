gluxon: Node /has/ sync IO APIs. They're just ... special cases and kinda weird, and mostly just for convenience scripts that are really short, or for doing things inside require() replacements and the few other cases where you really need to do it synchronously.