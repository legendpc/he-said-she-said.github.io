cactus, right I want to use multiple files... I just don't know what get's initialized where... i.e. do I pass a require('http').createServer(app) to the module or a require('socket.io').listen(server)