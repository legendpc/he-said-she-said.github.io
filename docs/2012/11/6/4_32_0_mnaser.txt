I have a task that runs every setInterval and throughout my application, i run into places where i make the same call inside that setInterval.  Is there some sort of locking to make sure that they both don't get called? if i call it manually, i wouldn't want it to run from the setInterval