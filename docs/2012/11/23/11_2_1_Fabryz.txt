imagine i'm doing some filesystem operations with node, but I need to be sure to do them sequentially, like: copy files, move files just created, check if a directory is there etc...  How should I do it? 