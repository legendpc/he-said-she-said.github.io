channels and users have modes, which are just letters that mean something.. .like this channel +Cnt, C means no colour messages, n means no messages to the channel from people whare aren't in it and t means only operators can set the topic