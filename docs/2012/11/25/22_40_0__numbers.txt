or i guess if i've got ruby installed i can just use guard gem to: restart node, compile , aggregate, and minify coffeescript to js and sass to css, aggregate images into css sprites, append fingerprint suffix to the compiled files in production, gzip them, etc.