gluxon: The real trick is not trying to make inheritance heirarchies. They're rarely needed, and when they are, it's pretty obvious when you need them. Often what you want is a closure, with several functions inside. function foo() { var stuff; function frob() { } function quux() {}; frob(); }