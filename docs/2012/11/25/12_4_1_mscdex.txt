sosnon: although, really you should save the server as `this._server = http.createServer....` and then have a prototype method like `Server.prototype.start = function....` instead of doing `this.start = ...`