Aria: understood. From what I can tell, all functions are working correctly I can emit to a specific socket and back just fine, but the namespace emit to all sockets with the same name does nothing at all. No errors or warnings in the debug log either