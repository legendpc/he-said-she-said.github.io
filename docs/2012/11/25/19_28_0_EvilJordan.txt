Aria: probably nothing. Just worth mentioning when I upgraded from 0.4 (or something like that) to 0.8.11, db-mysql no longer worked so I req-wrote the db layer with node-mysql. Can't imagine the issue is related, but that was the only major change.