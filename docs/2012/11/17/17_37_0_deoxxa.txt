ah that note is only for internal data emission events within steez (or an object extended from it) - so long as the twitter thing you're using as input properly implements streams, when the steez instance is paused it'll tell the twitter thing to stop sending data for a while