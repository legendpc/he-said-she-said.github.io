agu10^_, there's a Lisp framework that does that, it maintains one representation of the DOM tree for ~every~ client that ~ever~ connects to your server, for like ~ever~. So it's ~really~ memory intensive.