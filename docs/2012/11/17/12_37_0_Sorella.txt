chewed-on, most people on the Node community prefer to build their own architecture using libraries, rather than frameworks enforcing everything about what they do. So if they need ORM, they'll grab an ORM library from NPM. Since deps management is rather easy, libraries rock here.