specifically, a map/reduce job has an end. if you have a stream of tweets, it presumably has no end, so you'd be forever in the map/reduce stage and you'd never hit the finalise portion of the operation. thus you would never actually get results.