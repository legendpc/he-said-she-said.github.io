agu10^_, because they live in the memory, they're stateful, and they can be only used in a single node. Whereas REST is stateless, so you can just add several other computers to your network and start dividing the workload right away. All the data is encapsulated in the request.