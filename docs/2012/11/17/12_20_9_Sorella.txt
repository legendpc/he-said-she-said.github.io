agu10^_, no, it maintains the representation of how the page will be, then when the client does some action it sends an event back to the server, the server responds with a bit of HTML/JS to change the page and reflect the new server's state.