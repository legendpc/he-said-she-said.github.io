@mbalho even using streams fails var stream = fs.createReadStream(filepath); stream.pipe(req, {end: false});                 stream.on('close', function() {                     req.end();                 });