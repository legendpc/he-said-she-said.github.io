I am implementing a IMing system, but I am NOT using redis' pub/sub, instead I am manually handling sessions with redis (by storing objects etc) and doing all my event handling on the sockets themselves.