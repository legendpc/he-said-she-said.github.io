Hey friends, I have a pretty simple issue. My stylesheet cannot be found. I placed it in the root folder inside a folder called css but it simply won't load at all (404) when I use <link rel="stylesheet" src="/css/main.css">. I'm using Express and Socket.io if that has anything to do with anything.