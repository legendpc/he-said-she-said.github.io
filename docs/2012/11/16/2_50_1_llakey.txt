the current project is a web crawler. we need to spin up a bunch of spot instances for a few hours and then spin them down. it doesn't matter if any of the instances are terminated mid-crawl. very flexible