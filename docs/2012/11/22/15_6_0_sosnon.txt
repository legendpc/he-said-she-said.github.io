aandy: yeah, well, mongoose, for instance, still requires manually specifying save/all/load/remove/... (or 'CRUD', if you want) methods; so it boils down to the same builerplate for each model item, with the difference of an extra requirement (+ potential slowdown)