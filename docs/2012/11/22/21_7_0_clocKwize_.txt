so, I'm playing with vows.js - I want to pass something in to my topic (basically, an object which my class can get/set stuff on, which in code will be localStorage and in my test will be an empty object - how do I access this from inside my assets? so I can asset the class set something on it