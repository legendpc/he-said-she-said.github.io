If an event is triggered with function(err, obj) how can I truly make sure that obj is not null. I do if (obj), but actually it is invalid. It has property 0 with a value null. Is there a better way to check for this?