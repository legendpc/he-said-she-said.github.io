I'm trying to see if I can use ZeroMQ with NodeJS HTTP requests because those are Streams and ZeroMQ can do multipart messages. I figured implementing Streaming middleware was nice and maybe I could use ZeroMQ to route requests to different apps/websited depending on hostname.