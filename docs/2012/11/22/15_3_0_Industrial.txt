Say I have one server running on one port that sends a message when a HTTP request occurs, can I make the receiving end (hopefully) somewhere down the line send a HTTP response? http://nodemanual.org/latest/nodejs_ref_guide/http.html#http.ServerResponse says it's used only internally.