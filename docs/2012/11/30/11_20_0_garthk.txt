AAA_awright: the output stream returns false on write; the next stream up calls its pause method and emits the pause event; the util.pipe() command's subscriber to that event pauses the next stream up, and so on all the way through to the first input stream. 