problem is as they come from modules I cannot add more arguments and at the end of this callback chain I need to render some html which I want to give back and send back with res.end on the main (first) function in this chain