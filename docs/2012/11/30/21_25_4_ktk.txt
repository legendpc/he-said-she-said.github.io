so from my understanding of the MDN page about nested function I should be able to do that with scope chaning. so I define a var in the first function where the end of the callback chain will write the stuff into