With source.pipe(tagger).pipe(dest), though, I'm still not entirely sure by which magic tagger.pause() causes source.pause() without there being a subsequent source.write() for which tagger can return false…