I'm using phantomjs to convert pages to thumbnails. The urls will be invoked from server-side code, hitting one of the server's routes. Is it possible within a route to see if the request is coming from the same server? Can this easily be spoofed?