I'm trying to cast a dynamically provided value to an integer if possible (due to JSON encoding) and I'm curious if this seems like the best case scenario -- var is_integer = parseInt(value, 10); var value = (is_integer) ? is_integer : value;