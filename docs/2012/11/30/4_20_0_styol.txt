hah, jshint is calling me weird, but it seems to "work". Anyone see an issue here or something I should do instead? var foo = { bar: new function(){ this.baz = function(){ console.log('JSHint says, "Weird construction. Delete \'new\'." and "Missing \'()\' invoking a constructor."'); }; } };