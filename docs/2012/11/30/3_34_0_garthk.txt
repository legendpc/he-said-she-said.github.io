Ok, cool. So, I want that kind of stuff happening, only X is a fast network service and Z is a slow client. And, Y is a chain of streams M N O and P, of which M is doing the XML parsing and then N is translating to a stream of small objects on which O and P do some important aggregation work.