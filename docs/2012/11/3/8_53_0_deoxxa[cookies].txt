chovy: basically that "route" file is nothing special - it's just code. what you're doing there is defining a bunch of functions and assigning them as properties to the exports object. it gets evaluated just once, and those functions are probably shoved into express.