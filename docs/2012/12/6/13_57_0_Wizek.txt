Hello! I have a simple site which has some reused parts like header and footer. The previous developer used ServerSideIncludes to include these. I'd like to use a Node technology, which can complile the HTMl files, and also watch them and recompile upon changes. I know Jade is able to --watch, but then I'd need to rewrite all the files. I thought I could use EJS, but it seems to not provide a CLI