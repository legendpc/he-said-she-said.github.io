EJS does not provide a feature to include static files (.css/.js) in templates in a layout-templates setup (using some sort of custom handlebar-like yield) so that they end up in the header, but only if the specific template is included in the layout, does it?