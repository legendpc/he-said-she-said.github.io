in reality, GC2 protocol allows for any number of bytes to be mashed together as C string (null terminated), and then the interior of them seperated by the unique spaceship seperator (::!!::) for use as [command, args, n...]