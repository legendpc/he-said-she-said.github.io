is there some kind of thing that combines the power of streams and the power of dnode? or should I make something like that myself, either using MuxDemux, or by just passing events over dnode (which fails with write because it returns things) and implementing handlers if the dnode dies?