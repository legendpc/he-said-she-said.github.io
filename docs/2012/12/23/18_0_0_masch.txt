hi #node.js! How would so write a audio-transcoder-stream? i want to request('http://some/shoutcast/src').pipe(webmtranscoder).pipe(response). would it be best to use ffmpeg for that or is there a good js implementation for something like that?