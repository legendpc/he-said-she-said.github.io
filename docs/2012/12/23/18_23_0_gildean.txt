terietor: the code you pasted has no line that would print the chunk anywhere, it's calling a perculiar func1(req, res, chunk) where the req and res are passed down as arguments from the topmost function