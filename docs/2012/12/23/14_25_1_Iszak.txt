So I wasn't talking about writing a database in node.js, I figured the reason you'd use another thread to interact with the database is because the database is blocking and thus slowing down the main thread.