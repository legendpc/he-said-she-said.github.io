MitchW, what about doing this over the same socket? the user opens the socket, connects to node, and node pushes all data down to the client as it becomes avialble on the php server which tells node to push over localhost