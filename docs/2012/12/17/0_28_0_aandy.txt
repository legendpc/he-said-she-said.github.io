on a related note, i once had to maintain two search indexes on another mysql db, where i passed the "response" object to both queries. whichever answered the fastest, would win. i haven't really used that approach since, but it could have its merit :D