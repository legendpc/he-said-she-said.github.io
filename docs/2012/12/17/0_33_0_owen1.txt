Nexxy: regarding the mocking during tests, should i pass my dbs as argument to some functions? for example saveData(db) and db will be mongodb or redis in the real app, and it will be mock object in my tests?