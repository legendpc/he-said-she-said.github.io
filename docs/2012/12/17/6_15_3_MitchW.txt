socket.io imo is good because 1) you get the websockets, but 2) it falls back to things like HTTP long-polling when your device doesn't support websockets (or your carrier has blocked the appropriate ports)