in the client side:      user_obj = user_cache.get(id);  then it renders something using the user_obj... So are you guys saying that whenever I use the cache I need to make it look like  user_cache.get(id, function(user_obj) { /* render using user_obj? /* }); ?