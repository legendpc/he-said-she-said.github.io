samholmes: It's rife with insecurities, abstracts away things to the point where it's not clear what it's doing, and it parses/rewrites the queries you write before running them against the server, making it useless for testing queries that might go into devlopment