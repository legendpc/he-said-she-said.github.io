Hiya. So I've got a very simple node server running, and I'm attempting to implement an oauth2 client flow in it. I've gotten to the point where I'm routing calls to http://localhost:8888/oauth2callback{etc}, but how would I persistently store the access token from that callback?