even something that's smart enough to be able to do pushstate for modern browers and wire up templates locally, and do server side wiring up of templates for rendering when the url is called manually? Hopefully this framework already exists?