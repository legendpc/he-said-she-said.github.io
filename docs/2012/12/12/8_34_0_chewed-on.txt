gildean: it's making a HTTP POST request to my Symfony web server to get Notification entities for push notification, then parses the returned result if any, then sends push binary stream to Apple push notification server