if i put my DB related code (queries and all) in a separate file, and then lets say i am usin them from 10 routes (expressjs) by doing require() and calling the methods is it fine ? or should i just require() it once somewhere in the startup script and somehow use in routes ?