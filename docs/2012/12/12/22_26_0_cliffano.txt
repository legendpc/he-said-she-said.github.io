if i have streamA.pipe(streamB).pipe(streamC); and streamA emits data too fast for streamC to process, resulting in high memory usage (backpressure?) and eventually out of memory. is the right way to slow down streamA is to control streamA's pause and resume?