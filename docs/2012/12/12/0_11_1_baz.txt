I am attempting to make the "scrypt" library as an addon for the community (and to help me figure out how to do this stuff). It is a C library (just to note this). It calls a function SHA256_Update, but the function declared in the source code is not called