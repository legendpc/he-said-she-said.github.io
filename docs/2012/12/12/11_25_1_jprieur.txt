hi, when doing a `npm install jQuery` I can observe that the node_modules/jQuery/package.json has a "__dependencies" entry instead of "dependencies" one, meaning it doesn't pull jQuery's dependencies. I couldn't find any documentation on this "__dependencies", any clue?