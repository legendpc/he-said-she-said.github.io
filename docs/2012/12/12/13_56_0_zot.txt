RLa: hrm.  that doesn't seem to have worked.  perhaps that's only successful if you use Object.definePropertyDescriptor(), as opposed to doing Foo.prototype.__defineGetter__('bar', function() { … });