does the format of index.html change at all? either do it horribly by sticking it in manually (without parsing the html - which is eww, but could work) or yeah, parse the HTML, inject some script tags