just "npm publish"ed a new version of uploadfs that returns a function for creating instances rather than relying on global state. It's a bc break, but I don't have a huge userbase yet, or if I do it's their fault for not being talkative (: