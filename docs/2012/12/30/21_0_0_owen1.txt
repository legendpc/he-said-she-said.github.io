i try to install a process monitoring script (https://github.com/visionmedia/mon/) and it tried to copy the script into /usr/local/bin but bin is owned by root. is it ok to chown this folder to the one that do the install?