for cli tools intended to be installed globally, is there a preferred way to store config files? other than creating another dotfile in $HOME? I was looking through https://npmjs.org/doc/config.html#Per-Package-Config-Settings but that doesn't seem like what I want