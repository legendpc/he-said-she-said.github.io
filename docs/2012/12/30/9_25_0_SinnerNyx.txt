mistertwig: It's mostly mini-helper functions where even if they are later implemented will likely work in much the same way that I would make them. For instance, startsWith or StringifyNonCircular. I won't publish it if that helps :P.