i have a bit of confusion, what does 'this' refer to inside a callback function? I have an object which has an init(filename, params) function property, I call fs.readFile() from within the init() function, it seems the 'this' refers to fs instead of my object? :\ 