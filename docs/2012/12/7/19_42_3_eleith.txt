@isaacs wanted to see if there was interest in a 'skip-if-installed' feature for npm. our dev environment process runs a set of npm install commands when we switch branches, but it hits the network for each install request when most of the time, we already have the node modules installed and just need the deployment to move ahead