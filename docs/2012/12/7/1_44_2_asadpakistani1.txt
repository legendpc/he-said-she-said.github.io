If I am to have an application where my Rest API can modify both a database locally and a database on the cloud...and then the two Sync themselves...Which would be a better data store for such a problem...MongoDB or couchDB?