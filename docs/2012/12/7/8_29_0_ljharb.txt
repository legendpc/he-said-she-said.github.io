Function.prototype.call.bind(trim) creates an anon function that in turn reads from all the passed in arguments, so maybe it keeps a closure reference to the args (including the full array) longer, or something?