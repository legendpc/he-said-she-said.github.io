Sorry to interrupt guys. I've set a variable at the top of my script... and a function that edits it. I'm trying to move that function to a module, but it can't access that var... how do I solve that? Is there such a thing as global vars in node.js