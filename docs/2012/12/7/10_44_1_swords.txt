maintain DRY. So after some thinking I thought it would make sense to use exports + facade (provide just the buttons with the contained mask/image/labels) + inheritance (create 8 instances of button). But how the …. do I combine these patterns? Does anyone have a example?