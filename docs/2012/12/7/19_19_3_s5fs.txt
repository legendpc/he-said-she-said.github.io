Zitter: i prefer to keep components separate (where possible). my approach would be to write the code to poll the sensor(s) and manage the state. then i'd work on exposing that data to the world via some network-accessible service. then i'd begin the node.js integration.