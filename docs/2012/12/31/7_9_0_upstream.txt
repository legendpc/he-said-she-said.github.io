I want to use node to eval a quick JS snippet like so: `$ node -p -e "var x = require('./something'); x.foo"` but I get the message "Cannot find module './something'", is there a way to tell node to eval my snippet from the current working directory?