If I grab the HTML source using a node.js app, how can i detect things like tags on the page? for example, not only if <html></html> tags exist, but also if there are any images (<img>), and if so how many, etc ? all this without regex, at least.