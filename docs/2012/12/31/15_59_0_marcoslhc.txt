Sohcahtoa if node-canvas use CSS to style things like the browser version it should work with google fonts, you may have to preprocess things tho. Like load the font into your fs and then serve it to the app. 