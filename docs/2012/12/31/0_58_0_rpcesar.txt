yea, this needs to be hyper fast though, im running untrusted code in a child process, which is wrapped in a special context that defines certain core functionality. im trying to create a cute interface to expose this functionality