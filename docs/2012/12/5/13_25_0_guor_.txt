I think the background is that strings in v8 are stored as one-byte per char or two-byte per char, depending on what's necessary, but this is hidden from the embedder, so if you do WriteAscii, non-ascii get truncated to ascii