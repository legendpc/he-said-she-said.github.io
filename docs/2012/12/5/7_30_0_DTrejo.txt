or.... it seems like as long as you dont close any fds, then the child's FD has the name number as the parent's FD for that file, thus I could just set an environment variable equal to the number for the child, and then the child just reads it out and make ioctl calls on that