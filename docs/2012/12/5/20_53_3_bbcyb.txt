newb here. where is a good place to store db connection information? have a base install of express and node-mysql package. Should i just require a db file in each route function and open/close per query?