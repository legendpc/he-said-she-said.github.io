is there good reason to be using "use strict" in node.js modules? if so, how should it be done, is it really best practice to use function scope or should I just ignore that warning since they are commonjs modules?