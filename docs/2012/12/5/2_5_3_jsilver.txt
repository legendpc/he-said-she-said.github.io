it's not, but I needed the Node servers so that's why I wrote them. I have a private project "ServerDropper" which is a Rails server than monitors processes, used to be Ruby but then I switch em to Node