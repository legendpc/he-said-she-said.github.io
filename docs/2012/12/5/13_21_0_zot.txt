mainly i just can't figure out how to simply get the raw bytes.  i *thought* String::AsciiValue wouldn't modify the contents, but that doesn't appear to be the case; but this can't be a unique need :)