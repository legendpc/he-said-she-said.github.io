for example, if I do a series of calls in quick succession http->tcp->http->tcp and so on, it creates n new HTTP connections, because the next connection wasn't already enqueued when the last one finished