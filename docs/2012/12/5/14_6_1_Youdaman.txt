garbagecollectio: yep, and could be used for other things, like (in that example) if they're not logged in, take them to the login/session page, otherwise forward them onto the _next_ page they were destined for