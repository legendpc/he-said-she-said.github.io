anyone familiar with node-clone?  im using it to clone a Buffer, and everything looks ok, but the clone is broken somehow.  the data is identical, but something is broken.  Somehow the buffer is almost a buffer, but something fundamental is missing: http://pastebin.com/Bq7BB306