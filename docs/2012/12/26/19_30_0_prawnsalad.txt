owen1: have a config file for each environment (production, staging, dev). either require('config.js') and have the relevant config in each environment, or require() in the relevant file based on a environment flag somewhere