I've got an array of tools for “subclassing” Function (i.e. making objects, that prototypally inherit from something *other* than Function.prototype, so you can add methods to them; but that are themselves callable, i.e. `my_object()`)