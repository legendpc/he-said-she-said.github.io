I want to be able to run the code (tar.gz file) through git via a push, then node builds it with a config file, then pushes it to another repo on a new server. But I call it from a Capistrano like file, but in node.js