jcrew, well, i actually need the results sent to the client.... i'd prefer not to have the JS source revealed to the user, and Node.js allows that, since it runs it server side. I imagine it would be much more efficient doing this task in JS rather than PHP