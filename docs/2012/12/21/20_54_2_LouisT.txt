I'm using socket.io on a PaaS, so it's using xhr-polling, when one person closes their tab i need it to be reported to the other player but for some reason it doesn't work with xhr-polling as it would with 'normal' websockets, any ideas?