im a little lost with something... when i require() a module, it searches for module as per the doco (http://nodejs.org/api/modules.html), but it also looks in <install_directory>/lib/node/... is that a bug? shouldn't it be looking in lib/node_modules?