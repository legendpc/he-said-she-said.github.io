wouldn't it be easier to do that manually? have a function readNextThree() { data = []; data.push(this.readUint32LE()); data.push(this.readUint16LE()); data.push(this.readUint16LE()); self.emit('new_trio', data); }