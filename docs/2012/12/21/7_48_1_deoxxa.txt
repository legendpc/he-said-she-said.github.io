substack: will a newer seaport client (1.1.x) work with an older seaport server (0.7.x)? and vice versa? i'd prefer not to have to run up a whole test environment to find out, so a "probably" or "probably not" will work for me...