crl+c from the console, but I might send custom close request and use process.exit([code]); with the custom shutdown request I can ofc prepend process.exit() with logging functions, but I would like to receive message when the application is closed anyway