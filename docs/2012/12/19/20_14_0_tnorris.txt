I'm attempting (poorly) to make a new buffer implementation. started by coping the existing and making a few small changes (e.g. change the class name) but for some reason it still exports as 'SlowBuffer': http://git.io/rjeSYw