ok, i figured out my problem. The node JS docs example shows a file and a callback for fs.readFile(). I had to add the encoding parameter to make it work or else i get that buffer.. is that a documentation problem?