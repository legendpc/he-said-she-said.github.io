Trying to debug an issue with a native npm module, can't seem to call the method when it is installed via npm, but can require the .node file and call it require('./node_modules/netif/build/Debug/netif') any ideas?