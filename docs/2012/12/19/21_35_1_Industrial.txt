Say I want a Stream that under the hood has an input stream and an output stream, can I make it so that if I pipe a 'regular' stream into this stream, it would pipe it into the input stream, and when piping from this Stream, it would be piping from the output stream?