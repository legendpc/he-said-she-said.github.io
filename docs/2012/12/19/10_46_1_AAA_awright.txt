ljharb: Also arguments.callee is going away, but you can self-reference the function by using its name within the function (even for expression functions inside an assignment, instead of statement functions which are their own statement)