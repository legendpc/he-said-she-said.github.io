redis is single thread (for memory stuff, it forks for saving to disk) so I do presume there is some sort of bottleneck there. The docs there for the module mention it... but why does that eat up cpu?