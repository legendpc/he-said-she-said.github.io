but if say for sending a response I need 2 datrabase queries but also a template, the last action (being the response sending part) can just ask for a template and the result of the 2 queries by defining them as dependancies .. and they can run in parralel for all it cares.