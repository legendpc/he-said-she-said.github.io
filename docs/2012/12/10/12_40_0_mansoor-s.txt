Yellow, It only gets executed once.  The entire contents of the file is executed it just happens that you only get access to whatever the module exports, but those exported parts can have access to the other parts of the code that dont' get exported