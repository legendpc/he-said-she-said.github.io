Hello all. I'm using mranney/node_redis in a script to import a 480million row DB into redis. It seems node.js bottlenecks on CPU calling the redis setting (there's no callback, even.) Any ideas on that?