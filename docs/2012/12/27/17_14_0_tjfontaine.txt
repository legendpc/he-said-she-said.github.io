draginx: it would make sense to actually get numbers on this, but it would seem that spawning 15 processes before hand and letting them handle the queue would be faster than spawning a process for each queue item