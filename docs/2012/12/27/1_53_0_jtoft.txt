Is there anyway to take a string, before you put it into a buffer, and run charCodeAt(n) across each of the strings? I was looking at Buffer.write(msg, …, 'ascii') but that doesn't seem to convert properly.