draginx: each call to .identify results in a new process spawning and reading the file you've passed it, more processes doing that means the OS can distribute load over the cores and can do IO scheduling a little better, but if you're not careful you could call .identify so much that you essentially DDoS your server