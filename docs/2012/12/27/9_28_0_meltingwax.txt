just for shits and giggles, here's an example. i have a var L=[]; Server.listen(6667, callback). One callback does for (var i=0; i<L.length; i++) { ... }, and another does L.pop() while the latter loop is still going. 