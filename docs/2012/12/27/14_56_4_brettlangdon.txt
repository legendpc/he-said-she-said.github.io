the thing where this async/parallel code execution comes in handy is lets say you have a web server and are handling requests, each request needs to go to the database, by having all the requests handled in async a single slow db call wont block other incoming requests