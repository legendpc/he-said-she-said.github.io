If I write a session.js, include it using require(), run a var s = new Session(req, res, db); .. can I somehow edit req from the calling function (like i pass a pointer to req rather than a copy of req) ?