but if you can't use google anymore, you need to send the mail through your own server.. which could be on amazon ec2, which in turn is used for spam so it's harder to get it not to go into the spambox by default