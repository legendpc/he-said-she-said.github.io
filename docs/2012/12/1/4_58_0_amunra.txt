how do i go about posting a file in nodejs? i already have a function that uses the file's data (type, size, path, etc). I just want to know how I can post the file so it can actually RETRIEVE the file's data.