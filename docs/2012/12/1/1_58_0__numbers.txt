how does a function like fs.readFileSync() implement blocking? e.g. if i have a function that i know will only ever take 10ms and i'm willing to block for that, whats the common way to block? or is it evil?