celivia: so if you want to use some Node.js modules like path, fs, even http, then browserify might be better.. but if you just want to use same modules style as in node for client side applications then Webmake will be cleaner