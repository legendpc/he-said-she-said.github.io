because i have two named pipes which need to be connected before returning, its kinda awkard having to emit a custom `ready` event when connections are established: https://github.com/peters/pty.js/blob/master/lib/pty_win.js#L141