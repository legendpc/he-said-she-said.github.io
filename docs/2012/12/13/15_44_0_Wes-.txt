gildean: when the echo server server accepts a connection, the Server object emits "connection",clientSocket and "connect" on the clientSocket. The hello message is sent to the client over the clientSocket, using the "connect" listener on the clientSocket, which was set up by the connectionListener on the server.   It's this implied ordering (i.e. if it happens out of order the demo doesn't...