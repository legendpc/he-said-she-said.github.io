http://nodejs.org/api/dgram.html#dgram_udp_datagram_sockets has example code: var x = dgram.createSocket('udp4'); x.send(...); x.on( ...); , but if i check require('dgram').Socket.prototype , I could find send() method, but there is no on() method. where does on() come from?