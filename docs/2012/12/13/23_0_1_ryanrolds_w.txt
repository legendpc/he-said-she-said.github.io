Ok, if you're using vanilla http, not socket.io or web sockets, you can use expresses session middleware to store id you're clients. You could also generate an id, like a UUID, and tack that in to the session.