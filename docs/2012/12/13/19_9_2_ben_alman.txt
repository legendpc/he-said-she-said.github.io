`3rdEden: so the problem is that we unfortunately used to tell them to install it globally the bin was "grunt" - the new way, grunt is locally installed but the thing they install globally is "grunt-cli" which has a "grunt" bin