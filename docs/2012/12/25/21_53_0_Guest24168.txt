If I have a script foo.js containing something like "function Foo() {}; Foo.prototype.bar = function() {console.log('text')}; exports.Foo = Foo". Now in a node repl, why does the following not work? "foo = new require('./foo').Foo(); foo.bar()"