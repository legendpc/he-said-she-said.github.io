i think socket.io's best benefit is that you can build a system that adapts the protocol (Websocket, flashsocket, long polling, etc) based on the client, much easier.  I cannot say if that is the 'correct use of socket.io' but it would work