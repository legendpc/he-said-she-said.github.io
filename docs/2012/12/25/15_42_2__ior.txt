as it stands i have a method for each query. each of these methods then call a runQuery method, which deals with connection, and declares the on.row and on.end callbacks with the list of if else clauses based on the calling method