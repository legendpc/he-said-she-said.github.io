so: first run npm shrinkwrap, second edit the shrinkwrapped json to change the versions, third nuke node_modules, fourth run npm install and it'll use the shrinkwrap manifest instead of package.json to spec deps