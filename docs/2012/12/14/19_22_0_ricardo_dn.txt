Speaking of which.... :D so I forked a module that's on npm (lingo) and did some small changes to it. I pointed my app's dependencies to my forked repo and for some reason it always tries to fetch the module from npm and not from the url I provided. Any ideas why?