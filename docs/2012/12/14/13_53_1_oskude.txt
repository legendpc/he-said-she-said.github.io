hi, im stuck on how to parse a gzipped text file. found examples on how to send to http response and sax, e.g. fs.createReadStream(file).pipe(zlib.createGunzip()).pipe(sax); but i'm not sure how i implement my own parser. any pointers?