tls.connect({path:'/some/path'}) seems to work on MacOSx while on joyent machines you have to make a new socket and connect it to the unix socket then pass it in as the  value for the socket property tls.connect