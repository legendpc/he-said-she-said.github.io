so how can I properly change the scope inside a dom event? I have a bidirectional binding between the property 'target' and a property on the parent scope. I'm changing target via $apply, but it doesn't affect the bound parent property