Hmm. If memory becomes a problem, I could always grab data chunks until I find the end-tag for one of the ~500KB data blocks, then cut it out properly and emit it. That'd make a decent through-stream. 