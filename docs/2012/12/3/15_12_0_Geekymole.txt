It seems in Node that modules are/should be isolated so there are no dependencies on one another so to rewrite a particular module it should be self contained meaning it handles all of its own I/O and doesn't hand off to something else to perform CRUD applications that is shared.