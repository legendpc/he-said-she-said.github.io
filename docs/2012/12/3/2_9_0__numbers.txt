i'm thinking like if `npm install` could take an argument that applied recursively, which defined the directory (e.g., i'd use the top-level ./node_modules/ directory) for which all modules got installed, and suffixed the version number to each directory... that'd work