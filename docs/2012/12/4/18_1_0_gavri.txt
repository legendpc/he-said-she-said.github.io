I'm using express.static and am including its app.use() above the other routes. the images do get served, but the routes matching '*' included below this express.static() call are also executed. is this how it's supposed to work?