I have a bunch of helper files in my project directory.  To keep things modular, I'd like to be able to 'require' them by name but not include the file-paths. Should I put them in their own directories and then do a npm-install on them?