acidjazz: I'm not sure it is the "best" way, but one way is -- var user = { list = function(){}, foo = function(){ user.list(); } }; user.bar = function(){}; module.exports = user; // in another file, var user = require('./user.js'); user.foo(){}; user.bar(){};