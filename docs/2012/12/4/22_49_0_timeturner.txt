sanity check: is it better to iterate through an array of sockets and pick specific sockets based on their id or to store the socket's id in mongodb in the client's document and query mongodb for those socket ids?