Aetas: for example here's a really simple app i created one evening i was bored, it's a bit lazy in that it uses two servers instead of just one, but as a learning-tool/something to poke at, it might be useful: https://github.com/gildean/ServerInfo