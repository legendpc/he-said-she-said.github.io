dawra: nope, i just have exactly the same code as in the example from the docs, calling createConnection just once. the only difference in my test was i copy/pasted the query a few times, i.e. the code between connect() and end()