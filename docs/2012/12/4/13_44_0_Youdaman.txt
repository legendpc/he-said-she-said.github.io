dawra: i tested locally, changing the user/pass and the sql to 'SELECT 1 + 1 AS solution' and both versions worked fine calling them from a third script that simply was: require('./test1.js').get('bar', function(baz) { console.log(baz) });