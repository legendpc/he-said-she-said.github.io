substack: Then why would you need to pass d to cons in order to close over it when you have access to the outer scope anyway? More importantly, how is it that if I did d.on('end',..) from within the closure this listener only triggers on one specific remote's connection ending?