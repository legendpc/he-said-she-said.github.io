How would you serve some HTML pointing to image links in a directory? Assuming no images are added you could read synchronously once and store the generated HTML or pass the links to some template renderer per-request. 