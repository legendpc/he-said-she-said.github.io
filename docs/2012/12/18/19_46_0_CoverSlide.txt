also, protip if you care about performance: most engines use `with` by default, which is a major performance hit for any JS engine. some like jade give you the option of not using with, but has to be done explicitly