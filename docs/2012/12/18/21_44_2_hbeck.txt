for example, using the handy-dandy HyperSearch feature in jEdit on the project dir gives all kinds of results in the node_modules dir when really we want to exclude that from search (which I guess is possible, but a pain)