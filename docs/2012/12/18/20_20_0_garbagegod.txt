I'm running an instance of node on port 8080, and I can connect to it from the server itself (essentially localhost), but nowhere else on the network, connecting on port 80 externally works fine though