https://gist.github.com/507aa15addbee16ec619 - and here, some code which is using tty to launch the vim instance (which works), modified to spawn httperf instead - also... does not work as expected ;/