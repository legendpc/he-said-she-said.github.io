in javascript i can do this:  for(key in object){ console.log(  key + " : "+ object[key]) } and get a string name for key..           async.forEach(object, function(object.property, callback){      // How do I get the key name?            ) }