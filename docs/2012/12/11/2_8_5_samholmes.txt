I expose an `on` remote method via dnode/shoe that attaches a client's method to server-side event emitter. This is fantastic, because no I can emit events on the server and have both the server and clients respond to those events. However, what potential memory leaks could this setup have?