I have a PHP website in which I'd like to implement a notification/chat system. I was thinking to add node (and maybe redis) to the stack to do it. This is how I I'd implement it: User A does something that triggers the notification; the PHP script calls, with curl, to a node.js server; node.s delivers the notification via socket.io. if this makes any sense to you, I'd need some clarifications on how to do it. Anyone who is w