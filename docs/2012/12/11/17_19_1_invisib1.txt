zomg: There are things out there that run on Node like templating engines, etc. Would it make sense to set up a Rails webapp for the bulk of functionality, and create a Node worker to run a templating engine with data handed off by Rails, or should I just cut Node out of the loop completely?