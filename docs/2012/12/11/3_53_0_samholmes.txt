substack: now that I think more about it, it seems like dnode().on('remote') isn't very useful when using shoe, because dnode() is called everytime a shoe connection (stream) is created, there's only one remote per dnode() call, therefore, only one firing of the 'remote' event.