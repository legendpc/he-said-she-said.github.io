CouchDB and some others require some meditation on how map/reduce works, but once you realise it's just "return a list of results keyed by some property" for maps, and "perform some operation on that list to reduce it and get back one or more calculated values (like sum or average)" for reduce, you're dandy