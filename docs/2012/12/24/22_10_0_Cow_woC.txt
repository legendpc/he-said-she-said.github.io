Is there a way for me to specify the base path of URLs when running under Node.js? I am running $.ajax from JQuery. I pass in a relative path and I need it to control what base URL it resolves it relative to but I can't pass it into the function.