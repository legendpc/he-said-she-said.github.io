jcrew: Yes, kinda. I'm unit testing JS code that depends on some external server. Normally both the JS and webapp reside on the same server but in the case of the unit test they do not (because Node holds the JS and the server is running somewhere else)