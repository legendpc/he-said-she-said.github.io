I don't know. I've been messing with Streams as a way of controlling flow, so I essentially want to stream requests around the network to different nodes, a node maybe representing a service or a website.