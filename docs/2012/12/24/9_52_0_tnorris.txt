bnoordhuis: thanks for that catch. would have bit me in the ass later. but unfortunately it's not a fix. if you run `var buf = new require('buffer').SlowBuffer(8); buf.writeUInt32(1, 0);` it will return 1 when it should return 4.