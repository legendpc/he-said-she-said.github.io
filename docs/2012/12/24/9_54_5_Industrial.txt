Can i build an EventEmitter to sit in between two streams (for example a HTTP request and response stream) to apply logic to those streams? e.g. is the request for domain x? pipe it to this 'website' stream and pipe it back to the response.