chovy: Thanks for that! helped with the proxy part. But now my sessions still aren't persisting, even without nginx. It works great on localhost, but on EC2 req.session does not persist between requests. Any ideas why this might be?