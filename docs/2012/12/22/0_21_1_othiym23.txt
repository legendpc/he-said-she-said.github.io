if you're spamming process.nextTick a whole bunch of times in a row to break a computation task down into pieces or whatever (the lazy dev's mapreduce), setImmediate() is a better solution, because it allows other handles to be processed by the event loop