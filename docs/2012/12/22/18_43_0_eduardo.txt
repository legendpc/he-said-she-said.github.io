What are the best practices,if any, around what data structures get passed around. ie: since i can just store anyobject into mongo - do i pass around that object? or is it prefered to keep DB objects and app objects different?