What is the proper approach for writing command line programs with many commands (like git). Commander.js doesn't handle commands, as far as I can tell. Is the proper approach to split each command into separate programs?