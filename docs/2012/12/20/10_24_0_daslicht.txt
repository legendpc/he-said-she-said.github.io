not quite node related but, when you evaluate a page with phnatomjs it should render the page entirely inclusive all js code correct ? and return the rendered html so that i can be viewed with a non js browser ?