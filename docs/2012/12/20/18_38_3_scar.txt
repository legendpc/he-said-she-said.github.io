wilmoore, it seems running more than 1 Node.js is redundant. the "listening server" is created by launching a file.js that thru `node file.js` correct? then i could just write a script to create a listener for the domain as i need fit?