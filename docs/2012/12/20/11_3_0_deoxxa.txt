schlitzer: at work, i've got a few applications that use various wire protocols (some request/response, some streams, some fire-and-forget) and i found it was easiest to set up round-robin DNS between multiple machines and iptables rules to shove clients off to different node instances