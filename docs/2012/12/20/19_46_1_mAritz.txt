V8 question about optimizations: I've read that to prevent changing the hidden class, all members should be initialized in the constructor. Does that still work if the constructor calls methods that do the initialization?