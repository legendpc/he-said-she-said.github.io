hello everyone; when i start the REPL in ~ and try to require a module located in ~ by just require('./mymodule') it does not work; so i assume ~ is not the default dir that the REPL is running in; is there a way around it so i can access ~-based modules? or any other dir?