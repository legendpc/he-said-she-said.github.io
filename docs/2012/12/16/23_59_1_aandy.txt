owen1: haven't used the push ecosystem, but it really does depend on how the redis instance performs. i'm a bit surprised (as you probably are) that they only run one. even their id server runs two instances, but doable? yes