I'm making the assumption that IF there's a package.json checked into the repository, that's the file that gets pushed to npm and not some other package.json (e.g. I don't check in my package.json files)