TehShrike: that's exactly my point. Sax emits open, close, and text. It doesn't emit 'data' the way I want, though. When it emits 'data', the argument is the XML text. What I want is for it to emit { what: "open", tag: "X" } etc.