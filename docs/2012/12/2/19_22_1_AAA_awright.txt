garbagecollectio: First, collection.find() is executed. This means "send a query to the database server, and call this other function {here} when it's completed". Then b() is executed. Then, when the query is complete, that fires the event loop, and your other function is called with the result.