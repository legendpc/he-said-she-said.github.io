I'm not sure sax will handle the speed requirement, but I'll check it if node-expat's deployment problems on Windows shits me too much. The streaming API also isn't quite what I need: I definitely want a stream-of-parsed-XML-bits, not a stream-of-XML-text. 