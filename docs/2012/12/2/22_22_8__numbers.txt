ya. if you're mindful you could change the next variable name. or ya you could get in the habit of typing a little bit more and instead of naming it there just follow the var= definition with;  parallel.next = parallel.slice(-1)[0];