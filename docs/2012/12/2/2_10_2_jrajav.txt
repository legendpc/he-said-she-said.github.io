As written, I'm calling done() on the happy path, and then done(new Error(..)) in the callback that should not be called. However, when I synthesize a failure, it fails the NEXT test in the suite with "done() called multiple times"