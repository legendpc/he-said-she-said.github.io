I'm guessing if I'm using Mercurial then I can specify "repository" : { "type" : "hg", "url" : "http://bitbucket..." } in my package.json -- i.e. the type is "hg" not "mercurial" because it has to correspond to a system command yeah?