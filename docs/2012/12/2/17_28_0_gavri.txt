does calling require() multiple times actually execute the definitions in it multiple times? I thought it didn't, but that's how it seems to be working in my test. console.log() statements are being executed more than once