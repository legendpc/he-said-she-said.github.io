sorella: i had to install Python, then VC++, then tweak a core Python lib so it knew about the right version of VC++, then git clone dulwich (git Python lib), then manually configure the path to the hggit lib in .hgrc... and then... bam, I can now "hg clone <some git repo>" :D