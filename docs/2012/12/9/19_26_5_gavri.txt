how do I differentiate between different exceptions so that I can handle them differently in exception handlers? do JS developers build an exception hierarchy and then use the type to differentiate between approaches to logging/messaging-to-the-user?