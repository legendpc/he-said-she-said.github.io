how does mocha.js tell if beforeEach() or it() functions have specified a (done) argument? it seems like it is doing it before calling the function, because specifying it creates a blocking test, and not specifying it creates a non-blocking test.