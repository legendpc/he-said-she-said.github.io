substack: Why is it that I can't seem to cache the remotes? I have a variable in the outer scope that is set to an array, then d.on('remote', function(remote){ remotes.push(remote); console.log(remotes); }); I seem to always get just one remote in the array. :\