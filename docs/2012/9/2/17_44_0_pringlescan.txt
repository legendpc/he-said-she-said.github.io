rovar, i just remembered I didn't use .json because the postal module has validation regexps you can't really put in json unless you do it as a string… but that'd be a pretty backwards way of doing things