hi. I have a really strange problem: when node.js's http server loads a webpage, jQuery will not run on the said page. exact same file loaded from dhttpd (another WWW server, same machine) will run jQuery just fine.