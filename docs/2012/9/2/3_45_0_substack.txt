garrettwilkin: another more functional way to do merging is function merge (src,dst) { return Object.keys(src).concat(Object.keys(dst)).reduce(function (acc, key) { acc[key] = dst[key] || src[key]; return acc }, {}) }