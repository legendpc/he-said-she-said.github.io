garrettwilkin, that's the problem, if the object is of node.js's affection and you pass it to mongo it'll be sabatoged in transit, you have to use local unix sockets, it'll get fux0red with in the TCP layer