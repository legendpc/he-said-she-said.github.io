rovar, the documentation for how to parse and validate addresses barely uses a database but rather a range of known values, remember they had to do this on very old hardware, so it had to be very efficient