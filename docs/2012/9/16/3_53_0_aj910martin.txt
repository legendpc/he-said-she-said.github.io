Hellos, This has to do intercepting http requests. Assume that a server running node.js is expecting 50 reqs/sec. Now my requirement is to do some pre-processing on these 50 http requests before the requestListener does it's work