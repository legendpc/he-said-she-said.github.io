and additionally, if those objects contain local functions, it serializes those functions as numbered references to the local functions - if the remote server attempts to call them, a "reverse" RPC will happen and they will execute on the client.