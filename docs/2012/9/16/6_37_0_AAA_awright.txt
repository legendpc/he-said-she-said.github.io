I don't want to quite say that Nignx runs circles around Node.js, but the way it optimizes network traffic through the kernel eliminates most all the memcpy() operations and it's just mind-blowing how fast it is