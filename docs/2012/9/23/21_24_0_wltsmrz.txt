You could use Express sessions to attach whatever stuff you need to the session. Then you could parse the session ID cookie from somewhere within your websocket handler. Then you could query Redis using that ID (if you were using connect-redis). 