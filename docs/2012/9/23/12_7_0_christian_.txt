grange: I might be setting up the SSL server wrong in the node app, but I still set 'io.set( 'origins', 'my-app-name.herokuapp.com:*' );' as normal, so I thought that should prevent the origin error...