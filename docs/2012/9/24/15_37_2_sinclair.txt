substack: not only does signalR provide the abstraction (really tight programming model) but it also provides a protocol for clients to conform to, this means not only browsers can consume signalR but desktop applications, mobile, and win8 tiles (of course)