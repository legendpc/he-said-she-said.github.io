it's possible if you have the non-threaded runtime, that a single thread blocks everything, if you have a haskell thread that's doing a very simple but long-running without heap allocation (really very simple, a single recursive function that's completely unboxed by the optimizer)