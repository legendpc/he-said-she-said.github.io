Hey there, I have the following line request(url).pipe(fs.createWriteStream(filename)).on('end', doneFunc); but for some reason doneFunc never gets called, am I doing it wrong? Note the library request library is https://github.com/mikeal/request