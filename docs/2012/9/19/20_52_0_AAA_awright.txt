superjoe: It's the same thing. It creates new definitions of objects in every module, instead of using the same definitions. This means that a string returned from another module will not be an instanceof String