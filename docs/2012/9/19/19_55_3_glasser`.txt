tnorris: So this breaks the ability to do the common Unix practice of "run a subprocess, write to its stdin, but refer to its stdin as /dev/stdin because it doesn't support 'no arg means stdin' or '- means stdin'"