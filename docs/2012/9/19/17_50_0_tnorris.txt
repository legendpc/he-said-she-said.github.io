TooTallNate: sorry, not using data in same process. was thinking maybe a .cc extension existed that could serialize an Object to a Buffer,  quicker than turning it into a string first using JSON.stringify().