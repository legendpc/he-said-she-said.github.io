AAA_awright well i know I can do it with git submodules, but i'd prefer to avoid that if at all possible, all the rest of the deps are just listed in package.json.  Do the git urls not work as dependencies any more?