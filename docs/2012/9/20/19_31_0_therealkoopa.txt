konobi: I don't know if this is going to work for me. I creates a mocks/db/index.js where it is a module that requires mongoose and returns a test mongoose connection. mocked seems to try and look for a mongodb when mongoose requires it.