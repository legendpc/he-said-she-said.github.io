i'd like to ask, is it possible to use libuv in a C/C++ project ( VS ) as an abstraction layer for IOCP ? if yes, the exposed interfaces are the same as node.js or at a lower level? for example for creating a tcp server