bradleymeck: well there are two issues now. First issue is that PROCESSOR_ARCHITECTURE env variable has a very confusing name, since it doesn't tell the architecture of the processor or even OS. The second issue is that node and gyp both use that to detect if the host OS is 32/64 bit