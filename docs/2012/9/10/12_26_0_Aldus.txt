weird n00b question. For my chat app I am tempted by using JS arrays / objects instead of database tables for data I don't want to store (i.e. chat messages). Does this mean I'm going to use RAM memory instead of HD memory? (and avoiding heavy sql queries)