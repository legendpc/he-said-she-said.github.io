unexpected behavior: running `cp.spawn('tail', ['-0f', '/path/to/file.log'])` and producing output. but if I delete the log file node still receives data, and the log file isn't re-created. any ideas why this is happening?