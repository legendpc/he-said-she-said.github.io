JS doesn't know what a file is and so can do nothing to assist with this requirement. You can force an HTML file to download rather than display by setting the Content-Disposition to attachment using a server side language.