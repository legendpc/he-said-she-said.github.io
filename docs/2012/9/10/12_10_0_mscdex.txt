bcave: right now it only accepts streams, so probably the quick and easiest way would be to use something like mikeal's BufferedStream (`npm install morestreams`), use that in ftp.put(), then write to that stream and .end() it