For example Req A does 10 async db calls which each register a callback and increment a counter,, then req B does the same thing,,, but when looking at the output A's callbacks pulled B's events …. 