Protected: also if you're checking against core types, there's Array.isArray(), Buffer.isBuffer(), and the utils core module has other methods for checking other types (i.e. utils.isDate(), utils.isRegExp(), utils.IsError())