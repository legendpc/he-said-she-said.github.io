phrearch: if you are installing in a subdirectory of a module with a node_module directory above it will install to what it thinks is the current module, add a node_modules folder in your virtualenv dir