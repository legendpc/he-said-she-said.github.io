Hi. Does any 1 know why sqlite3 module blocks completly other request from begin when using large query with .each method? I though that node.js should behave more smoothly. But it turned out to be handle only ONE request at time. 