in one of my apps, I have server.js -> require('main_app'), and the main_app.js has a global var db which is the mongo connection. everything else is called into the main_app, and gets access to the db in that scope