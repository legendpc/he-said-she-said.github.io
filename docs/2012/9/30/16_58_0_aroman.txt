hi, I've got an express app, and I'm using mongoDB/mongoose for persistence/sessions. This works great, but I would be interested to know what standard practice for failing on database errors is? Suppose the database is down? Or a query blew up?