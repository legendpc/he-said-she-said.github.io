clever, it seems that the recursive interruptable solution of add2() works fine with next tick, however for 16 million calls, it takes 14 seconds, when if it was inside a for(1 to 16 million) loop it took just 600 ms :P