because probably the most straight forward thing to do would be 1) when a user loads the site, query the DB  2) when the result comes back, emit a bunch of events on an interval via socket.io 3) the client displays those events