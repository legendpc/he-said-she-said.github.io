ya, the only time man in the middle over ssl can do anything useful beyond replay attacks is if it was there when the connection was established. then it can ssl termination and essentially re-encrypt in between.