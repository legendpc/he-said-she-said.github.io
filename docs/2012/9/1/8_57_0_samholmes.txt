Also, if something fails at doing something, there could be a completely different approach to solving the problem. Each module would try to remain as independently reusable as it can for these sort of changes.