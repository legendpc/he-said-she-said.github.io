so in javascript's case this is ensured by simply not allowing js to be re-entrant. It runs until it's done. A synchronous callback is a function executed without deferring to an external schedular to allow for the code to continue executing instead of blocking it