AAA_awright, Right. npm will install the local package, but it still goes to the internet to look for dependencies instead of getting them from the node_modules directory in the package where they're already at.