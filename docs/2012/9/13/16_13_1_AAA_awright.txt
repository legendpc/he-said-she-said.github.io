You define your indexes, i.e. ``SELECT * FROM tabbe WHERE id=:0`` and if that's the only index, then it doesn't even have to do a btree, it can do a hash table, since it knows it'll never need to sort the records by id