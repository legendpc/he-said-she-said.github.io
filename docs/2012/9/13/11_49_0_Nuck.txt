TimTim`: Since the object is serialized (with any session store), no, you'll have to add the methods yourself.  However, the session store is designed for storing data and nothing more, so WTF are you putting methods on it for?