hey all, i've made a socket.io proxy for a legacy service that starts off in ascii but then can optionally re-negoitate to utf-8, should i re-encode immediately down to ascii and then internally encode back up to utf-8 or...dunno