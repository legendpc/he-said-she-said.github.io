tjholowaychuk: thanks i'll look into it, i did notice in the github they say: "An example of a JSON POST with jQuery typically might use $.post(), however once you need to start defining header fields you have to then re-write it using $.ajax()... so that might look like:"