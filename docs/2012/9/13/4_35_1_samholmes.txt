substack: Wait..what happens if you use a combination of dnode(handler) and d.on('local') to construct a local object; will the local constructed in the dnode() handler inherit the methods defined in the 'local' event?