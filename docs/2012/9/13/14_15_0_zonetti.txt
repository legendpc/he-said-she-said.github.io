Nuck, in all the samples I saw on streams, both the streams (read/write) are instantiated on the same server... How can I, at the server that I instantiated the write stream, pipe the read one on the other server?