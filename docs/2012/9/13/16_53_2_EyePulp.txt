wilornel: it's an app that runs JS files on the server, versus as part of a web browser, and has a raft of features added more in line with other server-side solutions, e.g. python, erlang, ruby, etc. Apache is unrelated to node, though apache can route web traffic to a node-based solution.