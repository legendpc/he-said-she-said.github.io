Hmm. If `this` from within the handler is going to be the local, and the handler is called on every remote connection, wouldn't that mean that you're creating a local object for every remote connection?