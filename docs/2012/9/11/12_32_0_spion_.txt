Are there any downsides to using request queuing instead of a callback, except for complexity? e.g. client = service.connectTo('url'); client.dostuff('param'); /* dostuff will be queued until a connection is established */