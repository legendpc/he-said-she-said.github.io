Like, if I do emitter.on(event, cb); where cb is a dnode reference callback, doesn't the emitter store a reference to this callback internally, so even if dnode drops references to this callback, the emitter will still hold a reference to it?