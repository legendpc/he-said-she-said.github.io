hey guys, i need to do the operation "check if member is in a set, if not add it" atomically. i.e. without getting interrupted by anything else. (for the set i use https://github.com/PeterScott/simplesets-nodejs ) so i'm looking for some mutex/lock library, but that might not even be needed?