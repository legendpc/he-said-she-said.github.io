or even better, is there a library that generically converts service.connectTo('url', function(client) { client.dostuff('param') ... }) to var client = service.connectTo('url'); client.call('dostuff', 'param')