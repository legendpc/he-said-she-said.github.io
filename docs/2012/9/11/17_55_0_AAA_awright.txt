tnorris: Normally SIGHUP signals that the terminal to write data to is no longer available, and by default that terminates the process, nohup avoids that by catching it, as well as redirecting output to a file