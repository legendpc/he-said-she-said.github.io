the other nice thing about that pattern is that you can create "class variables" (i.e. state shared across instances) by just defining variables in the module but outside the class, and they're local to the module