so I am used to creating JavaScript "classes" using something like function ClassName { … } - then ClassName.prototype.methodName = function () { … }  - then "instantiating" with c = new ClassName(); - how does this convert to the CommonJS system of doing things?