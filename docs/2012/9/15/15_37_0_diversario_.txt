Is there a way to stub-out what module requires when testing? E.g., module1 requires module2, and module2 requires some module3. I need to make module3 a noop when testing to avoid dealing with network, db and stuff.