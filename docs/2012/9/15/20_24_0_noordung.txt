This is what I want to do: have a directory which is a package, and inside it I want to have another directory which is a package. It would be far more convenient to have that directory exposed through npm rather than an ugly path require...