I noticed railwayjs uses require('vm') which is said to be slow and hogs up a lot of memory.. are there better alternatives to giving a file context without exports/module = function(argments) { .. } ?