other_guy: okay, i might be using the term wrong. what I mean was, for file uploads and such, reverse proxies which buffers the request before sending it to the backend is no good. I prefer to stream the body (or hand over the socket) once the origin has been determined