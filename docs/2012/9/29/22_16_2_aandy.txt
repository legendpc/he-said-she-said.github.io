other_guy: how about "piping", can that be done with HAProxy? because i've been looking for a load balancer which can handle that properly (nginx can't, which is why i have to run a node-http-proxy on top of that)