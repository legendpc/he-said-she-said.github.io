specifically I'm trying to use the query.on('row') event to pass row, result into the callback function and it's telling me that result is undefined; my code is identical to the last code sample at the end of https://github.com/brianc/node-postgres/wiki/Query except the query just does a SELECT * FROM posts