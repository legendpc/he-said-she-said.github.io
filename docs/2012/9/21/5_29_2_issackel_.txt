in node are people using processors that do LESS compilation on the fly when it's requested (for the first time, or doing it when the file is saved in development or what? In django I'd use django-compressor and just do it when it's first requested (and warm the cache)