I've got another question, why is it that Connect's "static" server doesn't allow me to define a prefix for the URL? So that I can have a folder called js/, another one called css/, and serve them both under http://domain.com/static/ or something like that?