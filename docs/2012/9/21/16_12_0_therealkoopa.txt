plato: I've thrown that idea around. I set up express sub apps. Each module in lib is a private module/app for the main app. I could put it in node_modules, but I thought it might be confusing with mixing it with what's in package.json