plato: i set up a Node.js server that just acts as a listener for my Drupal website, and forwards requests to another REST API I have running (so that Drupal doesn't have to wait for the response itself). So Drupal packs up the data into JSON, sends it to Node.js via a POST request.