plato: it works fine if i post directly to the processor, which is a REST server itself. But I want to use Node.js as an intermediary, so that I don't have to wait for a response from the backend for the page to load in Drupal