say i'm using npm, is it generally expected to gitignore dependencies, since they can easily be installed via npm install? and as far as deployment, is there a command that starts node and installs any  dependencies first? or do you just "npm install && npm start" or something?