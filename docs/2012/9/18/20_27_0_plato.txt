so Brightstar i would set req.session.messages = ['Bad username or password']; then when you're ready to display that, do (assuming Jade): var _m=req.session.messages; delete req.session.messages; res.render('templatename', {messages:_m});