no, because the scope of the loop variable is local to every callback in the loop (because you did function binding), all the callbacks get called once and only on of them will call the next eaync step.