seeing something weird with trying to install jade, actually through mocha but it won't install by itself either.  Getting Error: EROFS, symlink '../jade/bin/jade' on npm install jade and sudo npm install -g jade