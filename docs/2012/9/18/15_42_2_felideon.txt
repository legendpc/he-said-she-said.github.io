isaacs: it seems like i had installed node (not npm) from homebrew at some point. not sure if that's how it made it there. but when I installed the node binary from the nodejs website, it didn't put it in /usr/local/bin