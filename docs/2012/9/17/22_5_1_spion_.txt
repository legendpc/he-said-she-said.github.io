gerconfused, so basically, an example get_dimensions function should look like this, function(f, cb) { im.identify(self.source_dir + f, function(err, features) if (err) cb(err); else cb(null, {w: features.width, h: features.height});  }