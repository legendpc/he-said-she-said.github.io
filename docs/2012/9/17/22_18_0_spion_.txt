gerconfused, the logic was something like: Oh I'm passing a different function here, so my function doesn't know its "return" anymore. so I need a new async-y _return as an argument that I can use from anywhere