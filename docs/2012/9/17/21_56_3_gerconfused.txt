Nexxy: so if I'd put everything in one function and call the callback on null, it would step to the next item, but how would I write the line in the async.forEachSeries, do I need to add the callback parameter there?