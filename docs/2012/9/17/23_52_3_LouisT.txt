hm.. according to the mongodb docs "coll.findAndModify({id:id}, [['_id','asc']], obj, options, function (err, res) {});" should work fine, which it does in a way -- it does insert the object -- but for some reason the callback never gets triggered.. any ideas?