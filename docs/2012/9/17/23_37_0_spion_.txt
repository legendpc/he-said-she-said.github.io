actually, I'd do var parallelResizes = 5; async.mapSeries(files, function(item, cb) { im.identify(source_dir + file, cb); }, function(err, identifies) { var queue = async.queue(function(options, callback) { im.resize(options, callback); }, parallelResizes); identifiers.forEach(function(id) { create options object, queue.push(options); }); queue.drain = function() { console.log("resizing complete"); } });