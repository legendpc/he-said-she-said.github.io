you can't return because you have this.get_dimensions = function(file) { /* cool, I can return here normal */ im.identify(self.source_dir + file, function(err, features) { /* oops, i need to return from here, but can't because this is a different, nameless function function */ }); ...