so basically, to run tests you would go into your package directory and type "npm test" which will fire the appropriate command as defined in package.json. I have my `npm test` hooked up to `mocha -u qunit --recursive` for example