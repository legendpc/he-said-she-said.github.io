chakrit: I have a directory in my desktop app /models. Each file in there is a module that I require() and they expose a collection of functions that operate on the database and emit events on an exposed event emitter. I use the model files throughout my app.