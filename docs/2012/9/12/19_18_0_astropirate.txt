Are there any design patterns for handing application state (persisted to a database) for multiple node.js instances? The way I understand it, if I implement some IPC between the different node.js instances, the application state will be only take effect the next time the event loop starts