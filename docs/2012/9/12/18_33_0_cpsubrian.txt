isaacs: I'm trying to use read-package-json without any console logging.  It seems as though you allow for that through optional npmlog dependency. In the git repo, package.json looks fine, but when I install v0.1.5 npmlog is listed as a regular dep rather than an optional dep.