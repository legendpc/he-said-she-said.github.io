I wanna realize something like a server that offers an API for a webapp, so the webapp recives JSON calls from the server, but the server gets its data from XML resources, and filters the data out with stuff like (l)xml . How can I do that?