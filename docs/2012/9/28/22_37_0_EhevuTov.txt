so, you're queries would execute in a group asynchronously and would also decrement a copy of your query.length and when your length becomes 0 emit your 'finished' event and your watcher will then run your calback function