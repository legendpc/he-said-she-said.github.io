Bringing a project up from 0.4.x, it uses the constants module A LOT, is there a best practice for cleaning up `err.errno === constants.EEXIST`? I'm not sure if I should just go around replacing all the constants.xxxx with a string and compare that to err.code?