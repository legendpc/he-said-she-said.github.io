require('./controller/foobar')(app); is what I do for each controller file in app.js. Each controller exports a function that accepts app and adds all the route callbacks to it. Of course the routes are all orginized and maintained in the controller file.