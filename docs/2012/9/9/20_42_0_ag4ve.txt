is there a way to import file global variables as bulk so that i can have different models and don't have to keep doing: var mongoose = require('mongoose'); var Schema = mongoose.Schema; var mongooseTypes = require("mongoose-types"); ..... for every single schema?