does anyone know of a c project that can act as a template for writing a node.js native library? I'm pretty sure you pass in a node.js callback as a parameter, and you need the v8 lib, but what else?  I need to write a PAM auth lib, and don't want to have to use a child process