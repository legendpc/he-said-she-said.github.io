timeturner: If a function has a callback, Bad Things can happen if you throw an error before you call the callback. I'd even call the callback and throw, before I would just throw, and not call the callback