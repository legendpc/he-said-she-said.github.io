How would one go about creating false req/res objects for a `disaptch(req, res, callback)` setup? For testing/etc. (I ask, because there is a lot of potential data that various frameworks might expect, so i would like to create req/res objects that are as "real"