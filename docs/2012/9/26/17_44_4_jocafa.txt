that's something i've been wanting to work on:   some sort of benchmarking utility that gets run with every release and then checks the results into a git branch and then can later be analyzed/graphed/whatever