dudes, look at broadway, then whenever you want to introduce a module that needs some sort of shared state app.use(require('thing-using-shared-state')) then using .attach or .init conventions you can get it.