i need advice about structuring my api. i currently have GET /merges/chainId=1 that return array of hashes. i would like to provide another api that get's array of ids and return array of ids. the return values in each api are different. what should the second api looks like?