whomp: Node is just JavaScript.  If you already know JavaScript, you already know the language.  From there, I recommend starting with the base modules built-in.  The web server example on the home page is actually a pretty good one.