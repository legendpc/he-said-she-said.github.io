I think what you then want is response time, and then yes you can approximate that with js hitting the server for a request, then calculating the time between when you sent and when you received the response