npm root -g shows /usr/local/share/npm/lib/node_modules, but i think my old modules lived in /usr/local/lib/node_modules. which one is preferred? and is it better to move my old libs to the new dir, or change what npm root -g resolves to somehow?