when my code is organized hierarchically into a few dozen files and each file uses some common set of objects, it gets tiresome constantly either requiring the same thing or passing the shared objects through to each module.exports function