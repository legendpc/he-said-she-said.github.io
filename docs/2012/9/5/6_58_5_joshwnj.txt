sente: only real way to do this is to identify functions which take an input, and give an output (and don't rely on anything else being in scope). once you have this, splitting into multiple files is simple