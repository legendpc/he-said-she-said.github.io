sinclair: the embedded device would host the web server and client webpage, it runs linux and will be accessible via ipv4. so, think about a linksys router type of deal, it's got an OS of some sort and a web interface (and therefore a web server) and a bunch of other crap