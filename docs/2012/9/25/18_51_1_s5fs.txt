jarr0dsz: ensure state is managed by the server, the client should reflect state and initiate state changes on the server. state probably shouldn't be reflected on the client unless you get an ack from the server (if that makes sense)