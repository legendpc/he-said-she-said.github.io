larzconwell: do you feel that could be handled in js though? like a deferred file approach? e.g. `var saved = saveFile('file.wat')` returns a promise and `saved.done(function(){ /* do something with saved file /*});`