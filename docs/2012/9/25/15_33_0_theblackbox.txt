hello all, I think I'm getting very confused as to the correct use of socket.io… what I would like to do is to define a couple of custom events on the server that tally on the client and invoke the backbone.sync method I override. The problem is (and I've gone through a few backbone specific modules before deciding to get my fingers dirty directtly), that I do not understand how I should include socket.io client side… it seems as thoug