If I execute queery which takes ~18 seconds or more and Node.JS tries to query MysQL in the same time, seems taht connection breaks,. and w/o errors, seems like node.js is waiting forever for mysql to respond. 