So multiple people can register listeners with a .then() without affecting each other, no matter what they do in their fullfillment/rejection callbacks. The behavior in those callbacks just modifies the new promise they create.