return foo().then(function (fooValue) { return bar(fooValue) .then(function (barValue) { // if we get here without an error, the value returned here or the exception thrown here resolves the promise returned by the first line