I've discovered in the process that even though the writable stream is basically a layer ontop of put() and batch() operations it's actually a much nicer interface to use for writing than doing it direct, just keep a writablestream around and write() to it when you need