Ok, so: use socket.io to connect out… if that works, try layering dnode on top of it, with the interior node passing a callback to the exterior node it can use to create a new stream for bouncy… I need to look at socket.io's multiplexing, if it has any.