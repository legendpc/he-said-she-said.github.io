Gino_: when a new TCP socket is created, you would have to somehow keep track of the socket to be able to refer to it specifically instead of just reacting to some event when something related to that socket happens (incoming data, for example). do you understand that?