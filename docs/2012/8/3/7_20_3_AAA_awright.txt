isaacs: Why is `npm login` asking for my email, and sometime since v0.8.5 `make build` installing npm even though I have --without-npm. And why is npm and blog posts and all this bloat nonsense in the Git repository at all? That really needs to get pulled out of there