samholmes: you would need to fork jsdom, replace contextify inside package.json of jsdom with the path to that tarball, then fork juice and replace path to jsdom with the path to the tarball of your fork, then fork node-email-templates and replace juice with path to tarball of your juice fork, then in your project replace node-email-templates version with the path to tarball of your fork of node-e