niftylettuce: But, even if I could get contextify compiled, how do npm email-templates? email-templates fail to install after it gets to contextify, so how could I simply replace the dependency with the binaries from Benvie?