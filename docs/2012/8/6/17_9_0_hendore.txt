if client one emits "move" event to server, would the server then let all the other clients know that client one has moved? Or should the server update player position, then a few milliseconds later send out to all clients the new positions for each client?