How can I convert a (file) stream into base64 continuously? I'd like to be able to do something like this: stream = fs.createReadStream(..., ...); stream.on('data', function(data) {   output( data.toString('base64') );   });         but it doesn't work