which methods (or libraries if necessary) do you folks recommend for managing fairly complex node apps? from serving pages (i use express) to managing users/databases to simple stuff like password resets, etc.