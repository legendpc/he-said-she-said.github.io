I need advice: I'm creating a library that makes an async request and returns an produces an object once it's done. Should I return the object through a callback, or return a dummy object until the request is done?