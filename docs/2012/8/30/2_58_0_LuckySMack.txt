like what would I do differently if I wanted my controllers/models available on both client/server so the app could handle limited offline capabilities, versus something completely serverside where if i lose internet, i cant use the site.