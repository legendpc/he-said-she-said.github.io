so outside of offline usage, regarding director. when writing my code, how would I make the distinction between server only code, and client/server code? do i just point router to where the client code is? i would want to not load my server code onto the client.