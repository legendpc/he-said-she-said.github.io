miblon: also, try and remember that return & throw shouldn't be used in functions that run async, any function that takes a callback shouldn't need to 'return' unless you're doing fancy chaining or something, and shouldn't 'throw' because it'll likely get lost if it's running on a later tick