fully offline, no. just if the person loses some connectivity. i was actually wanting to use a local copy of couchdb for storage and have it sync with the server. if theres no local couchdb, then it uses the servers couch instance.