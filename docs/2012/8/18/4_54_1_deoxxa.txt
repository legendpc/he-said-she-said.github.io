> When a spider requests an HTML snapshot of a page, the Meteor server runs the client half of the application and returns the full HTML generated by the client code. This version of spiderable uses phantomjs, a headless browser.