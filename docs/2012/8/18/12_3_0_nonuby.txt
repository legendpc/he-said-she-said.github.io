is there any http wrapper that exposes drain events etc.. so if I have a http tailing a log file over long period of times it would be sane memory wise, continuuously dumping to express.js via res.send would cause a memory build up if producer is faster than consumer