chrisdotcode: the only thing wrong is that its unexpected so if, for instance, your array gets stringified and parsed as json later or goes through some other array specific function it is unlikely that the property will go along with it