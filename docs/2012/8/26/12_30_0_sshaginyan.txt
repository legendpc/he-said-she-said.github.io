Hey guys I need to detect changes on a directory and everything inside of it. What would be the best way to do this? Should I traverse the directory and do fs.watch(filename, [options], [listener]) on every single file?