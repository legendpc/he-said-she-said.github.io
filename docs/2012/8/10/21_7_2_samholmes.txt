Wait... what will a token do? The client has the session ID in their cookies. Once the server gets that ID then all it has to do is look up the session data in the session store and store it in the connection's context.