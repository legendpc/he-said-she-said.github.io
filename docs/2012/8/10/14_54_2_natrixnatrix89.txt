But how would it work if I wanted to have a python script that would connect to the same node server and emit messages? Or an alternate version would be having a pc running node and connectiong to node socket.io server to emit messages..