Stephen, in this case I'm just parsing the html, removing all text, parsing for html entities, then blindly running regexps on it to match telephone numbers and such… I'm not performing any I/O other than reading the HTML and making some DB queries.