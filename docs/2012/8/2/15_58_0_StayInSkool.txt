I feel very dumb for asking this but I can't seem to find an answer that works for node 0.8. How does one send POST data? request.write() seems to expect a string or a buffer these days, and i can't see how else i'd pass the `request` lib a data object