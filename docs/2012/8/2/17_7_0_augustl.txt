are there any libraries with a combined node.js server and a web browser lib that does "send data to and from the server and clients" that does XHR but Web Sockets if present? No events/pubsub, just raw data.