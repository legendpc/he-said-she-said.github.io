prettyrobots: FWIW, if I saw "template function" in docs, I'd go looking for a glossary to figure out what that library considers a template function.. confusion++ if it happens to be a templating library