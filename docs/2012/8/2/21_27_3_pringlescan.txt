when using async.Auto() is it best to pass the results of each function using callback, or just pass the error to callback and use variables because they'll execute in a known order and the functions are all inner functions anway?