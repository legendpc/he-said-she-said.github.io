maxogden: finished when i have the stdin and see when it is stream in node and feed i have to collect input it from another program, will multiple 'data' events, so that the 'data' event always be message, or may messages produce called only once with a buffer which contains the whole?