with require.js or dojo's AMD loader, there's a require('text!templates/foo.html') that will return the text of a file as a module's output.  This works in-browser or elsewhere.  Is there an equivalent in node/commonjs?  I've looked but I haven't found anything yet