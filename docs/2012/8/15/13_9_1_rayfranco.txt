Hi there, I've just installed grunt module globally "npm install -g grunt" but if do a "npm ls -g installed" it seems not installed. I only can see it without grepping to installed. Do I have missed a step ? Newbs to Node though. Thanks for help !