Question.. for Nodejs, is there a module level 'global' var? That is to say, if you want a function to expose a bunch of vars to a module, how would you do it without using global? `global.a = 1; console.log(a)` works, but is there a `module.a = 1; console.log(a)` version?