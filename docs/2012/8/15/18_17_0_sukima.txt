Google fail: ubuntu 11.04, apt-get install npm; npm install -g spine.app -> fails with missing 'package.json' in directory . I don't understand. why does npm need a package.json for a global install? (node.js v0.4.9, npm v0.2.19)