freewil: A stream cipher encodes a chain of encrypted blocks, and a block is output every some number of bytes. However, the last block usually isn't filled to the end, so it needs to be padded out to the end before it can be sent out.