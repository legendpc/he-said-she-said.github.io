ok, so the actual connection between the browser and my local proxy is unencrypted, though the encrypted portion is implicit because it's https. hence I have a few extra headers passed to me. If I strip out everything except what the remote proxy wants, maybe that will work.