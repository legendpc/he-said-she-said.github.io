konobi - i don't need it to be easy, just need it to work. the forwarding works if the local proxy forwards the request directly to the remote host, but as soon as I involve a remote proxy, something about the way I'm sending it causes an error to be returned