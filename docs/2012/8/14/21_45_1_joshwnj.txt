addisonj: you could think about installing your modules to a different location with a specific version in the name (eg. `request-1.0.1`) and then instead of doing npm install, symlink to those directories