Bennit: Routing websockets through Nginx is a bit more complex than just enabling it in the configuration, because you've got to know which Node.js instance it's being routed to (if you're using a load-balancer setup) and those processes have to talk to each other