that can't be. I sniffed the stream myself, as it came from the browser. it contained a CONNECT header, a host header, a Proxy-Connection header, a Host header, and then was followed by the encrypted stream