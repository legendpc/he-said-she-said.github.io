I have long requests (>6sec) going to outside server so I need to cache the result to not completely slow down the UI. I use cluster so I can't use regular memoize. Problem is that second request usually comes before first one has finished so get/set isn't enough and I need pub/sub.