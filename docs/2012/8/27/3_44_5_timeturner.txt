the only safe assumption I can make is that if they are making a newer version of SHA then the reason is probably related to algorithm effectiveness and it is probably better to use the latest version of SHA