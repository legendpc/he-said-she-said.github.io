pringlescan: well it would crash eventually anyway. If they don't handle the input to your function correctly, and your function returns an error code instead of the expected output, it would probably cause the application to fail eventually, but only in a more confusing way :D