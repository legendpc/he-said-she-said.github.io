ok, so i've been digging through the issues relating the usage of DELIMITER statements, and it seems that the behavior has been corrected for calling a sproc/proc, but i can't get it to work when creating a schema from node via .sql scripts