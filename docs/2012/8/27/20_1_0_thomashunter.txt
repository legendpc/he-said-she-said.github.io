I want to build a way for req/res web servers (e.g. PHP) to be able to 'subscribe' to events on a Node.js server, so that the PHP server could tell the node.js server to ping it at a certain URL when an event occurs. Basically pub/sub over HTTP. Anyone know of a module for doing this?