that ppa, has node and npm packages, so it means if i install the ppa, ubuntu will be able to install both packages from there right? so before, i had installed npm from the exact place you are pointing me now