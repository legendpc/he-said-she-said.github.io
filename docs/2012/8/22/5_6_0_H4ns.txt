hi - is there a way for an npm package to depend on a non-npm resource at all?  my module needs a c library from github, but i can't figure out how to pull it in it other than using a preinstall script