For example, if i am working on project `foo` and it installs a dependancy it has (call it `bar`), but Bar *also* depends on `foo`, bar won't actually install npmjs.com/package/foo, Bar will simply use the local version of Foo. 