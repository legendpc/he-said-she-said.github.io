Morning everyone ! I have a web page that needs basic authentication (used a .htaccess file for it). What is the "proper" way to authenticate with node ? I tried sending authentication headers but it doesn't seem to work...