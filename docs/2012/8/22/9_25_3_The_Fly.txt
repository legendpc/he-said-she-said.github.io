hello, using node for a project, need help concerning frontend development. quite enjoying using browserify, but want to avoid a large monolithic jslib like jquery... quite like how ender solves the problem, but not sure how to bridge the gap between browserify and ender