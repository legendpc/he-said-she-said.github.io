astropirate: But if I'm running on virtuals with a single core is there still a point for having nginx? I'm sure nginx is faster at static file serving probably because it can stream cached files faster, but is it enough faster than Buffet to be worth the extra config trouble?