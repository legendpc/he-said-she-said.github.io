Yeah, it's the JSON thing that's stuffing me: I think bounce() hasn't set the encoding of the incoming stream, so the data events are getting buffers rather than strings. You then stringily it, and what comes out at the other end is an object. When pipe tries to write that to the socket, boom.