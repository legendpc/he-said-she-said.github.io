futjikato: well, right now I parse the http data manually (pick out the headers, body etc) and initiate a requests by hand. But I figured why not stream the raw data to the http server (which has a much more solid http parser anyway).