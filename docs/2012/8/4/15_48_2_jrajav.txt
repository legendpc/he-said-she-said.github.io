Ingwie: Basically, anywhere that you use a function literal (which is often with asynchronous code), that function has access to all the local variables (including parameters) of the scope the literal is in