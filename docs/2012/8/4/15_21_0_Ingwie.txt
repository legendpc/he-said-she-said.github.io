Gloorian, so if i ran the script as option like var child=exec("path/script.sh --options" function(e,so,se){}) it would be wrong? but the problem is that the script i am executing is quite large and long, so I cant put into a single string/var.