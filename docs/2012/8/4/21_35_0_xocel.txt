to the most part. Its using fs.readdir's callback , function(err1, files) { }. The for loop is within the callback so will only get run once fs.readdir completes. Not exactly what your after but demonstrates the concept