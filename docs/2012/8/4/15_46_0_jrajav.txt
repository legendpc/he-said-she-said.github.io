The main point to understand about closures in this context is: if you have a function with a callback parameter, and you call *another* asynchronous function inside THAT function, you can use the callback parameter inside the anonymous function that you're passing as callback to that second asynchronous function