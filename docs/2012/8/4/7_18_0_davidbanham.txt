Yeah, exactly. Want to hash a string to get a UUID. The hash gets passed around as the public identifier, then we can also authenticate the administrator of the resource when she presents the correct input string.