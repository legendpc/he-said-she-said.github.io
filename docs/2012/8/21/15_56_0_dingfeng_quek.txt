have a problem with https.request(options, ...               : when specifying headers under options, the header-keys are converted to lower-case. doesn't feel like the right behaviour - might as well make it as strict as possible, even if the RFC states that headers should be case-insensitive