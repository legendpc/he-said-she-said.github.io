I need to do some CPU-bound work in node. i can't do this in the node server process because it would block, obviously. What are folks doing in this sitauion? having another node process doing just heavylifting and talking over using some IPC/RPC system?