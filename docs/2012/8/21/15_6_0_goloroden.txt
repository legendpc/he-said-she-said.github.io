anyone but me having problems logging in to npm using "npm login" on the command line? it always tells me that my credentials are wrong, but they aren't (I can login to npmjs.org using the same username / password)