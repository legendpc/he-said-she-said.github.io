tesseract_ocr doesn't allow writing to stdout, only to a file, I wanted to offer streaming in my wrapper, would it be insane to open a pipe to the process and give the named pipe as the filename for output to the app?