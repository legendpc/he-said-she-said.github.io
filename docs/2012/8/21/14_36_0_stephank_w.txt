roastbird: Odd! I haven't touched our code in a while, but it looks like I'm not doing anything special, not even sending any headers. It's a plain GET request with query string parameters. I do use `urlencoded`, though.