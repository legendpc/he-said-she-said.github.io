landinar: hmm... well, in that case you could do per request. i mean, it would scale well enough with a slow rate like that. you would add some latency doing it per request, but it might simplify the implementation