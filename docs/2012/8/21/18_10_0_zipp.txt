So say I have /user/:id, I have a redismodel object, and an inherited user object. Instead of using app.param (which I could do as well), I have a middleware that is activated when it sees a variable, db dependent object, and checks the DB for it, else errors out in some graceful fashion