Wrapping a terminal app in Node.js and writing the output to a fifo and reading from it only takes 140ms longer according to time than executing it without node… that's really not too bad when stdout isn't an option.