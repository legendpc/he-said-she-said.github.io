pace_t_zulu: it probably has a higher level abstraction than just handling each row event one at a time, but if it doesn't i suggest you build one so you can run the whole query and get a single callback with all the rows