Since in my app (PHP) I store the ID of a user, to use later on with inserts, perhaps I should stick to letting PHP just do the inserts to the db as it was (using ajax), and just using socket io to emit ajax success.