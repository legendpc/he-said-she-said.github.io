pace_t_zulu, so if its a long polling server, you will want to have a copy of that  'res' object from line 35... but instead of calling res.end, you'd call res.write, and write to res every time you get an event with new messages, as long as they've changed. 