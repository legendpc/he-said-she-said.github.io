make setInterval work outside of  requestFunction and even the http server..  and when you get a NEW message, add it to a list of messages in self..  then when you get a serverRequest, send down the list of messages then clear it