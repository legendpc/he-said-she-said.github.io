but yeah the gist of it is that you ask for a socket descriptor from the operating system (which is an unprivileged action), then ask it to bind to a specific port (which is a privileged action if port < 1024), then drop privileges