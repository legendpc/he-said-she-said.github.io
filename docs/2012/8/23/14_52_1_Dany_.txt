@deoxxa, The two variables are received, I can't see that in the terminal "args" [{"user_id":"15","group_id":"2"}], but when I print them:  console.log(data.user_id+ " is joined to p" + data.group_id + ".");