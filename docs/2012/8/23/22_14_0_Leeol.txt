But say both `/node_modules/bar` and `/foo/node_modules/bar` existed, file.js's require will always resolve to `/foo/node_modules/bar`. This isn't bad per say, but i need a way to specify a starting point, so i would specify `require('bar', '..')`