livinded if i set the build a binary on local machine, upload it to the build server then have npm spawn something during the install lifecycle that calls it, it would be non-trivial to detect and still use the code from my local machine rather than build server