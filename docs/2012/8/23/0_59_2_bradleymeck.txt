would probably want to move to something else but we basically require the _changes feed unless we want to write our own replication and semi-realtime data feed for a db without an orm (I am not writing a postgres addon)