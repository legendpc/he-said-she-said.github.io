I have an HTTP server (using express but I think it's not relevant to this), and I'd like to know when a client has closed the connection before the response has finished. Which events of the request will tell me this ? the 'close' event of the request only fires when a timeout happens...