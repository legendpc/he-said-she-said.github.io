therealkoopa: The problem with templates is that all of those vars that get injected are dynamic, and could be computed by running any number of functions, loops, db lookups, etc... so you could end up hitting io wires to fill in template values, and that introduces a lot of overhead.