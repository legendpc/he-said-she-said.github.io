when I do: npm install package-name it does check it out property and move it into node_modules. But when you do: npm update package-name it checks it againts the npmjs repo and doesnt move it to the node_modules folder although it cloned correctly in the ~/tmp/npm-*