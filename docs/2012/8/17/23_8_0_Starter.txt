From official example: socket.set('nickname', name, function () {   // this kind of emit will send to all!          io.sockets.emit('chat', {  msg : "naay nag apil2! si " + name + '!',  msgr : "mr. server"  });  });