so my problem isnt that its not cloning the private git repo properly but its because it still checks with https://registry.npmjs.org/packagename/version for it and ultimately does not move it from the ~/tmp/npm-* to my node_modules