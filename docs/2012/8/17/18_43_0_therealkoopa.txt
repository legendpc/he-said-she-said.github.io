ericelliott: Sweet, I'll check that out.  I only have four jade files (layout, index, login, register).  Once they are in, it's a single page app, just sending json objects.  I was just playing around w/ testing and noticed that jade was slow as balls compared to sending in the response directly.