but from that code i can't quite figure out why addedPresentationToUser would be called more than twice per route call. (except of course if user.save calls the callback multiple times for some reason. no idea what user.save is from here ;) )