well, when i do npm install appjs, it freezes (left it for like 30 mins, says it's still downloading the package) and when I dl the tarball and run app.js I got libc errors (from the node bin) so I symlinked that and now I get AppJS needs node v0.8.x (which i have running)