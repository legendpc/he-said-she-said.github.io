maxogden: at this point, whatever works. Client is sending in a variable length xml message. I want to read in data until </foo> gets received, send the xml message in the request body of an HTTP POST, and write the HTTP response back to the client.