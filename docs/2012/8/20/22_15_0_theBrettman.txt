i think the error isn't in the webconfig but server.js. I'm using the express site template in webmatrix. webmatrix says 'static' is a new reserved word and should not be used as an identifier. it's in the app.configure function: app.use(express.static(__dirname + '/public'));