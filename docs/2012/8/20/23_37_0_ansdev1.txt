hi, i've installed nodejs via the only windows installer i could find that worked.. it installed npm so that i can run it from inside /prog files/nodejs/ and this directory is also in my paths env variables so that "node" works... but npm still doesn't work from outside this directory