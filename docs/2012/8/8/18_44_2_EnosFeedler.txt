livended: ok thanks.  from the docs it says "Note that once a single socket has been sent to a child the parent can no longer keep track of when the socket is destroyed. " does this condition revert when the child is killed?