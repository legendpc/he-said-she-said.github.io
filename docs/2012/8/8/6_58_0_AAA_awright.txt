jondot_: the function presumably will make multiple calls the service, and those will have callbacks. But you will want to keep track of them, and then call `callback` only when all of them have finished