if i'm understanding this correctly, every call to .default returns a new instance of some wrapper... so for every test you run you would just need to create a new wrapper with a fresh jar and the code inside those tests would share the cookies they generated without polluting your other tests 