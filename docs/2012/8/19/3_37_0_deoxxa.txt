deltab: basically you put messages into the network, then fetch them by id. so you'd split up large files into smaller parts and push them into the network and they'd be stored at various nodes based on the proximity of their node id to the hash of the message.