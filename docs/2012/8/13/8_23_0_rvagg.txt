EricBlade1: they are if you only need a single instance of the module, but if you're wanting to export something that can be instantiated multiple times in the one app then you'll probably want to use the prototype stuff