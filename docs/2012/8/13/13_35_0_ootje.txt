since node is single threaded: is there a simple framework for one high i/o thread to push worker items (no return/responses requied) to worker nodes?  i've tested using redis queues (rpush, blpop) but cpu usage goes OTT with the volume i'm testing with now :\