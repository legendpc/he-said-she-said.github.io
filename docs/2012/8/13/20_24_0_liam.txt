is this a bug? var req = require('https').request({host:'wepay.com', port:443, path:'/', method:'HEAD', agent:false}, function (res) {console.log("done " + res.headers['server']);}); req.end(); ....... it doesn't return anything, and after a lot of minutes sometimes it returns error: socket hang up