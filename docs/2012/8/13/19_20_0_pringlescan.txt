I've been having trouble running my code as a batch job when iterating over files or databases using the Async module. I was thinking of just exposing it as a web service (since I have to do that anyway). What's the easiest module for a simple web service that will only be used internally?