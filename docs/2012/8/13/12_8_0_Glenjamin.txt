ok, with the usual disclaimer that security is you're responsibility not mine.... XSS => use a templating language with default escaping, SQLi => use parameterised queries, CRSF => only modify data on a POST, and have single-use tokens in forms