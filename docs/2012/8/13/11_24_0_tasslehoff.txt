When someone clicks a link on my site that points to a folder, I want to call a shell command to tar that folder to /tmp, and then serve the file. Is this a matter of using exec and then some static file server module?