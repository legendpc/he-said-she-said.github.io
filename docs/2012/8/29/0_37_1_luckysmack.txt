im thinking i would have to put together some kind of package the user can install, that would have couchdb in it. the db's sync normally. if there is no local, it goes straight to the remote. if there is a local instance (they installed the app) then it saves there and couch syncs it.