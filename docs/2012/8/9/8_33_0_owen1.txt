ok. but my question is why does node use keep-alive? is it so the client will be able to make additional requests without the http hand-shake or is it so the server will be able to send data multiple times(streams?) in a single request?