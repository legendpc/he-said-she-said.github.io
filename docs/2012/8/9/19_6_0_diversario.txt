Ok, so with VM, the sandbox properties aren't shared with the "parent" script. And changes are copied onto sandbox object after it runs. Is there a way to poke around in the context as it runs? I need this for testing.