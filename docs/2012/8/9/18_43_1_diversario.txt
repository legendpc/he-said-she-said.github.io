Using vm.runInNewContext,(myModule) I want to check the global variables inside of myModule. It appears that after being initialized, they aren't reflecting changes that module's function make to them.