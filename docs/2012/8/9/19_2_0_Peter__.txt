bradletmeck: That's for the server end, right? I'm trying to have the client just send its headers. Like http.request(...).writeHead(200) would be exactly what i need, but I can't seem to find anything like that.