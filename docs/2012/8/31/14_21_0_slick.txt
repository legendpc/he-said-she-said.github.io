Basically, I'm passing the Express 2.x req object to Formidable and the callback never gets called. I've attached events to formidable and the 'progress' event gets called once (0 bytes of x bytes in the event) but nothing else gets called.