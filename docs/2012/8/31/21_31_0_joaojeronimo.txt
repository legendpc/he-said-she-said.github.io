wltsmrz, but that's only for sending messages... not quite emitting stuff... I'd have to send messages with a prefix and on the server have a switch block to filter the prefixes just so I can know which message goes where