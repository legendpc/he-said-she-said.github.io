cjblomqvist: basically make .pipe emit 'pipe' to a readable stream and then emit 'pause' 'resume' 'data' and 'end' if its only one way if you need to consume stuff, listen for on('pipe',parentStream) and hook up appropriately