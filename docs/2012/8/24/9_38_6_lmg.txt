zomg: that nested class has e.g. some async http or sql jobs to do. some of those emit 'error' events, some others throw exceptions. I'd like to wrap it all nicely and pass 'error' events to my main app. for logging and handling.