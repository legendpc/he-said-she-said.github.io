question: I'd like to write an API that parses a stream of CSV data, emitting 'row' events for each row of data it finds.  Does it make sense to implement this as a writable stream?  If so, are there any examples for best practices on how to do something like this?