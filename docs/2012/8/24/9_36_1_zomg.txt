lmg: you should probably take the approach used by other nodejs code, and not throw exceptions. Instead, if you have a function which takes a callback, you would pass the error which occurred as the first parameter to it