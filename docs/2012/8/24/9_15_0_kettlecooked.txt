Hi there. I have Ubuntu server. Is there a way to use the binary files (the "Linux Binaries (.tar.gz)") found on nodejs.org? I've managed to download them, but I can't call the node bin file, even when typing "./node" while in the bin folder. Any hints?