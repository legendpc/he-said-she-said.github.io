and much of the JS-businesscode uses methods of a framework, so the first step is to get that framework running witinn nodejs without changing it. So it is no option to insert moduules.export.FRAMEWOK_CLASS_1 or something like that, e.g. to get require runing. so I try to eval them ....