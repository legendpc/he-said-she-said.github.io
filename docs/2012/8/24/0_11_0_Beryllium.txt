but anyway, as for ansdev1's question … I'd imagine that browsers tie the cookie to host & port, so you'd need some way to reference the session out-of-band of the HTTP request - not sure the ideal solution to that. And like I said, I haven't used node, so I could  be way off base.