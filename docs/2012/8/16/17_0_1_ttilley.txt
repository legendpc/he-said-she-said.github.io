RLa: i was hoping to discover something like `resourceful` but abstracting out in-memory and mongodb rather than in-memory and couchdb. i can use postgresql, redis, and mongo... but not couch. there's a half-written mongodb shim for resourceful but, well... it's half-written. ;)