alright - /usr/local/bin/node/node -v works… so I know node is in there, but for some reason I can no longer run node globally. my .bashrc and .bash_profile both have export PATH=/usr/local/bin:$PATH