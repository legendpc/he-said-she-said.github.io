hey all...  I need something like a promise, but I would like to queue up multiple functions to be executed when the promise is fufilled, or if I queue up a function after the promise is fufilled, it executes it immediately. 