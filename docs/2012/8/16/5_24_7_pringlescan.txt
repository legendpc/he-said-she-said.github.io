My module has data sets in JSON (non-changing)… and I know Node caches modules when they're first loaded. How should I include the JSON data, since using the fs functions will impede node's ability to cache the code, no?