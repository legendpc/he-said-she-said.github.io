pringlescan: Well, there might be a good use-case, but most of the ones I can think of involve storing runtime configuration in a file (which libraries don't do), or requiring() a file at runtime, which is also a no-no