if it would, I'd just prepared statements of some kind to be sure nothing crazy goes on. This is the reason for why I choosed to store "online users", "users count" in internal arrays on server-side instead of in a database