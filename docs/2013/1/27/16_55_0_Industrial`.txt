I was thinking 2 implementations for save, one to mongodb and one to the server from the client through a websocket, which returns an error if the server side validation fails, so I could handle e.g. authentication/business logic still on the server.. but write the schema up once =)