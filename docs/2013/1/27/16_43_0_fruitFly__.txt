I understand the process of requesting and changing the protocol. But what technically allows websocket to be a faster protocol than http? Does it not pass through the same routers etc. to reach the destination, or is it just that the parsers have to parse less application data?