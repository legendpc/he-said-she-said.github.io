fruitFly__: frames are totally NOT A THING because we are building on top of TCP. TCP is a connected, reliable, streaming protocol. You're sending a stream of bytes, and they eventually get there, in that order. No packets, no frames. Those things only exist at lower OSI levels.