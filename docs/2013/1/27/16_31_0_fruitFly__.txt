ok.. so typically there is http layer then tcp layer then ip layer...... To initialize a websocket an http would have to be sent first, but then after how do the frames look? I imagine they still must have http... but what is technically different that makes it a socket?