Industrial`: people do that sort of thing with backbone a lot. That's not technically running the same code on browser and server, but it is just the fraction of the ODM/ORM the browser actually needs, and easy to shim up via REST to a full ODM/ORM on the server.