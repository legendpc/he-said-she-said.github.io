a websocket init is something like this: client requests a websocket => http request => server upgrades and removes the http parser => http response => client now knows the tcp socket is no http socket anymore => send data over tcp via the websocket layer