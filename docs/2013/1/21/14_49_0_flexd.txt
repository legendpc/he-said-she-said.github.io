I think a few of the "clientside" javascript frameworks (like Backbone, Ember, Angular and so on) do both clientside (full javascript, just json/data passed over the network after the initial load) and serverside (everything generated on the server and sent over as full HTML)