niix: What you could do, which is quite normal, is to both minify your javascript and css (essentially remove as much whitespace and other stuff as possible), as well as concatenate them into one js file and one css file.