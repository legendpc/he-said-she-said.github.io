cellvia, loading about:blank then modifying the page and finally rendering it? no, I just thought of the idea now and tried it in chrome (you can change document.body.innerHTML). and phantomjs is a regular webkit so everything that normally works in other webkits should work fine.