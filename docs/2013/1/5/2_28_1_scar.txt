othiym23, yeah this is by no means a huge deal, but would be nice to have these requests send to the server... any external source provides a url, and a percentage to the tenths point gets returned, rather than serving all the JS code