hmm, is it possible to re-require a file in node? i'm trying to create something like 'config reload', and my config file is just a node module, but it seems that it is cached so new content isn't visible after second require