I currently only intend to collect the data of the user, will be using node to hit the graph api and fetch data and store it in datastore. But I can later on use something like jade to render the data, as well