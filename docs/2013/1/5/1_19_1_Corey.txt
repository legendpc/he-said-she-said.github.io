I've got a strange issue wherein I've set Apache to proxy a node app.  The node app remains responsive on its port, but times out and eventually gets a 503 when viewed through the proxy.  Restarting the node app resolves it, but how do I fix this longer term?