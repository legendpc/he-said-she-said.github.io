geNAZt: Eh.. it parses the values from OAuth, yeah, but to get those values it needs to talk to the server. That makes sense, doesn't it? Whenever you're using an external resource you'll get delays, be it authentication, remote databases, reading feeds, etc.