I have a question about sockets. A lot of times, I get a 'close' event without receiving an 'end' first. Why would that happen? If the other party sends me a FIN packet, then I'll get an 'end' event. Why else would Node.js decide that the socket is closed?