relatively new to all this. but I have an amazon ec2 instance running with an elastic public IP. I have two node instances on different ports. I have nginx for reverse proxying, but I can't seem to hit a subdomain of the ip address