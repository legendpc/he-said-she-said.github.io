hmm, so there is a need for server-side middle-ware applications in order to proxy a websocket connection to a standard tcp connection. This means it's difficult to make javascript clients for existing (non-web) servers without going through extensive hoops to do so