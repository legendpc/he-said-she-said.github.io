geNAZt, this works, but if I put process.exit() on the end, it doesn't. I gotta run for a bit, will bbl. var fs = require('fs'); var w = fs.createWriteStream('log.txt', {'flags': 'a'}); w.write("test");