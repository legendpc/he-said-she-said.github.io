hey, I'm trying to attach a node debugger to a running process to debug some hard to find issue. it looks like I can get it into debug mode by sending it SIGUSR1, but how do I attach the debugger after that?