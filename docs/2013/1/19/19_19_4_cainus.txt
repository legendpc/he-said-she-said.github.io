isaacbw: say I'm writing a mysql driver, for example... I want it to auto-reconnect when it disconnects.  what do I do with all the SQL calls it gets while it's reconnecting.  I can imagine some possibilities, but is there a standard pattern?