I have a script written in node where it's only job is to listen as an http server, taking incoming get requests containing a url and fetch the url's contents and return it back to the client as an http response