(though, some framework by default makes all the requests non-simple -- by introducing custom HTTP headers which is another clause for simple request -- you can only use a small subset of http headers in simple requests)