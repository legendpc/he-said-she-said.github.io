lord4163: CPU intensive tasks aren't the forte of NodeJS, after all, we are running a single thread per CPU (if you cluster the application). CPU intensive tasks, like that, can be done by spawning a process in NodeJS to do the work and update that way, but you'll be OS locked if you do that. In PHP, you can actually just send partial updates using echo and flush and have your JavaScript update 