on the plus side you can handle errors only once - on the minus side, you must somehow tell the chain that its going to end so if an error happened it should report it somehow, not wait for some possible additional promise that might handle the error.