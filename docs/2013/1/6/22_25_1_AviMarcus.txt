3 apps, none public. 1) CLI stock data - grabs yahoo stock data and returns annual yields for a given ticker and start/end time 2) fetch then import a 3gb CSV file into redis + updates 3) a REST API for my client side, built on restify