with harmony proxies it will be quite easy to auto-convert callback-style functions to promise style and use both... I predict (lol) that ultimately (after ES6) a lot of node code will switch to a promises + task.js style