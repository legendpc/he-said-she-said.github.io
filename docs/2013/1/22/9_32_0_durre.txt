SomeoneWeird: if a client with a specific token sends 100 request at once, I might want to throttle these requests to 1 request every 100ms. also, if a client makes 100 requests / second when the limit is 100 requests / hour I want to end the request saying that the limit has been reached