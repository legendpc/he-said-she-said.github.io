Nexxy: I'm hoping that I could examine the v8 variables with gdb to determine the approximate location in my .js code where the execution is going circles. Other option would be somehow to get inside the node.js event loop to see what's in there.