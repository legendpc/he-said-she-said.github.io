Is it acceptable to start a socket connection, and then use that to emit an event? e.g., socket.connect( someport, somehost, function() { this.emit( 'connection'); } ). The only reason I ask is because the 'connection' event doesn't seem to be emitted.