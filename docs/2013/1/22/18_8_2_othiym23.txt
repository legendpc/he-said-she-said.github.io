but if you're looking to do less typing for a large set of files in a single module, the standard response is that that's a code smell, and the package should probably be broken up into a set of smaller modules