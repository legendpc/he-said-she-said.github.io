quick question for nodejs.. if I have an express app.. simple one with like app.get('/hello', function(req, res){ console.log('hi'); var variable = undefined; var hello = 'world'; res.send(200); }); <-- why that request to '/hello' be blocked until we get to res.send for the next client?