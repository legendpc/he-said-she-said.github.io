davidcornu: I'd have to look at the implementation of suite.run (I use the BDD interface), but it's possible set2 hasn't had time to do its bookkeeping and report at the point at which process.exit is called