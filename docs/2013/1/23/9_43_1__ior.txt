okay, let's pretend you're writing a webserver, and you allow users to upload a file that you then write to disk. let's pretend you use fs.writeSync to write the file, and then let's pretend a user on a 56k modem uploads a 2 gig file