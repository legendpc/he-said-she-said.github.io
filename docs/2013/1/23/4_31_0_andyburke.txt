Hey guys, I'm running node 0.8.18 on both my local machine (OSX) and a hosted server (Ubuntu).  On OSX, response.req.connection is a net.Socket, but on my linux machine, response.req.connection appears to be something else, which has a member 'socket'