othiym23: hi, sorry for the delay, but in response to your comment about ` var client = /* init code */; var Dal = require('./dal'); var dal = new Dal(client); var user = dal.getUser(userId, function (err, user) {});`, what did you envision the `var dal = new Dal(client)` bit doing? Specifically, what is 'client'?