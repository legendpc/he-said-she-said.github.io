Omega_: yeah, and that is g-r-o-s-s if you have a long execution chain, so you end up doing things like [promisedFunc1, promisedFunc2, promisedFunc3].reduce(function (last, next) { return last.then(next); }, Q.resolve(initialValue))