Node.js is a runtime that combines a JavaScript interperter / compiler, a low-level evented platform abstraction layer (libuv), and some other statically-linked libraries that it needs to have a complete standard distribution (most notably OpenSSL)