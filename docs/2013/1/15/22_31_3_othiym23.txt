squeakytoy: the alternative is to look into domains (http://nodejs.org/api/domain.html), which will adapt exception-throwing code into an evented paradigm, and also provides helper functions like domain.intercept to DRY up boilerplate