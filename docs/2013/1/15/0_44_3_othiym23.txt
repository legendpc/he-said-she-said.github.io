so if you have a var declared in function scope, wrap it up in a closure via a nested function, and then return the closure while the same scope changes the varialbe, it will change in the closure as well