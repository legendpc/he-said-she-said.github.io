substack: Can you point me to something that describes paths within browserify? I am trying to do a require('./app') from within a html page, and it can't find the module. If I just load everything from within the bundle, it finds it.