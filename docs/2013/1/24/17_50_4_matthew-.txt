err, npm seems to actually source in README.md, and if your README.md gets too big, npm then blows up when it tries to run any script with "execvp(): Argument list too long". Anyone else come across this?