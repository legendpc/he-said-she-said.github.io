i'm adding some more locking around pack_() and unpack_() in npm to fix the race condition, which helps, still sometimes the pack() method destroys the tarball just as it's being unpacked to the destination