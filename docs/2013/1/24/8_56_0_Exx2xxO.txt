Hello node ! A question aboute node's filesystem: Is there a possibility to detect windows system folders? Actual I use readdir and detect all folders with stat.isDirectory() but it shows all folders and I want to hide the system folders which are by default not shown by windows..