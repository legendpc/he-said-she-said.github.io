if the latter it may still confirm to some schema like scuttlebutt ( https://github.com/dominictarr/scuttlebutt ) where it's either a hash representing the vector clock or a triplet representing an update