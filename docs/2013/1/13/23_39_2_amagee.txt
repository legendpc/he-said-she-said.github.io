out that section of code, everything is fine, but interestingly, if I comment out just the code in the handler, I still get performance degradation and an eventual hang, suggesting the problem is due to the event listener mechanism itself and not the handler.  Does anyone have any ideas?