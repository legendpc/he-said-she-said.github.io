would there be significant performance gains from hashing passwords only on the client's machine and then storing that directly in the db, as opposed to sending the (encrypted) plaintext password and hashing it on the server?