kgee: take for example fs.open. fs.open (javascript) calls into the filesystem "binding" that is in node core (C++). the binding then calls the appropriate OS-level file open mechanism. for Windows, it's IOCP iirc, for anything else a thread from a thread pool is used to execute open()