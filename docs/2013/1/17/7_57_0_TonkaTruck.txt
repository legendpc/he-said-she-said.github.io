segomos: Would you say it's best to never have to call a callback with anything other than Error?  Or is it common to create custom error objects to clue the module's client into more info about the error than just a string?