Ah, so along the OS -> V8 -> Node stack I'm working with, Node (hypothetically) requests a file. V8 makes the OS call, which spawns a thread (allowing V8 to return/continue)? Is that how it goes? I still havent discovered how the OS returns the file to JavaScript-land