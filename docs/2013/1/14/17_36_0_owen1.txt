i have an http call that return a big json file. it's slow at the http call and in the parsing phase, so JSONStream is not very helpful. is my only option is change the server that return that api and make sure it streams this json instead of sending it back in 1 chunk?