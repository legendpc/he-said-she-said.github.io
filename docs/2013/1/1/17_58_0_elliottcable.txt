I'd *like* to use EOF as an input-seperator (for multiple files passed in to a running interpreter instance via stdin), but receiving one EOF apparently closes the stream so it can no longer be written to by another process