been trying for some time to just have a username/password form that when you submit, the values go into Redis. What I'm confused with is....well, the response.end() function, I don't know much about how HTTP works so Node requires a strong knowledge of that it seems