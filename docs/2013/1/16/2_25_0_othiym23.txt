aroman: the module using the DAL needs to have the collection injected somehow, either via initialization or by being object-based itself, and that's where it gets the collection / connection it's using