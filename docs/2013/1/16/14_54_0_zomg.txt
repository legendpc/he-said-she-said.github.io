would probably be a good idea to implement that on the client too to avoid the traffic altogether, assuming of course they aren't trying to break it by manually sending the messages instead of spamming the button or w/e