launching it normally in terminal works fine. and stdout gets all the data (right before the exit event, as mentioned earlier). right now i have config error on the server (on purpose) so that it will exit faster, because otherwise it runs forever (or until i ^C it)