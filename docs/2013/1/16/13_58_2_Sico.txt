The JSON object is created from http.get method that fetches the object from a PHP API. I assig the about to a var and send that var to an EJS. I then try this <%=data.title %> which returns "undefined"