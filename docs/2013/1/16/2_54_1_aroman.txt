since it's really a question of setting a template rendering option in express (the template engine happens to be jade). so you'd have to look up how to generically pass template rendering options through express (express docs) and then what variable you actually need (jade docs, presumably)