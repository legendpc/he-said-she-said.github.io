Is there a proper/better name for boolean text searches, eg: "Search for this" +"and this" -"but not this"? I'm trying to find a node module which supports boolean text searches, but because a "boolean search" is something very different, I'm having a hard time finding hits