thats the whole point of all this, it is because arrays are objects that makes it hard READABILITY wise to distinguish if x.shift() for instance, is x an array? array-like? just an object with shift property? add in bracket notation ? its like what?