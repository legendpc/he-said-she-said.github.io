I get the impression the session id cookie is encrypted with a secret so the browser doesn't actually know the session ID and I can't just send it as the first socket.io message and let the server pull that session to verify their identity or something 