mainly you have to admit, it makes x << 1, x stand out more when scanning code, makes it easier to know x is an array, and not some array like object, or some object that happens to also have a similar method name to an array etc