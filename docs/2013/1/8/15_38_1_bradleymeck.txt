ozette example args you are passing? http://nodejs.org/api/fs.html#fs_fs_symlink_srcpath_dstpath_type_callback seems to imply something about Windows needing absolute paths? (also remote paths probably always going to fail)