Tobsn: it's a meta problem, really. I was wanting to check out zombie, but the installation fails because the contextify build fails. It seems that node-gyp doesn't include node headers file (node-waf builds the package successfully)