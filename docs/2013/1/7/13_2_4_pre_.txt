hello. I have a package, npm linked, and in that package I load a file of the package, for example: ./etc/aaa.ejs. However, when I load this package on another app node crashes because it can't find that file. How can I reference the file _in_ the module? Thanks :)