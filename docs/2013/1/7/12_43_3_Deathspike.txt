mpajor_op5: You can just place open connections into different "groups" (make a group like a key on an array, for exmaple) and push content to the appropriate connections. I don't see the problem here at all, the client can just JSON it back and execute the stuff.