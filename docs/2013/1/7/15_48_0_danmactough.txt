Deathspike: You can do it by proxying the query method. You're app wouldn't create the connection but instead call the proxied query method directly. That's where the connection pool would get managed.