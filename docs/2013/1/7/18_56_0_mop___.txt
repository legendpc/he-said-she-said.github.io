hi … i am trying to share a session between two node processes (one is the webserver => express, one is a websocket server => websocket). it works now but i can't figure out how to extract the sessionId from the signed cookie….sessionId = request.cookies[i].value.split('.')[0].substr(2);