Deathspike: fyi…whatever the "s:" in front of the cookie does but requiring utils = require('express/node_modules/connect/lib/utils') and then utils.parseSignedCookie(request.cookies[i].value, secret) does the job :) ty again