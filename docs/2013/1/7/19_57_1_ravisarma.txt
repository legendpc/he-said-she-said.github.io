But if I do require('express'), node does not find it from /x/y/node/lib/node_modules/express (where I can see it). If I strace() the node process I see that it does not search in this directory at all, despite what this says (http://blog.nodejs.org/2011/03/23/npm-1-0-global-vs-local-installation/) and despite the fact that 'npm' puts it there