CloverSlide: The thing that bugs me the most is that you can't chain anything. $('something').parent().contents() <- will go wrong because parent does not return a cheerio object, but a string with the parent content.