one of our recent clients just has a SHITLOAD more data than everyone else and the analytics just dies because the time it takes to load the stats into memory in the rails app is longer than the timeout