andylippitt: I was intending to fix and abstract the pool for node-postgres but through discussion we kinda figured that'd reinvent the wheel of this: https://github.com/grncdr/node-any-db-pool -- so I am going to very slowly deprecate the pool within node-postgres as any-db-pool matures