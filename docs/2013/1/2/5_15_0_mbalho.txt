jtane: there is a hack where if you want backpressure in the client with fs reads you can make a new FileReader at a specific byte offset in a file and read a chunk and stop and then repeat over and over