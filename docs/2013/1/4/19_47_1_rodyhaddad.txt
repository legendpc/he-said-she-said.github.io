if it's possible to do that, then someone could do: var k = require("./test"); k.module.exports = otherObject; // that way the next person that does require("./test") will get otherObject instead of the initial value of the module