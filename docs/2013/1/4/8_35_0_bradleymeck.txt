the patch test case for fork fails hard on windows if running through a separate process so going through node itself is the only sane way… but that means having same execPath (unless using links, which on windows is confusing / make is not happy with hard linking idea after compile)