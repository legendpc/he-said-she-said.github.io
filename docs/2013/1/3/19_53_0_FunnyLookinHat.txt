ChrisMatheson, i.e. You'll have faster access by having them loaded in your application as objects ( thus stored in memory ) - and because they're read only once, and never being written to - it's just simpler to store locally.