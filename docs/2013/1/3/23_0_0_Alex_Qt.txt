io.sockets.on('connection', function (socket) {                 g_socket=socket;                 //console.log("### Client id: "+g_socket.id)                 //users_id=socket.id                 var welcome = {                     status:'ready',                     statusMessage: 'Server ready to uploading!',                     clientID:socket.id                      };                 socket.emit('welcome', JSON.stringify(welcome));      