is there a way to run a CPU intensive task in a different thread with a callback style API in which you're only allowed to touch data that you pass in as an argument, and any output comes with the callback?