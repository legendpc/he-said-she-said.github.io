they're getting to be popular in data warehouses where you're read heavy because they store data in columns rather than rows, so instead of trying to compress or index all of the data in a row they compress and index like data, only with like data