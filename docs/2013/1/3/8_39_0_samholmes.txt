yeah, if you have a single connection and you start a transaction and do stuff, then some other requests wants to start a transaction on the _same_ connection, then it'll commit the previous one before starting the new one.