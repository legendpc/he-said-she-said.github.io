hi, does anyone know if there's a package on npm that parses binary structures nicely and supports bits? i've seen modules like "binary" but they usually doesn't support anything smaller than a whole byte