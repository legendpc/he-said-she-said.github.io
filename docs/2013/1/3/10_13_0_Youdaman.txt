plain js q: if i have an associate array/object where the keys are arrays, e.g. foo[[bar,baz]] -- is it "better" (time- or space-wise) to instead do foo[JSON.stringify([bar,baz])] -- i.e. key by actual arrays or string representations of arrays?