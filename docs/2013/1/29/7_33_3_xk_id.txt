does it make sense to have an "async.reduce" if all I do in the iterator is an http-request to the same webserver? I'm asking because it seems the maximum TCP connections to the same server at one time seems to be one.