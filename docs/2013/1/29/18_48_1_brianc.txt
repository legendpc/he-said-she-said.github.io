torvalamo: it doesn't even mess with your database at all. doesn't even have a concept of your database. it's just a string builder w/ more sql-related methods on it to give somewhat readable query building instead of dealing w/ multi-line strings in javascript