a function will always run to its entirety, or until it hits a return in its own scope. setTimeout uses a callback which has a different scope than its caller. returning from there does nothing, because it only returns from the callback, not the main function