redshadowhero: The HTML client just creates a div for each channel and hides it/shows it accordingly. When you get a NAMES response, rebuild the list, when you get JOIN, append to it, when you get PART/QUIT, remove from it