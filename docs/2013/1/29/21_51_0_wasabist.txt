so its a matter of adding Access-Control-Allow-Origin:* to server headers, yes? the code for my client comes from apache, i added that header there. socket connection is directed at node.js, do i need to add that header there as well?