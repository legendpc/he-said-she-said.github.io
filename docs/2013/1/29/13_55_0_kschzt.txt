isaacs, https://github.com/rocketpack/lockfile/commit/1402639c1a345edfca63ceb620bacb591ca10f95 there was a race condition in lock() when stat() and unlock() were async, callers would unlock based on stale stat info