you can get around that some with declarative / pure functional environments like Erlang where the runtime hides a lot of that from you (which is why the Actor model is so appealing), but at some point you do have to care about all that stuff