gator102: Correct. I believe there's an RFC on it. A browser will make 8 asynchronous network requests per FQDN. Which is how it's beneficial when you're loading many many requests to spread them across subdomains to allow for more asynchronous network requests. 