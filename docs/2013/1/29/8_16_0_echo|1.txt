gator102: If the images are small, a sprite is still recommended. Where you can combine ~15-30 small images into one medium sized optimized image and use css to select each one for each purpose. Can you elaborate on what you mean by storing their string representation in CSS, though?