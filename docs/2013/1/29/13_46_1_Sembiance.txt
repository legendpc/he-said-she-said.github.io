fyi, my favorite modules:  step (async handling/control flow),  moment (date parsing, formatting and manipulation),  request (fetching HTTP pages easily),  cheerio (parsing HTML pages and searching via jQuery like CSS selectors),  accounting (format numbers)