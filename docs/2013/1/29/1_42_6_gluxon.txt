mthscndd: V8 and SpiderMonkey are both JavaScript execution engines. Both perform well, but V8 is generally scores better on benchmarks and has a much smaller code base. V8 is also used in Chrome as well as this project. Chrome gets its reputation for speed from V8.