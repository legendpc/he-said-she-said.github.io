is there any case when line like 'this.sock.send(rtp.packet, 0, rtp.packet.length, this.port, this.ip);' could cause that will somehow store rtp content and on any other it will just send previous value? 