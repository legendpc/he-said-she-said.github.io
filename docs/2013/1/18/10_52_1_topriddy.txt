anyways newbie js question whats the difference between var sum = function (number1, number2) { return number1 + number2;}  || and just writing: function sum(number1, number2){ return number1 + number2;}