Omega_Work: might be best to run them as entirely separate node processes tbh, and if you need them hosted from the same box, put them on separate ports and use a proxy to select correct port based on domain