geNAZt: bundling multiple protocols and fallbacks into a single tool is a good step; the next step would be to build a dynamic API capable of finding the most capable tool both client and server provide, and unifying the api ;)