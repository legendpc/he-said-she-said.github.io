jcrugzz_, e.g. paypal and ingdirect (and dwolla) have an API that you can limit to view. But I don't actually know of any other bank that does that. So then.. the 10? 20? apps now that aggregate financial data all need your actual username/pass