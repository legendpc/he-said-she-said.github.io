Are the browser restrictions time or file size based? To start with, I wish to keep things simple. Photographers currently upload pictures via FTP into directories, where each directory represents an event.