trevnorris: when you concatenate two strings it creates a cons string, which is just a reference to the two parts you were concatenating (except if both strings are tiny - then it just allocates a normal string and copies its contents)