isnt there a way (like npm dedupe) to specify in the parent package.json (maybe npm shrinkwrap) or some `npm install` flag, to tell npm that when it installs the parent package, it can re-use any dependencies installed by the parent also for the dependency dependencies? or specific ones?