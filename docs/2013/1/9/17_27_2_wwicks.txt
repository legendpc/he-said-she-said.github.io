jpotts: a CMS backend is basically a focused use of version control. I say this because the company I used to work with had an agnostic CMS and it was awesome. But as soon as git came around- it was easy to replace the custom one we built with git & a layer on top