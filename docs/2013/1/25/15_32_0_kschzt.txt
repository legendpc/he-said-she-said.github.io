isaacs, fixed by refactoring lockfile so that it returns a LockObject in lock(), which has to be used to unlock() the lock. plus checks whether or not we actually got a lock. so something was unlocking paths that it never got locks for, under heavy activity