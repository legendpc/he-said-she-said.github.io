styol: if that object you're constructing ever has its properties change because of outside interaction, or if it changes any other variables inside the scope of your module, that would be state that you would probably want to be specific to each other module that would be using yours