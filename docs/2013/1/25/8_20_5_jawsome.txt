MiracleBlue: I recommend express if you absolutely have to use node for static files. I prefer to configure an nginx reverse proxy that serves static files directly (what nginx does best) and proxies normal requests to the express server.