I'm trying to write my first package.json. When I run "npm test" on my dev machine everything works fine. When I run it on a build server it fails because a dependancy is missing (it is listed under dependencies). Should npm test install the deps automatically?