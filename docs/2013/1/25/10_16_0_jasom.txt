konobi: okay, I'm porting an application to javascript with emscripten.  I got a call stack exception, which was not surprising since the original C program will only run if you bump up the C stack with ulimit -s.  I tried changing --max-stack-size to all sorts of values and it still always dies in the same placee