i just noticed that node doesn't honor the promise to fill Buffer._charsWritten if the string written == ''.  i haven't found anything in the issues on github, so thought i'd check here before filing.