em, node.js is a set of built in libraries in the same way the DOM is built into browsers, Javascript is the language used to access those libraries in both cases. Learning Javascript will let you use the libraries effectively in both cases