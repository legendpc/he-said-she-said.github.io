i am using mikeal's request like this request(img).pipe(fs.createWriteStream(imgdir+'/'+imgname)), and I don't have a clue how to know what goes wrong when this fails. there is a try catch around it, but it's not catching anything. 