bradleymeck: if I was to use that, I'd have to use this kind of flow all the way down to the model just to have something simple like... logging from the model and having the request ID in that log line