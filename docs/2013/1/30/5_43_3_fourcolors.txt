min|dvir|us: so what happens is I do a npm start and it will first run the npm install command which works fine, but then it gets to the second command which sits and hangs watching coffeescript to compile and never gets to the "anywhere" command 