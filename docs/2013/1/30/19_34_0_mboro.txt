but I can't deliver any markup from node.js at page render because the delivered content depends on the user's location. so when I visit /app/search/whatever/, node.js can render results because it doesn't know my location yet