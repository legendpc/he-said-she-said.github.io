btw after installing the headers through node-gyp itself, node-gyp is able to compile stuff fine. I'd rather not have two copies of the same headers around though: I'm really struggling with npm replicating all the dependencies already