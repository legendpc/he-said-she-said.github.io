api feedback: working on improving buffer read/write method performance and will need to expose methods through SlowBuffer. since node has the goal of using typed arrays should I use the same naming convention (e.g. setUint32), or stick with the current (e.g. writeUInt32)?