I'm running `npm install mysql-db` and it claims to have completed successfully, but there's a node_modules directory in my home directory, and they're not listed as loaded by `nodejs`. What happened?