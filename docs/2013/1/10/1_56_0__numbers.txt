isaacs: on machine A, i change without bumping version and then  `npm publish -f`. on machine B where i already have the package checked out from before, i try `npm update --force` and it keeps getting the prior version