hrm you can always do freefuncs = {};  (for var f in obj.prototype) if (obj.hasOwnProperty(f) && typeof(f) === 'function') freefuncs[f] = function() { return obj.prototype[f].call.apply(obj.prototype[f], arguments); } but that may be too hacky :)