yeah. so correct me if i'm wrong, but it basically breaks down to the memory store (ArrayBuffer) and how it's viewed (Uint32Array). But there is also DataView which allows you to pull memory from an ArrayBuffer  by use of a function call.