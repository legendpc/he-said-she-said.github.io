Hi. So I have this weird thing when using NPM. I installed a package, "http-console". It's in ~/node_modules/ and ~/node_modules/.bin is on my $PATH. I can run "~/node_modules/.bin/http-console" fine but just "http-console" doesn't work. Any ideas?