chovy: depends on if you have the UID accessible already from say the drop down list (which you probably do if you're retrieving a list). If you don't, the URL might indeed be the username that then looks up the user for the UID before doing that sort of session switch