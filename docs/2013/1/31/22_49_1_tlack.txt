traveler__: in that case i would make Users.write(newData) do globalUsers[newData.id] = newData; and never check it at all. if you insist on checking, do globalUsers[newData.id] = { data: newData, setAt: new Date() }