what is the limiting factor when playing audio files? do audio files by default play at the same bit rate, or is there bitrate determined by the processors ability to process the bits against some algorithm?