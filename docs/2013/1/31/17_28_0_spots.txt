i have a simple question (at least i hope it is simple), i want to make a node server that connects to a device via rs232, but the server should get a value from the device every second and serve that value. how would one do that?