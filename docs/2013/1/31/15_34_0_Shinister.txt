you then get things like variable bit rates, where the encoder will use what's best for that part of an audio file, so a moment of silence will have a low bit rate (you don't need much to encode that well), but higher bitrate for other sections 