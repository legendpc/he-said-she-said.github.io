i wonder if HTML script languages will get to native performance (V8, Google Dart) or if C will get into the browser (Google Native Client). it seems from the loose style fo the js  community dart and c dont have no chance