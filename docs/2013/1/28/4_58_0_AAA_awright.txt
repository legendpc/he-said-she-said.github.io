Hmm I think I see. If the new context has asynchronous code then the context will be returned to your application, but the only thing you can do to terminate is make sure that references to those callback functions get deleted