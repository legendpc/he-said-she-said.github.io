Somelauw, it is generally recommended (at least it was to me) to only install modules globally if they are being installed for their scripts, i.e.: forever, nave, npm, etc, and to install everything else locally to each project