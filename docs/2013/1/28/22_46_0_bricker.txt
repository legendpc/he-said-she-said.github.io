cellvia: well it didn't work anyways. I guess I really don't understand unix. `python -v` is 2.7.3 ... `./configure` and `make` work, then `sudo make install` fails because it's using the system version of python