superjoe: I've an simple templating engine that uses HTML with ERB-like inline JavaScript tags (like embedded PHP). Would like to provide (optional) CoffeScript support, only if the option is set by the user.