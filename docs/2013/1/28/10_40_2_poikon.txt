but if you understand the basics of the http protocol (like, let's say a really basic concept would be a http response would contain a status, a header, and a body), then you'll find it easier to see what node is doing.