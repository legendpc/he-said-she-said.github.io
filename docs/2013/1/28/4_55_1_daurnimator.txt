AAA_awright: I have a js file that does XHRs. I want to run it from node. so I set up a vm with a window +document and a function XMLHttpRequest. eventually it all works. now I need to make sure it doesn't have any left over resources allocated, and gets cleaned up correctly