Also, just in case anyone has encountered this, I'm streaming a HTTP request to a spawned process's stdin. I'm sometimes getting cases where I either stream more data than the client sent or less data than the client sent (usually it seems to be +/- 1024 the Content-Length)