that makes sense it would mimic browser behavior, though I'd guess I'm doing an extra parse and render to bypass the sandbox… would be way more slick to just turn it off… ::checks code:: thanks for the kick in the right direction