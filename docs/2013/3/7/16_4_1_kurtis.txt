hmm. Well they offer basic HTTP Auth and then a Key-based auth system. I need something more along the lines of an API-based auth (login/pass) and then a token session (along the lines of how Django does it, if anyone is familiar there)