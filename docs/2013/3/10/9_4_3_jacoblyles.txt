what's the best way for one node server to trigger commands on another node server? I was thinking of using http and expressjs, but if I control both servers can't they open some kind of socket? They'll need to pass JSON back and forth