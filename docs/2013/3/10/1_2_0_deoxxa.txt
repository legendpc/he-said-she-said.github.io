i think emitting "end" more than once would be problematic, when you think about how much code is out there that looks like s.on("end", function() { this.clients.splice(this.clients.indexOf(s), 1); }.bind(this)) or similar