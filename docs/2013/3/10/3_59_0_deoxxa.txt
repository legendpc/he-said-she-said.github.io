webdesserts: practical answer? i'd use caolan's async module (caolan/async on github). pure-javascript education answer? have a callback that keeps a counter, and on that counter reaching a specific value, call another callback that reacts to the completion of both operations.