happycrappy: the redis server will always return responses in the same order as the requests, so it doesn't matter what order your requests are in, as long as the module is keeping things in order when matching up responses with requests