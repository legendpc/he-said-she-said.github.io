quincy, calling foo() uses "window" for the this context, while calling obj.bar() uses "bar". doing `foo2 = obj.bar; foo2()` acts the exact same as `foo()` - it still uses `window` as the this context