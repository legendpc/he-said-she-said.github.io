happycrappy: it depends. if the kernel buffer is not full, no. if node is buffering writes in memory because the kernel buffer is currently full, then yes -- but the writes are queued up in memory in order