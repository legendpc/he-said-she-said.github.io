I mean, `require('tasks')(grunt)`, declare your dependencies in the package.json for your task, npm install and you're all set. But some people apparently think to come with clever solutions for problems that are already solved in a simple way.