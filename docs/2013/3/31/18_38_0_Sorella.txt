Vendethiel, hm, I'm still not sure what you mean. `npm install` will install all dependencies you have in your package.json, `npm link` will put the package at the current directory in the /lib folders. `npm link foo` will install something you've previously linked.