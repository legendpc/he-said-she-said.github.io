Voting: that aside, it's also not entirely clear why the examples on http://socket.io have something inside the brackets anyway; since io.connect() can figure everything out itself if you just leave the brackets empty. 