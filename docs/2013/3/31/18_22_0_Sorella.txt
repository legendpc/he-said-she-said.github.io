If I want to do something after Stream A has been fully piped onto Stream B, which event do I have to listen to? `finished` doesn't seem to fire, even though Node's documentation says that `pipe` will call `end()` on the write stream after fully piping unless you pass `{ end: false }` as option.