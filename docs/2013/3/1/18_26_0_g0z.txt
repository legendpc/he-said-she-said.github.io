I don't think this is an http issue but this is weird as hell. I have one http.request() what works and one that doesn't. they are both to the same host, dumping the http object after 'end' of both results in the same object according to 'diff'.