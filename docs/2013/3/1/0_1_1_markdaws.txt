@isaacs - yup, well "npm shrinkwrap" could just download the tars for each package from the server and hash.  So what about the checking in node_modules approach, how to easily get just the source and not the built files?