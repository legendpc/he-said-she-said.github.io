pixie79: but your program probably will not quit either way. Your connection is probably 'keep-alive' which means node will keep the script running. You can call process.exit() if you want to force it.