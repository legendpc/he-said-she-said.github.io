(offtopic) speaking of ui, i've never really used css3. i have two elements, one of which should toggle when a button is clicked. of course i know how to do that using javascript, but can i do it in css3 (say, if a user will not allow js to run), or are the two connected somehow?