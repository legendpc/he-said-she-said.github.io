is there somewhere i can read about how npm works? e.g., what it does when you try to publish a folder (does it store a copy in the registry?); what if I publish a folder (of compiled assets) and then someone uses a git url for the dep value instead and needs to build the dist assets?