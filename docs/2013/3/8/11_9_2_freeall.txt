SomeoneWeird: I really like how AppJS is setting it up. It's just a basic wrapper to the window, and then it uses basic HTTP for the communication. I like that. node-webkit was more a framework and you can't simply install it from npm.