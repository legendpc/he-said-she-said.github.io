DNode works like this dnode(function(remote){ /* use remote object here */ }). But how do I make other scripts use the remote object? Should I just repeat the whole dnode() bit in other scripts hand have repeated connections?