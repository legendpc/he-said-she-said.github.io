Read a column of numbers from a text file into memory as fast as possible.  I already have it working by streaming over the buffer, stopping at newline chars, and then converting the slice from the last newline into a string, then parsing that string