yeah, i couldn't figure out how to buffer the other requests or whatever, the code gets the follow up requests for the images and its logged to the console but either it does not get sent on or my browser has no idea what to do with it past the first bit