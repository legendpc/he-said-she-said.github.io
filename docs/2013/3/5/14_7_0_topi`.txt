felixge: any tips as to how to debug DB accesses? we're seeing the service block when any kind of DB query is being performed, but there's something that triggers this behaviour (i.e. the service works for the first 2 minutes)