when i make a change to a version number in package.json and want to update/reinstall that module to that version, what command does this? npm install does nothing, npm update will update every single module.