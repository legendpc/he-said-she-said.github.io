ningu, but considering there can be multiple controller files.. would it be fine to do it the way I have?? It seems more efficient to have node keep track of whats already loaded for me. and if a controller is never hit it doesn't ever need to load it