ptdorf: ah, yeah i meant from the `node app.js` itself -- you may want to pipe it to a file if you're not actively seeing output. I can't remember if you need an & at the end or not but `node app.js > output.log`