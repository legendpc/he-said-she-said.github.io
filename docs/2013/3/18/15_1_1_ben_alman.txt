what's weird is the installer sets the path to be one thing, and then after install is done, no matter what the user tries, every install goes into a different path than what the installer set the path to be