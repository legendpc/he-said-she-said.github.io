yes. or if you find a good scheme for automagically guessing a good next version number, the pre-push hook could add that one (i heard git tags are used for that), commit the version and only then push it.