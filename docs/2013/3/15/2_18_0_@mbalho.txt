hm..... so im trying to stream a video http request into the google cloud storage api. if i pipe the request directly to google i get a TCP.onread ECONNRESET from node but if i pipe it to a file first then it works as expected