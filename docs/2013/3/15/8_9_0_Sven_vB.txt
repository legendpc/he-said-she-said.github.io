you can save some kittens by clamping your model to window explicitly (window.model = ... instead of var) and even more if you open your own, hopefully-unique named namespace below window and clamp your model to like window.COM_EXAMPLE_NINGU.model