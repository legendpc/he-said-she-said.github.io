Ralt http://substack.net/browserify "Browserify bundles with es5-shim by default so you can use shiny new javascript features like Function.prototype.bind() even if your browser doesn't have those yet!"