there ways to get around it, you can tell apache to forward requests to node for a given path, like localhost/node/ actually gets handled by the node server, but youd need to configure apache, I'll paste that in case you can't get the other working