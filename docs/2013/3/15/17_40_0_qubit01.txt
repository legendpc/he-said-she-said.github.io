mrfox,  no you can't post directly to the client.  You would have to instantiate a request from the client side javascript, or use something like socket.io which allows you to push data to the client through web sockets.  But in traditional web development, you can't push data to the browser