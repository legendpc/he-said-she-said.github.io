The first parameter is the collection, the second is the iterator (the function to be run on every element in the collection), and the last is the callback that gives you an error (if there was one) or the array of results