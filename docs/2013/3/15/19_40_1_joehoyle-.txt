hey, I am using a a nodejs libary that uses .success() and .error() methods for callbacks, what does one call this method of callback, presuming it's not the same as standard node callabcks, and also not primises?