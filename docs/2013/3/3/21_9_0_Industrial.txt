asdf12: so should I create a HTTPRequestStream from a httpServer (in the reverse proxy) and pipe that to the hostname router which has some routes (with streams) registered and sends the http requests to one of its output streams based on the route function of each over the input request