Are there any projects e.g. on github so I can view the code that make use of streams heavily? I've taken a look at https://github.com/substack/stream-handbook but I'm not sure where I can use a stream instead of another construct in my app.