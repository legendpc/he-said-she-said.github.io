graaa. just looked into one of the modules i use and by accident found out it uses synchronous file system access. isn't there a way to make node deny that? then such modules would fail on the first try and i knew i should use a better one.