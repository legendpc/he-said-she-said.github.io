If you think scaling might be an issue you can playing w/ the cluster api  http://nodejs.org/api/cluster.html... basically allows you to run multiple processes to take advantage of multicore processes, although the api is still experimental