then maybe i should combine the monte carlo approach with the "knowledge about number formats" approach, and if some of the numbers turn out to be more precise than 32 bit, assume they even have 64 bits (and, 128, and so forth)