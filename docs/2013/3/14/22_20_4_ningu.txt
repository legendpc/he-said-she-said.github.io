mikeal: by the way, in request how do you make sure the callback is only called once? do you have to use a semaphore and always check it, like: var alreadyDone = false; if (!alreadyDone) { alreadyDone = true; cb(); }