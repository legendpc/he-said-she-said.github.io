bnoordhuis, also, I love being able to do stuff like making a ringbuffer using `mmap` and `remap_file_pages` (so that there are two adjacent virtual memory regions pointing to the same physical memory region)