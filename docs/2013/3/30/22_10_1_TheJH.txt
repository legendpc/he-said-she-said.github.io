ningu, buffer overflows aren't hard to protect against. with complex program logic, for example, use-after-free is a problem. also, you need to know details about how the filesystem works when you do stuff like chmod'ing directories or so, those are the more common issues, I think