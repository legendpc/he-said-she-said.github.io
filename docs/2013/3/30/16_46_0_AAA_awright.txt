TechieShane: Basically, the Web security model allows anyone to instruct a user to make a request that's authenticated as themselves. You have to make sure that when these requests are made, they're not requests that say "Delete all content" or something like that