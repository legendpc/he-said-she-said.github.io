prettyrobots: That's pretty cool, but I am looking to set up node myself, rather than using a service. So I'd be doing what transloadit does (on a small scale) on my server. Just trying to figure out the best options/setup to handle that sort of thing with node...