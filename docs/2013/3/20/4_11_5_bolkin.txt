i have node 0.10.0 built from sources on linux, db-mysql module fails to install, apparently it looks and fails to find node-waf which was removed in 0.8, does it means that this module is unmaintained?