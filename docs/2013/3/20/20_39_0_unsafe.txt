it's feasible that you could monkey-patch XMLHttpRequest to do that-- and I may do that in a reference implementation-- but right now I just have an ajax library which targets the workers if you give the httpl protocol