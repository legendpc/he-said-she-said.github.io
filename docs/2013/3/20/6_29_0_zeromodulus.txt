but... it looks like subsequent .connect() calls make a brand new connection, so all the .connect() calls I did previously, all of the callbacks I passed to those basically appear to have had no effect.