So I get a connection from three clients.  I create a sub-domain for each of them under the process domain.  In to each I .add(req) and .add(res).  So when I call myHandler(), I can call domain.active()?  domain.current()?  Neither are in the docs.