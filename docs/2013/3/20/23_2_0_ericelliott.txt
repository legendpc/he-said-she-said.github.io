ljharb: Not critical. It's infrequently stale, and not stale very long. Everything still works -- users will just see a slightly old feature set (likely the feature set they're already familiar with) -- this is a feature-toggle configuration JSON blob.