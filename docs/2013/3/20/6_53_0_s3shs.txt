bradleymeck, does this sound right? (1) create global domain.  (2) create subdomains for every transaction and add(req)/add(res) on each.  (3) From within whatever cascade of functions, when I need res, just call globalDomain.active().res/req?  Or will res/req just appear in my scope automatically?