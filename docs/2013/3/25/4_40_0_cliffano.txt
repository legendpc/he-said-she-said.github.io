bradleymeck: that documentation only has an example where couch does not require username and password, my private npm registry always returns 401 in that case... and npm config https://github.com/isaacs/npm-registry-client does not have any field for couchdb admin username/password setup