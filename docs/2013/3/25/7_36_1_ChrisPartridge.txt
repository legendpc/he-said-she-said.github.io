someprimetime: another thing you can do, is in your "addView" middleware, fire the code to save the 'view', but call next() immediately, that way the user gets served ASAP, and then you can capture/log errors in the save callback if something went wrong 