So I can call pdb.set_trace() anywhere in a program, and then when I run the program from command line the shell will drop me into an interactive REPL which has access to the local variable context at that point in the program.