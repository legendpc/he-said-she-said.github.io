Perhaps a stupid question, but I am exporting a model class that should read a file in the directory of the file that represents the model, but I want to package the base model as a node package. I then want to do something like this: https://gist.github.com/rschmukler/5241007