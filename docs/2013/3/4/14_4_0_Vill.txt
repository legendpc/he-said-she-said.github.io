SargoDarya_ 2 hours later... removng "extends layout" in the sub-layout solved the problem, so using "include" is actually better than using blocks, also, when a layout has "include" and we are trying to render a sub-layout the "include" in the layout won't be rendered, probably because the async methods