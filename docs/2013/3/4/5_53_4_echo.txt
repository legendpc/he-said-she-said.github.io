Could be an http proxy or a SOCKS proxy, either way, it's not a reverse proxy (i.e. a load balancer), it would be transparent, so any internet-requiring requests would need to be properly parameterized: i.e. for the request - var r = request.defaults({'proxy':'http://localproxy.com'})