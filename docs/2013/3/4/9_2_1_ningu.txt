but what I don't understand is, this was in an async.each(...) with an array of 15000 items. and it was dying after a couple thousand or so, on the item whose handler created the socket without a close handler.