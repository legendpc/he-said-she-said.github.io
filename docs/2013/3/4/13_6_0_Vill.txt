I have "app.use(app.router);" in my main app.js then "res.render('main', { title: 'Precision - Home' })" pointing to the  template. Template contains "extends ../layout" and defines blocks. The only this i modified, my dear SargoDarya_ is "layout" --> "main" in res.render(