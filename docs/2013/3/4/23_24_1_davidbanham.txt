dobedobedoh: If there's a node_modules directory present, npm will install things to there. Otherwise (I might be subtly wrong here but I think I'm okay) it traverses up the dir tree until it either finds node_modules or hits $HOME and installs it there.