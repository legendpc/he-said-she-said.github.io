slightly off topic, if there are any chef users here...would anyone have a clue why an 'npm install' in a chef recipe would fail(odd permissions error I can't figure out), but logging in and running 'npm install' manually(with the same user as in the chef recipe) works just fine?