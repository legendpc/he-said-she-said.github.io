what is the best way to work on two node package that have dependency. By this I mean both package A and B are developed by myself, and package A needs B. If I had B to the dependencies in the package.json file, is is not going to mess up my work in package B on npm update etc...?