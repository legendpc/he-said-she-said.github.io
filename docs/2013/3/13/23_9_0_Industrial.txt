Without putting it into a database, how do I recover scope (e.g. I want to create a server that matches a name to a port on a host) when my server stops? Can I prevent having to re-register all clients?