I tried a conditional constructor function, where if a JSON string argument is present, it loads all of the data as the instances properties, i.e. `for (property in json) { this[property] = json[property]; }` but if I do this it seems I don't actually get an instance back, just undefined.