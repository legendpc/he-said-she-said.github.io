since my environment now will have around 10 servers, and it uses seaport, I thought bouncy was a good fit. I guess, in essence, all it has to do is ports.query() at some point in a middleware chain and decode what to do based on that.