asterokid: That might be a symptom of something else. If you really need to be modifying them both concurrently, I'd just require it explicitly from the separate directory. var packageB = require('../packageB') or '/home/asterokid/packageBdevdir' etc