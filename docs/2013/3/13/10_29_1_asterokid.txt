davidbanham, ok so I imagine that package B is a git clone leaving in the ignored node_modules of package A right? I suppose I am worried that if I try to do npm update from package A, it will redownload  and overwrite my package B with the one available on NPM.