on node 0.10.0 I'm seeing require('file.json') cause the node process to use up a lot of memory then eventually die due to a memory allocation error.  the json file is only 2.8MB in size.  has anyone else seen this?