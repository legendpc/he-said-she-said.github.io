ningu: for example, if you've used page.js, you'll notice that each route function has the ctx and next parameters (express hase req, res, next). But what if in page.js you used the module to access these things, page.ctx, page.next.