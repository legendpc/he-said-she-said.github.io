I'm thinking of making the feed updater into a worker process which I spawn. The trouble with that is, the process will need its own connection to the database, etc. I have to run all the boilerplate code again, because objects can't be shared.