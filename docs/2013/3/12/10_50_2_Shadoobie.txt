if i have an app.js that is calling injex.js via a route.index command and that index.js file needs an array that lives in app.js do i have to export a function and in that function seeriale the array to a JSON object?