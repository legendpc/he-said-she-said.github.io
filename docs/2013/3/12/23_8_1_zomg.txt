worldhacker: I would imagine websocket only supports websockets, while socket.io supports other transport methods as well, allowing a wider browser support. Socket.io's API probably also is on a higher level (for example, it supports function callbacks directly etc.)