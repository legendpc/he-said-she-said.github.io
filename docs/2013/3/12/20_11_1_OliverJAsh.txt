I have an application which caches articles from RSS feeds. Currently, when the user requests to see articles, the application will ping the RSS feeds to check for new content and compare it with the cached date, merging the two. The request will run an asynchronous function which will cause the feeds to update, but immediately deliver the old content to the user. However, if the user tries to hit the API again, and