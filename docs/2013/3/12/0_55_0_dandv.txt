well, in the case of the drone project at the link above, it seems superflous to have the code laid out that way, instead of just client = arDrone.createClient().takeoff().wait(1000).clockwise(0.5).stop().land(); // jQuery chaining style