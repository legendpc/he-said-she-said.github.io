[conrad]: what you are doing in that code is putting 10,000 database queries into a big queue which is just a bunch of CPU operations but the queries themselves are I/O which means they are really really slow