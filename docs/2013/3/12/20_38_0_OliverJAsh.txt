stantonwjones: My problem with making this into a daemon is that it will require its own database connection, etc. Rather than starting the daemon with every request, it therefore seems more efficient the start the daemon with the application (once only) and delegate tasks to it.