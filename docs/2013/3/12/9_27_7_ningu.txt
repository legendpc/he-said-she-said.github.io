in the crockford talk on monads he mentioned that the stack is unwound after every function ends. so I guess node must be able to maintain a lot of listeners and keep the event loop running with relatively low overhead.