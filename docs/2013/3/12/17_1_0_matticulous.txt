i know it probably doesn't matter a ton, but is there a "common practice" path for deploying node.js apps in production? for example, if my install is "git clone [blah]; npm install .", where should i be running this from? /opt/local? /var? other?