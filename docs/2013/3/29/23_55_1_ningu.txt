TooTallNate: I'm seeing segfaults with node-sqlite3, which uses gyp. but it's hard for me to narrow down what's causing it -- using the sqlite3 module directly works fine, but using it through any-db segfaults. any idea how to go about debugging this?