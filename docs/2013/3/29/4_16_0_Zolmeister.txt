jaha: I created a log.js file which exports the configured winston module, so maybe try importing as 'console'? Though i'm surprised its failing, perhaps you added the transport twice? (the default is to log to console)