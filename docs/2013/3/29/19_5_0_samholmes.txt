I'm going to loop through files in a directory called /controllers and for each file/module in that directory I'm going to call a .router method on the exported object (if method exists), passing the app variable. The router for each file would add routing callbacks to the app.