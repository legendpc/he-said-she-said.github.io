		zlib.deflate( data, function( err, buffer ) 		{ 			if( !err ) 			{ 				console.log( buffer ); 				//console.log( buffer.toString() ); 				socket.write( buffer.toString() ); 			} 			else 			{ 				socket.write( "error encoding" ); 			} 		});