@eddyb well I'll be a monkey's uncle… so I figured out the issue… dpkg -l | grep node-less … the machine that -works- installed the node-less module somehow using apt-get, while the other 3 have the module installed using only npm, and there's some symlinking that is missing that the package does… 