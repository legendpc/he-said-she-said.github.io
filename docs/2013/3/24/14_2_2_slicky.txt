can't you just set up a virtual audio link that will automatically create the audio stream for the html5 audio player? that way the stream simply looks like a file that doesn't have a specific size (cause it'll stream as long as you want it to0