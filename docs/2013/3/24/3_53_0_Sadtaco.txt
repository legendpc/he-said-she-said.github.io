The difficulty was in ordering the code in a new makeObj("x", callback); when I am using a getter of the object to add stuff to it and don't know if/when I want to add those things. And there is so many, I can't just wrap my socket connect on a single callback