If I add an event listener inside a module is there a way to remove it without actually doing emitter.removeListener. I was hoping something like http://stackoverflow.com/questions/6676612/unloading-code-modules#answers-header would do it but that doesn't seem to work.