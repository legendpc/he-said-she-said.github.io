hey, if i update the version of a dependency in package.json, what's the quickest way to install the changes? npm update <package> just seems to use the default version, not the one specified in package.json.