If the PHP script is written for being executed through Apache (returns HTML, for example), then you'll want to consider that when parsing the output. I think calling from the command line would be faster/less process intensive.