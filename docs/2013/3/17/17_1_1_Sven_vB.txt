ForSpareParts, i think pre-compile there means to compile the template. the template still has slots for data. to get plain markup, you'd have to render the template, possibly with data for those slots