ken… do you recommend pulling static pages (login and such) directly from disk (or cached in memory); and only render the dynamic stuff as needed, or do you recommend pulling those elements via API calls into the browser?