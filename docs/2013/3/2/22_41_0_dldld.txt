I have a really simple server that runs this long function when a particular URL is called, and well i thought it might be better to code this function somewhere else and use nodejs as a forwarder only