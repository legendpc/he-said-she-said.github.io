basically for what i know, node.js is single thread in the meaning that there is a threadpool that does the underlying work [like read() write() or even open()] but at the end only the main thread is signaled of the completion, and the main thread calls the js code callbacks