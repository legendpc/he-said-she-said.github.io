isaacs st_luke the issue appears when first installing from package and then just removing /usr/local/lib/node_modules/npm and installing from source.  Even though the issue is strange, removing the entire /usr/local/lib/node_modules directory seems to resolve the problem.  I will remember to do that from now on :)