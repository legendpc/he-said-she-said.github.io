does a function like async.map or async.filter offload the work to a different thread/process/whatever so that the main process can continue doing other things at the same time as the call to map/filter is being processed some place else?