Also consider, if you are using an async library to iterate over an object you are calling a function for each object in the array, as well as a function to say "i'm done". Seems like a bit of unnecessary overhead if you're just returning a property of each object.