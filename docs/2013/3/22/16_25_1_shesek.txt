euoia, if its just doing some stuff in nodejs land, and doesn't use any external I/O (file system, database, http requests, sockets, etc), than it makes no sense to run it asynchronously, or using nextTick, or whatever