using express / restify... how do you distinguish error handling middleware from other kinds of middleware? It seems every request passes through my error handling middleware -- so I need to detect to see if it's an actual error?