TooTallNate: I'm cross compiling from Linux intel to Linux ARM. I think the issue you mentioned in email was that --dest-cpu=arm isn't supported by node-gyp yet. In any case, I got it cross-compiling for node, so I'll look at the makefile for that and see what I can re-use.