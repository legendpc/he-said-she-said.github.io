I've been dealing with relatively small numbers of modules where I've been shuffling the deps around -- for example I had any-db but then decided to use gesundheit which wraps it, but I install gesundheit before removing any-db so it didn't put any-db under gesundheit