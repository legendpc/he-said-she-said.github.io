Beck_, my guess is it hasn't had time to init yet and it can't bind to the address you've specified. Try ordering it later if possible or run something else in it's place to give you some output of the status of the interface