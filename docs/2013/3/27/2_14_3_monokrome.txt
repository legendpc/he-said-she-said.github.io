My original question is simply if I can have a project using the file structure of my scripts being in lib/example and letting projects using my library require files from lib/example with the syntax: require('example/filename')