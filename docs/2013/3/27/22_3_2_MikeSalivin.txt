SO guys I created an proxy server that connects to a service retrieves data parses it and then returns it to the client. The Json is huge and the parsing might take a while. should use a callback for the parsing?