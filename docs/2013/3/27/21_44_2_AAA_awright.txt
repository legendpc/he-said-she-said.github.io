When I use function proto(){ /* constructor */ } I get a free "prototype" property initialized for me, a "constructor" property for me to use, and if I don't want to call the constructor, then I just use Object.create(proto.prototype) which is all of 10 extra characters IF I need to do that