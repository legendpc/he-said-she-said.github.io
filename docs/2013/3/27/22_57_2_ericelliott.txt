MikeSalivin: There are lots of ways to manage asynchronous behaviors. You might want to look into libraries such as async and q (async offers underscore-esque functional utilities for asynchronous actions, while q offers a promises implementation that lets you say foo().then(doSomething).then(doSomethingElse))