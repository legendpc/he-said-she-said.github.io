Two different things call callbacks when they're done loading. I want to do something when both are done. Is there a built-in or famous-lib that implements this? For example: wait(db_loaded,files_loaded,function(){}) 