It basically means "I can't encode that resource as a series of bytes because it's a CAR, but you say you want JSON, so here is a JSON document that talks about that car, and here's the URL where you can get this particular encoding in the future"