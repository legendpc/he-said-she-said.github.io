MikeSalivin: You have a few choices: fork and have the (CPU-intensive) process run in parallel, incurring IPC overhead, run it sync and block immediately, or defer it until the next tick of the event loop (using process.nextTick() or setTimeout())