caulagi: You'll have to decide. They're fundamentally different. Redis is a memory store and pubsub server on top of which you implement a queuing system. ZeroMQ is a library that implements message queuing over the network directly between peers, without (necessarily) an intermediate server.