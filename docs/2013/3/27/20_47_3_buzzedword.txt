the "ViewHelper" class can be instanciated with the new keyword, has a constructor, and at the application level, we're even aware of it. however, the method that's attached to the ViewHelper's prototypal chain cannot get it's parent instance's prototypal chain-- it returns undefined