The actual functionality Redis implements is super easy to use, definitely. But if you want to do message queuing with Redis, you still have to implement it yourself on top of Redis' primitives. (Or find a library that does it for you.)