delaying your parsing with process.nextTick() will just delay that blocking parse until the next iteration through the event loop. What you need to do to make your processing non-blocking is to fork a worker.