ljharb: There's good reason to use JSON Schema anyways, you use it to define how a JSON document shall be constructed, and then you give that schema a URL. Now you can differentiate different types of JSON documents from each other, for instance, v1 of an API from v2