If the modules installed via npm then you don't need a direct folder name or a .js file, if the module has a proper package.json require will pick up the name and use the main file defined in the package.json