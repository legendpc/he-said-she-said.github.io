sevenup, if you set the path using the shell and set, the changed variable are only provided for the shell the variables are edited in, I recommend chaning the variable using the gui, and close and reopen the shells you have so that they may refresh the content of the variables