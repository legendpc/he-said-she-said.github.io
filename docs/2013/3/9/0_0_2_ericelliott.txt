I have a bunch of data that I want to inject into an index template that's rendered for every request. I've made middleware that builds up a req.data object that gets injected into the page template at render time. Is this a good strategy?