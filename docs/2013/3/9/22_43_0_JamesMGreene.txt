isaacs et al: If an `install` script hook fails, will the install of that package fail? I'd assume so. But doing so in `postinstall` would still have "successfully" installed the package even if the `postinstall` script failed therafter.