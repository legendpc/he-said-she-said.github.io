But basically it works the same as the "phantomjs" published package: on install, download the parallel version of the PhantomJS executable for this OS, unzip it, and put it into a sub-dir; then the module just offers up a path to that binary