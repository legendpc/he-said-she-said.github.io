peterschmidler: you need to decide if you want to maintain the files separately in modules and then cat them all together in one blob for the client, or implement some sort of client-side require (there are various attempts to do that)