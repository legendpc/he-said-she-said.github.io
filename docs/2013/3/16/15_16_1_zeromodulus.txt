so, redis is like, kind of a bare bones kind of thing, where a lot of the niceties of databases like MongoDB and CouchDB are considered convenience layers that the programmer needs to create should one want them?