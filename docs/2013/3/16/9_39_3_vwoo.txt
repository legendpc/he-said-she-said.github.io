paultman: it depends on your setup. its harder if the app servers are externalm but presumably you have app servers on the same box as the router? if so, you MIGHT be able to listen to local ports on loopback, or use some sort of unix socket descriptor