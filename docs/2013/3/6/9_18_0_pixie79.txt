if i call a sub script that is in bin that same place as the main script when executing it via a fork will it look in bin/ if i set that or will it look in CWD/bin/ ? I know for libs i require them with ../lib but i am unsure here on the bin