can anyone recommend a module that parses apache-sytle config files? i.e. simple key-value pairs delimited by whitespace. (i realize it's trivial to write one, but, ya know.. wheels already exist too..)