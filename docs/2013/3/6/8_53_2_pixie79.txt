when using child_process.fork how do i tell when the child process is complete so as to collect the exit code as the docs say I must send process.exit() explicitly (or does it mean in the child process i must send that?) in which case i just look for child.on('exit') ?