For example, I might have `input` nested within a few selectors (in stylus), then on a specific page I would like to set the width of all `inputs`, but the selector wouldn't be nested as deep, so I would need an !important clause.