perlitch, my idea is that when jade.compile(fs.readFileSync("file")) is encountered, if you include both brfs and jade-precompile, it will precompile your templates. if you only include brfs, it won't :)