is there a simple way to 'stream' the output from a browserify pipeline into a callback? Something like .pipe(function(output) { ... })? Or do I have to create a through stream myself and capture the output manually?