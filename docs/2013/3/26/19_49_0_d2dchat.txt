I'm trying to use lodash to convert an object with camel cased keys to an object with underscored keys. Anyone have a good idea how to do this? I took a look at _.merge, but the callback only returns the values, not the keys nor the object it is recursively merging.