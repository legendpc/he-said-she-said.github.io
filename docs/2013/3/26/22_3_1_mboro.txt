mscdex: I think app.use(function(error, req, res, next) { is the proper way to do it as it catches the errors. my `locals` just aren't present for some reason. well, perhaps because an internal server error occured