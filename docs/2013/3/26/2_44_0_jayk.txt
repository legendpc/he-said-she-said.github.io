yeah.  stream.waitfor(256, callback(data) { .... } );  where waitfor does a 'try to read x bytes, if so call callback immediately, if not stream.once('readable', function() { stream.waitfor(size, callback) });