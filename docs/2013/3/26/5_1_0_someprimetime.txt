Zak: i'm running into an issue upon looking up a submission. Basically I A) lookup the submission from the id in the url B) if it exists, check if the IP has viewed it before C) If it didn't view it, then save a view in my SubmissionsView collection