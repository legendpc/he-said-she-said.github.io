I have an app.get and app.post with the same route e.g. ('/:page?/:post?) When I log req.params for the uri /foo/bar app.get gives me 'foo' and 'bar' and app.post only 'foo'. With a trailing slash both behave similar. Why? And how can it be prevented?