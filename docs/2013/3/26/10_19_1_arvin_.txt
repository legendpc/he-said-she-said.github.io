the iterator itself will probably be called in sequence (not in parallel), but what it means by "parallel" is that it won't wait for each invocation of the iterator function to complete (i.e. its callback to fire) before invoking it again with the next item in the array