ningu: I think if you try logging the cb variable inside the add method - function(add, cb) { console.log(cb.toString()) }, you'll see the callback function which I passed as the third parameter to the async.each method is getting called