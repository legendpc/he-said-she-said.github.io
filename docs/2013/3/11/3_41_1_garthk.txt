(Meanwhile, isaacs, dominictarr was saying I should report bugs on any Node module which claims to do streams but doesn't emit 'data' events. That'd include your fstream and sax modules, which brings me back to my XML speed parsing questions. Any recommendations?