For example, I might want to include a template, but that template needs a few locals defined. If I define them, then would they be available to _all_ parts of my template, not just the include. Is there a way to add a scope?