if I've set up a schema (var fS = mogoose.schema({ SRID: String, units: Number });) for example, but will occasionally have an optional field tucked it, how do I apply that optional un-schemad field in prior to write to db?