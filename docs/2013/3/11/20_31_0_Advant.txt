TooTallNate:  I was working with a client that doesn't support unicode, so I attmpted to write extended ascii directly like client.write('\xC0'), but when I looked at the traffic dump it looks like '\x86' was being appended to my single bytes