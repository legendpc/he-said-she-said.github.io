airtanah: Because AWS boots the kernel directly or something, there's no bootloader. If you want a bootloader to boot your kernel, you actually have to run Grub as a kernel/OS, and chain load your kernel.