sshaginyan: there's nothing wrong with node-static, but if you're planning on hosting a lot of static assets, that's much better delegated either to nginx, S3, or something with varnish in front of it, leaving Node to handle only the application logic