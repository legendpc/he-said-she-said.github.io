express question... I want to run all /api calls through a piece of middleware to check for login... why doesn't app.use('/api', checkLogin) trigger ?  I know I can add checkLogin to each route manually, like app.get('/api/field', checkLogin, ... ) ... but can it be done on an entire path at once?