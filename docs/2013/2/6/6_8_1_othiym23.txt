asdf12: it's just some lightweight file descriptor magic and some convenience functions to make it look like one logical cluster, you can do everything cluster does with child_process anyway, just not as conveniently (but on the plus side, not as magically)