codepython777: to write a script like that for `minify`, you would need to have an executable script that can parse parameters (which is never fun, regardless of your platform) and pass them to minify.optimize( )