so say I have a 'redis.createClient(...); client.dosomething(...);' that's currently executing, waiting on IO.  I want to now do 'mysql.createClient(...); client.dosomething(...);' and have it also running.