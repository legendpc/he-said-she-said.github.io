codepython777: the minify module doesn't come with a command-line interface, so you can't do it directly from the command line. You have to write a node.js script to invoke minify, thus the need for a separate executable for your makefile to run.