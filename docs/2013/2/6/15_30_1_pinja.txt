motaro: It's just a memcache api to the underlying sql data. There's still a relational schema underneath everything taht still needs updated as data requirements change. you still have all the same challenges when trying to scale it to 128 nodes. etc