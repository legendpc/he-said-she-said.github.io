asdf12: It is possible: r.js is the usual answer to this. I found that too complicated, so I whipped up my own asset pipeline using esprima (to rewrite defines to have static module names) and uglifyjs