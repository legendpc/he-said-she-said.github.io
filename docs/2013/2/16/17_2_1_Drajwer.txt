what is the difference between 'require('./')' vs 'require('./lib/mylib.js')' assuming package.json contains "main": "./lib/mylib.js" so the first one should work. mylib.js exports build/Release/mylib.node. I see difference in how my module works.