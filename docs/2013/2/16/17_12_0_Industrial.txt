if I clone component and do an npm install it will try to (and succeed) somehow to install Observable instead of Observer. I recently renamed my Observable project to Observer. Do I need to clear a cache somewhere?