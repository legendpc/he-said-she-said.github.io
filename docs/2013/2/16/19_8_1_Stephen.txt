But in all truth: When spawning processes it's always been important that the messages passed between them be in a queue independent of the processes. These queues should be unlimited in size, and should support both push and pop in single and batch(transaction) .