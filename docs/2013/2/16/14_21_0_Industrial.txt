Right now I'm applying methods and properties to instances, not prototypes, but if I would do it on the prototype (most notably the events member, holding all the events and their handlers) it would mess up / share handlers for all instances, which is ofcourse not what I want