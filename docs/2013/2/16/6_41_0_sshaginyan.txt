Stephen: Basically I have config.js file with var config = exports; config.url = "one" +x + "two"; I'm doing a var config = require('config') in another file that has the variable x set. WHen I do a config.url in this other file I want x to be set