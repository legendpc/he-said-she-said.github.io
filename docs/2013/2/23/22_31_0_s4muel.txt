Depends on what you mean by port type. pipes and such operate on file descriptors, specifically the three posix descriptors (stdin,stderr,stdout) -- but it is trivial to adapt that to a socket. Take a look at 'nc' for example.