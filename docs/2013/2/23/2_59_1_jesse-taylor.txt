storing room names nad usernames is all from an object right now, passes the room object to each user with nowjs and alters it on the server side, and only gets touched when an event happens. is this the worst idea ever? whats another option? db?