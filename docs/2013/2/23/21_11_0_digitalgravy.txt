mfn: nope, just forgot to say sudo; so "sudo npm install -g consoleplusplus" should get you there; also Aria is right, -g just installs it globally so you can require it from whatever script you create