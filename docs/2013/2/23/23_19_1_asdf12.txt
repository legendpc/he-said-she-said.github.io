that just depends on how you already have things setup i guess? but generally how about just do this function Data(mongoJSON) { this.mongoJSON = mongoJSON } Data.prototype.toLib1 = function() {// transform}