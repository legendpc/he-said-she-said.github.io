ideally one that queues the process, passes some state information to a temporary database like redis or memcached specific to the job, monitors to ensure the job started and finished successfully, and possibly produces a log and comes with a web admin for seeing an overview of the jobs