im trying to store objects in redis, via node_redis package. I understand redis stores everything as a string, but is it my job to JSON.stringify objects before passing them to node_redis, or can I just pass an object and the module will sringify it for me?