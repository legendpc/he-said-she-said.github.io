i've installed basic express.js app, it is using all the defaults, when i change the route entries for app.get() to invalid it still shows me the page as if it existed.. what's wrong? shouldn't the server detect this? i'm on OSX and Chrome stable