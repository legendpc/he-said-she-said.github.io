groundwater, well, whether gdb lets you peer into a binary or whether its the optimal way to do so for a given binary depends on what language the original code was written in and what/how the binary was produced.