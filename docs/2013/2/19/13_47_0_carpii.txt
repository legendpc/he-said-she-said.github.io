i must have launched my node server as root when i was devving last night. now its getting an exception on listen, when i run it as normal user (but still runs as root). How can I find out which socket files its trying to write ?