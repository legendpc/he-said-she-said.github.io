Maybe the answer is 'go benchmark it', but how costly are console.log() calls?  If a webserver is logging ~500 times per second would it be worthwhile to batch those as one single console.log() call and store them in memory (per second or so).