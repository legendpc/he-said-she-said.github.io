in the browser, doing var x = 4; attaches itself to this (root), doing so in node, it doesn't (polluting namespace i'm guessing), only way to achieve it is to be explicit, this.x, but there has to be a better way to refer to the root of the module's 'this' ? 