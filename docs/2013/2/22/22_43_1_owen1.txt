I built an HTTP end-point that return chunked responses. I want to call it in the browser. do i need a special http library in order to display the chunks as they come or is it the default behaviour of any xml http request?