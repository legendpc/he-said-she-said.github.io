in the example, the listener is created and removed straight away. if I wanted to create a listener and then remove it after i get the first emitted event, i would have to remove the listener in the callback?