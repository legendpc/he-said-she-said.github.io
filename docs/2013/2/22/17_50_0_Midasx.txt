I am taking a command line argument with process.argv.splice(2) and it works fine. However when I calculate its length with input.length it always equals 1 when infact the string is 32 digits long. Any ideas where I am going wrong? (This is my first program in nodejs!)