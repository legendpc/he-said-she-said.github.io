after having used Scala, I no longer see any benefit of dynamic typing. static typing with inference is genius. why would one ever want a variable to be string in one moment, and an array in the next? the type is pretty much always fixed, only its value should ever be dynamic