hmm... weak typing just causes troubles... but it's fine if you, in a scripting language, just define a variable like this: i=0; and it sets it as an int... would be fine, but then you should only be able to use it as a number.