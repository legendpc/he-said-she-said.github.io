this is probably a really really really stupid question but I just installed node from source but the node --version is not returning an error. "/usr/bin/node" no such file. node installed to /usr/local/bin/node. did I install incorrectly or should I just add /usr/local/bin to path?