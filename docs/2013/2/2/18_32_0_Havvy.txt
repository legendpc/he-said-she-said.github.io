You can either prepare for that by using ~0.2.13 as the version in the package.json file or by adding nrc to your callback and at the top do:  if (!nrc) { nrc = this; }  and then using nrc instead of this.