fwg: the only so called "advantage" is that, you can use AMD directly in browser, Node.js style modules needs to be bundled with some guy (if any exists) or console command, for some people it's too scary to call one command in console ;-)