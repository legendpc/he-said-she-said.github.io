So I'm evaling code, inside that eval we do a readdir(async, tried sync too), that looks through some stuff and then stores it in a variable. We then (in a later template rendering portion of the script try and print that, but when we print it it's apparently blank