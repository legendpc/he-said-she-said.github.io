pingupingu, http://nodejs.org/api/http.html#http_request_headers .. that is a good place to start. you can get the request info there. which is all of what was passed in GET/PUT plus the headers that followed