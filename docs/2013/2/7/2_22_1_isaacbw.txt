when users of my app first connect, they need to be send to a server chosen by a load balancer (or whatever), and then they need requests to keep going to that same server for the duration of the session