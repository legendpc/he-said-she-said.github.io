othiym23: But if I need to write a file in one server, then read it on another server, write another file based on the 1st file, and then provide it for the user to download. All at real time (as users click stuff) - Filesystem is not an option....