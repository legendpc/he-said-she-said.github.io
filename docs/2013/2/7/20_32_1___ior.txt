well, not in this implementation. we enqueue the incoming function and arguments, hit a noop. when the function in the mutex releases the lock, its callback pops the next function off the waitlist and calls it