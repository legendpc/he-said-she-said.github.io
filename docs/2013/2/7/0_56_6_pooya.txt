isaacs bnoordhuis , i was looking at npmjs code and noticed that we already pretty much cache everything in "~/.npm" but npm install checks registry again for all of them and fetches all from private gits too. any reason for not using the cached one directly?