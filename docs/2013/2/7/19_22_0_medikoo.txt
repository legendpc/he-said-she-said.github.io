othiym23: there are few ways, just chain promises with then: promise.then(promise).. or use reduce: https://github.com/medikoo/deferred#reduce best if you show it on some gist, then it'll be easiest to chose best approach