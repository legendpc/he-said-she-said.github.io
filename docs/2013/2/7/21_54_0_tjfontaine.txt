kenperkins: there are two mechanisms, lookup which uses gethostbyname (for all intents and purposes "os"), and resolve which uses the dns servers configured for your server and does resolution itself bypassing the os (no internal caching, only the other side may be doing caching)