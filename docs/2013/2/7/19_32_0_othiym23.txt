and because the tests are integration tests and start up external services (like databases and web services), they need to be run serially, instead of (say) creating a bunch of test arenas and running the tests concurrently