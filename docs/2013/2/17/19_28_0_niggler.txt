owen1 basically, the shell parses the command line arguments (so globs like cat * work properly) and handles other features like redirects (> blah) and pipes (| blah).  To get those features you have to run it as if it were a shell command (using bash -c).