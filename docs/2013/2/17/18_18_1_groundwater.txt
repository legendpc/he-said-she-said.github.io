i think a cool stream API would have let you go streamParser.on('node', function(nodePath, nodeAsObject){..}) where nodePath looks like /a/1/b or something, and nodeAsObject is the total parsed object, the last object emitted would be the root, complete object