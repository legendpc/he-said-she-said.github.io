Basically I'm trying to dynamically load a picture into a submitting form(like ebay), by sending the picture to my api through a post request, and then load an img element with the Url to the uploaded picture 