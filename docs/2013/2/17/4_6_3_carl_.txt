Because if I have a form in the page and a submit button the whole page will reload, but If I only have the input field and use a controller to send the post request, it doesn't get enctype='multipart/form-data', even though I encapsulate it in a form