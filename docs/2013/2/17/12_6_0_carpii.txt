i need to serve html files (and socket.io client js), in addition to creating a web socket server. Is it best to just have node server both http and sockets, or is it generally recommended to just serve the static files from an httpd server like nginx, leaving node just for websocket comms? 