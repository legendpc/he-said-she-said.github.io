hi, im just learning about node and express. If i create a template express app using 'express --sessions --css stylus --ejs myapp', then app.js has 'var routes = require('./routes')'.  But /routes is a directory and I cannot understand what its actually doing with that