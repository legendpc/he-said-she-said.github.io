ok, so timeout checkups until the server is fully staged, then let the com sockets govern the task scheduling, and failover back to a timeout checkup system to bring the server down after resting the database..