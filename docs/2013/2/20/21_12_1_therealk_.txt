I'm running into a browserify issue. If I have a require('../../../component/widget/type/foo') it returns the correct object. But from somewhere else, if I require require('../type/foo'), it returns an empty object. Any clues on how to debug this?