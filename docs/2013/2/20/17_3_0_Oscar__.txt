Hi! What do you suggest for building an simple api with mongodb as the database. I found some specific options for mongoose but they don't seem very maintained. The server only needs to serve JSON and don't do any rendering.