RSA_: npm is the package manager that lets you install stuff... it is bundled with node. you shouldn't need to trying install it. all you should need to do it `mkdir testapp; cd testapp; npm install socket.io`