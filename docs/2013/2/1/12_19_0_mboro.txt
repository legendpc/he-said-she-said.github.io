hi! I'm using i18next for translation. I want to set certain translated strings in `app.locals` but I can't do that before i18n has fired its callback. is is ok/best practice to set `app.locals` and bind `app.listen` in the i18n callback?