systemfault: `new` isn't pseudo classical inheritance.. in fact, it's not even an inheritance mechanism.. all it does is set the context of 'this' within the constructor function and return a new object with the constructor .prototype as the new object's prototype.