looks like the httpRequest doesn't want to wait for the mongo callback, and closes the connection immediately. trying the same thing via browser works like a charm, but I cannot hijack the Host header via Chrome...