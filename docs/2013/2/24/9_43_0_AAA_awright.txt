Also, which other system is cool enough that you can specify a recipe that has a preresiquite on *some* file, containing a wildcard, invokes a recipie to generate a file matching that wildcard, and returns to the recipie the filename with wildcard filled in