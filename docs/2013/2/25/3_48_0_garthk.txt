substack: Feeding millions of values from ~1.5TB of XML through aggregation functions to get a few hundred graphs. Original time resolution is 4K pixels wide, resulting in big fuzzy caterpillar graphs.