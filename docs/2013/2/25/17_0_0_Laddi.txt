I have a question (might be silly):  Has anyone implemented a way for me to do a request to a server that fetches a file from another server and then pipes it down to the original caller as an attachment?