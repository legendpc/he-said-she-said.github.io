fruitFly: Did you get node working? It's easy. Download the tar.gz source. cd to the downloads directory. Type 'tar -zxvf node.tar.gz' (or whatever the filename is). cd into the node direction. type './confugre'. Then type 'sudo make install' and if it prompts for your password, type that in.