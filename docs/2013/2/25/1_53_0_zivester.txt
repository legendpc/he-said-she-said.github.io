is there a way in express, to override a res.render, and output the locals in JSON? i.e. if I want ?raw=1 to run normal application logic, but instead of sending back the HTML, just send the res.locals as JSON