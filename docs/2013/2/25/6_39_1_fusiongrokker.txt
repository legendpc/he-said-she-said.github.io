so given that I've got authentication working and can stuff something in the session to indicate logged in / not, what's the cleanest way to specify which routes require logging in? a route middleware?