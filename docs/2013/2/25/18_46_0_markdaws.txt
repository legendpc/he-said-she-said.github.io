hi - how does npm determine what the global link directory is (used in npm link)?  I don't have an .npmrc file and I don't have NODE_PATH set but somehow it is choosing a path relative to my node install node/lib/node_modules is that the default location always, relative to the node install?