I have a working request handler for a http server, which stops working (it looks like the request never emits 'end') when I wrap the handler code in a callback for a mongodb query. Isn’t that supposed to work? How else am I supposed to look up stuff in the db when I receive a request?