i have an array indexed by strings, whats the best way to remove an item?  Im using delete array[uername] at the moment, but apparently this just sets it to undefined, and doesnt actually shorten the array length