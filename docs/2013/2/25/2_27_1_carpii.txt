in socket.io, is it better to define a different message type for each message (thus writing seperate .on("messagetype" handlers, or better to send all messages as the same type, and use the same handler as a router?