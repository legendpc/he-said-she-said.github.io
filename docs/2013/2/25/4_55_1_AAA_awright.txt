fusiongrokker: bcrypt is modified blowfish, which isn't designed as a hash, and it's *modified* blowfish at that. There's no good reason to make modifications to cryptographic protocols. So I don't trust it. PBKDF2 is a published standard, anyhow.