Super simple question: I'm writing mocha tests. Should I do something along the lines of 'system("./program");' to to run my program in the tests or am I missing something? If true, what's the way to run shell commands from inside node (like ruby's `system`)?