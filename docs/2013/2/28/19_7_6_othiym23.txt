Gambit--: as turnkey as we can make it ;) -- install the module, create a config file or set some environment variables (documented in the README), add require('newrelic') to the top of your main script