I suppose them simply clicking on a specially crafted link that includes the hmac'd user in a GET param might work just fine, verify it like we would a POST to a login page (no secret is sent, just hmac hash which can be validated along with uid)