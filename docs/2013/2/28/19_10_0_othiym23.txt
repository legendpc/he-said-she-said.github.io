Gambit--: for now it's 100% JS and its only dependency is bunyan, because I didn't feel like writing my logging module (so it's pinned < 0.14 so the node-dtrace stuff isn't included, due to its native component)