seems like it's a security vulnerability if you are using npm and shrinkwrap to get your packages and someone can just replace a package content with the same version number (what if the package publishers machine has been compromsed and bad code gets uploaded to the same version)