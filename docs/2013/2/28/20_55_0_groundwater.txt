warz: the exports object is created _before_ an object is loaded, so everything will have the correct reference to the modules exports, but that exported object might not be filled with anything meaningful until later