I think, since the cookie will expire, the application secret can only be revoked, I might just append on the link GET params that validate the user and when express receives it, it can log them in and redirect them to their online content page