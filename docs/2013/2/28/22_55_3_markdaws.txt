@isaacbw - what if a package is compromised, say a bad person can slip in some code that reads all the files from your disk into the "async" package and publishes it with the same version, no error checking will catch that