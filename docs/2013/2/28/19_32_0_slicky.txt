Anyone know if you can simply take an express session cookie from a nodejs https.request response, inject it into another browser, and have that browser use the cookie and be successfully logged in (POC for chromium embedded frame where nodejs has direct access to the chrome frame & cookies)