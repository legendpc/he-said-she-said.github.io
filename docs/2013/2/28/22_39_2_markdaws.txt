@isaacbw - could get from git or the top level package (if it was present), but what about dependant packages?  The version the top level one depends on might be hard to get from git etc and a lot of work