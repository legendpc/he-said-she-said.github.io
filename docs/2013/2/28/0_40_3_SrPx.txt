samholmes: OSX, no problems with command line, but what do I do exactly? Ask the host an ip where I can connect and make commands from my terminal, that will be executed on the host machine? Is that it?