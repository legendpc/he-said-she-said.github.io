what is the best way to read a variable from a json file, I have the file loaded and parsed, but my issue is with selecting it. Currently I have var customer = config.routing.XX.customer; where I have XX stored as a different variable