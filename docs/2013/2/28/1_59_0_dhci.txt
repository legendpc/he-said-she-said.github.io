mscdex: I noticed that NodeJS would work really well with say a jQuery submission of a json string (application/json) but for just parsing form data you can't exactly use any built in function that I can see