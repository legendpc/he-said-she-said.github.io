wwicks: Well it looks like that, but if you use the new FuncName keyword, and that function has a prototype with variables, you can effectively create a class that's not a hash table. V8 won't actually use a hash table until it's more efficent than a struct