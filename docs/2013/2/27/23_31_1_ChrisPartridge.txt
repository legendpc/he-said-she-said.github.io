context: pretty sure express has a method override middleware, which basically means you send a post request with an additional paramater (such as _method), and the server will translate it to a "put" request