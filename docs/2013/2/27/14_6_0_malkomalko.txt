anybody ever run into any weird when running some commands with child_process/exec? sometimes it appears as though the command doesn't send any output to stdout.. even if it works from the command line, and even if I wrap it in a bash script