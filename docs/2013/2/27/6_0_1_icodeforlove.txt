ChrisPartridge: i would if i could even figure it out, it seems like their embed server uses express 3.X, so if you just comment it out it will run on 2.X (i was getting a bunch of issues on 3.X because they are utilizing body-parser)