specifically I have postDATA coming in via chunk form, which I can log and I see its there. But to break it apart should I use something like strtok or the previously described querystring or qs.parse functions?