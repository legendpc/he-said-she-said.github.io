creationix: so, if you're going to go with whitespace based indentation, have indentation work like: `<indent-block> :: <newline> INDENT <block> DEDENT | <indented-block>`, where `indented-block` forces everything to start on the same column the block starts.