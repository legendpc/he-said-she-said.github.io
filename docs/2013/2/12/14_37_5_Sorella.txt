tongcx: WebSockets are a protocol for real-time communication between server and client with low latency, it was designed because people were misusing XHR for doing push services (where the server sends the data to the client).