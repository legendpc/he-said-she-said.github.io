tongcx: MongoDB is a document store. You use it to store data as documents that have little to no relationships to the other documents you store. Redis is a key/value store in memory, you use it for anything that you need blazing fast access (read/write) for simple data.