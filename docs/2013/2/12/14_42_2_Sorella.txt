Trex005: there is a way of making things global, but *YOU DON'T WANT TO DO THAT*. Global variables add tight coupling and defeat the use for modules. What you should probably do is just `require` the module in your other modules, or make them parametric, so you can pass objects over to them.