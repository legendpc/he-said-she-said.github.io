What I want to realize for the beginning is: aggregating different sources of RSS Feeds, and "data base queries"(?) (- I do not know the name for those, but structures like: http://xx.com?q=a&w=12), XPATH queries, filter them and output them on a website / in an app, and store them.