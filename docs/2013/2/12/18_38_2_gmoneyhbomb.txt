cloudbender: it depends on who is running the callback stack, but generally speaking the method that loops through the callbacks would be the outer context, each callback of course has it's own execution context