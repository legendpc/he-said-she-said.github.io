Sorella:  when optimization time comes, i like to bundle a lot of the common files together, then lazy load others as needed… for instance, why load the code for a very complex widget if it's only used in one spot