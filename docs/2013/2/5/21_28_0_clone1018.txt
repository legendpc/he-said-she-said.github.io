ANYTHING can go inside the code we're evaling, including long ajax requests for example, the problem is by the time we've finished the ajax call we've already moved on to the bottom part of the simplate, so the variable isnt set and we cant use it