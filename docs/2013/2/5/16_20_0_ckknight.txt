clone1018: well, if you use Function, then you can pass in your own callback immediately. If you want to retain eval, which I don't recommend, you can wrap the inside in a function(callback) { }, then invoke that