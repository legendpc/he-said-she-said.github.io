hmm if i do that in background with a process, then i would define a callback function (which also blocks, but it just assings that value). if some http requests would block node, the the callback is also blocked and i cannot garantee that its updated every 3 seconds?