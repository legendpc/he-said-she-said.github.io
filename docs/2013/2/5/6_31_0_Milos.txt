the client connects, so the server sends the client the current servo position. in order to get the current position, it queries over serial. that part is easy. now how do I READ that response synchronously and reply.