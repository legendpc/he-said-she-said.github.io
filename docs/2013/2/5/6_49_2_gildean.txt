Milos: if you can pass the socket.id around the functions that handle the serial-ports events, then you could just pass the id first there and then on answer go like: io.sockets.socket(< session id>).emit('event name', arguments)