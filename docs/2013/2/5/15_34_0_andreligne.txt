Hi! I'm wondering how I should use a database with node. I will have multiple controllers and some of them will need to make database queries. Should I just make a databaseController and then have multiple connections which each instance of this controller?