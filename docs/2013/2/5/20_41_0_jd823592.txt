Hi again, does anyone know why when i create mongoose Schema {user: {type: Schema.ObjectId, ref: "User"}, text: String} and then create an entry in model with this schema any subsequent queries return objects that do not have 'user' field (not even after populate("user")