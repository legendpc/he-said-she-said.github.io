This framework's pages work like a CV (controller view), you have 3 sections of a page, top is ran once, when the page is first loaded, it's for use with defining globals and requiring libraries and what not, middle is ran every time, bottom is what we render