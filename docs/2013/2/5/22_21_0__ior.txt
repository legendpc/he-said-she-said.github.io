so i have a node app that at some point calls a child_process.exec that's a critical section. so i mutexed it (using a simple lock with a process.nextTick()  and also a bakery algo that doesn't do empty spinning) , yet node hits 30-99.9% cpu time