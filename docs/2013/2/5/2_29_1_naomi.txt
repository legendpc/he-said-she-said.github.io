jorge_: that's unlikely because you can only attach event listeners to the proc after you initialize one. e.g., `var proc = child_process.spawn(...); proc.stdout.on("data", ...); proc.on("exit", ...);`