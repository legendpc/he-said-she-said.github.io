one question though: in the example you gave, the mysql module would have to implement the inject method, right? and the redis module would only be loaded when the mysql module is finished? and thus when it starts loading the app module both mysql and redis are for sure loaded?