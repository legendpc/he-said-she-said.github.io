TooTallNate: yeah i was hoping so as well, but 'npm rebuild' doesn't show it compiling the binary at all. i think the project is in flux right now, it only has a target for darwin in the rebuilt.js for instance.