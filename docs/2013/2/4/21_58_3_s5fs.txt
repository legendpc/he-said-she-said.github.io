bnoordhuis: npm rebuild returns immediately. stepping into the modules dir proper and running 'node rebuild.js' (as instructed on the project git) returns immediately and does no work. further inspection shows that they're checking process.platform and only building for darwin