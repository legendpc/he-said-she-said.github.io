well if node-webkit somehow abstracts an API that allows you to make sockets and lets you do other stuff without having to use an HTTP server for the app's internals, then I would be in favor of node-webkit