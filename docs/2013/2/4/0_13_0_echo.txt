TommehM: If your node.js script is built to executed via command line and not run as a daemon, you could use shell_exec in PHP to call execution of your node.js file using node. However, that's an ass-backwards and insecure way to send email, imo.