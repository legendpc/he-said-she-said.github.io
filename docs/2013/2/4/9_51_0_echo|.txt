The same way Riak does their map/reduce. It distributes the jobs to the nodes which the partitions for the data you are querying against lie. Bringing the computation to the data instead of the other way around, which is more expensive. 