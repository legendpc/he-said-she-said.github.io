any ideas why if I have two dependencies in my package.json file, and do 'nmp install' the first time it fails http://pastebin.com/CPVNCtU8 then if i just run 'npm install' again it works. and if i remove the node_modules dir and it redoes it all it works fine the first time after that?