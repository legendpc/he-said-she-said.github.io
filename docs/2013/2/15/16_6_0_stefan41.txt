anyone a nodejs / windows wizard? I've got a script that works great on linux / mac, trying to port it to windows. It runs a ruby script, but the subshell that child_process creates doesn't seem to get all of the %PATH% (windows) that it needs