othiym23: i have a question. at the moment i'm accumulating data into a buffer until the connection is ended, then on end i'm running JSON.parse on the data. what if i wanted to send multiple packets of JSON data over the course of one connection?