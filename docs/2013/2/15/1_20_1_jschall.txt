hmm, i just ran into an interesting problem. i wrote a tcp server that asynchronously runs a hashing algorithm that takes a lot of time... i set timeout to 10000ms and i socket.end(); in the timeout handler, but it doesn't stop the (very cpu intensive) hash