AAA_awright: some processes might execute in a separate thread. And you can spawn other processes to run some code and communicate through message passing. Still, the user code is synchronous *and* single-threaded.