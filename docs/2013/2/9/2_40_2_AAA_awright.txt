Even if we only consider the user-exposed API, point it is that it's not *merely* single-threaded (default for programs unless you -lpthread), it's event-oriented, as opposed to most "single-threaded" programs