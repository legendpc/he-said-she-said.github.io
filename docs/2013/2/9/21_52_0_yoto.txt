Hello. I have an weird behaviour with npm. I tried to manually update an package-dependency. The package.json of the 'main'-package just specifies the git-url of the dependency. I thought it would fetch the newest version with 'npm install x', but it always installs the old one. any idea?