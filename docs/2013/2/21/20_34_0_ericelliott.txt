bradleymeck: I read somewhere that relative requires can return different results for singletons... require('../foo'); in one module might return a different singleton than require('../../foo'); in another module. Is that not the case?