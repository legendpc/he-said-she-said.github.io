I'm loading some settings for my app using var config: require('./../config.js').config but to be able to access this in other required files I need to also load it in those files. I found that you can do "global.config" but is this considered bad practive? (Using node.js and express).