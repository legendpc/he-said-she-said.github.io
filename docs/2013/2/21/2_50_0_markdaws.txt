hi - if I do an "npm config list" it shows a value for the "node bin location" this path seems to be where npm installs the global symlink when you do an "npm list".  How does this location get set, I made sure I don't have anything in my .npmrc file and I don't have a NODE_PATH env variable