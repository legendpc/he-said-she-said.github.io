Hi guys, newbie question: how can I export a variable from app.js? adding exports.foo = function(){return foo} (to the bottom of app.js) and then trying to access this variable on other js file via: app = require('./app'); app.foo(); doesn't seem to work. Any tips/hints?