bradleymeck: now i have this: app.use(express.cookieSession({ secret: config.sessionsecret, cookie: { maxAge: 60 * 60 * 1000 }})); and get the TypeError: Cannot read property 'connect.sess' of undefined so it looks like it needs connect ?