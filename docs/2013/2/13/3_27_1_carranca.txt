Hi, im using node with express, and have some doubts with the require and paths. If im inside a module called controller/user_crud, and want to access the model/user, I should do something like require("../model/user"). Isn't there a way to define a path relative to the base module?