sshaginyan_,  so basically you eliminate the need for threads when doing I/O. But you're still using some CPU (e.g. to serialize to JSON or call template rendering functions) so multiple processes are still beneficial (although you don't need as many as you need threads with blocking I/O)