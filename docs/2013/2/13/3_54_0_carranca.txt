denom: the namespace thing is optional there, right? I just did an example where model/index.js has a exports.user = require("./user.js") then I can call in my controller model = require("../model") and it works... and at least I only have to move one reference around