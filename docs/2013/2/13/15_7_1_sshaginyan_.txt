So the operating system will distribute the load amongst the 8 cores? But v8 holds a queue of all the requests and as soon as a single I/O operation occurs on a thread another request id popped from the queue?