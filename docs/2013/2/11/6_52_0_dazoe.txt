so, the mongodb client for node.js has a few options that are confusing me. connectTimeoutMS, and socketTimeoutMS. according to http://mongodb.github.com/node-mongodb-native/driver-articles/mongoclient.html "For new implementations, the default should be to never timeout." Is that a good idea?