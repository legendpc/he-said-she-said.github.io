So, I have a follow up question in the sharing logic browser and server side which is probably best asked in a JS channel than node. How can I bind a js mixin to an array of models without looping through the array and extending each object?