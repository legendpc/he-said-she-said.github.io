bradleymeck: it downloads it to a temp folder, then unpacks it into the temp folder, then re-packs it into the cache tarball, then unpacks it into the cache folder, then reads cache-folder/package/package.json to see what it is.