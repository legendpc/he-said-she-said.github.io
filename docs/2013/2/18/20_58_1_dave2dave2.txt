hi guys, is there any way to specify dependency as local tar.gz file/package in package.json? It is possible to do so in 'npm install package.tar.gz', but I haven't found a way to specify it in package.json.