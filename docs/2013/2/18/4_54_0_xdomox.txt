im trying to keep a list of online clients and their user names and w/e anyway so when the page changes they quickly disconnect and reconnect, which updates the array and w/e, but the array quickly gets messed up