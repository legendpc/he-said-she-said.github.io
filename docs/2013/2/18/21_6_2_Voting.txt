gildean: I'm trying to understand "then you need to either do the emitting in the callback of the connection, where the client 'socket' resides, or use io.sockets.sockets[socket.id].emit out of the scope while passing the clients socket.id forward" now... THANKS FOR YOUR HELP! 