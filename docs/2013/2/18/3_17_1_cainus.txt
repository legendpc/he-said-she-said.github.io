somebody sent me a pull request for my queue client that emits events instead of forcing the user to poll... it's so much more node-ish, but then it makes you just through more hoops to keep things sequential... :\