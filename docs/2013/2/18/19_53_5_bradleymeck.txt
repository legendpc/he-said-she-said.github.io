owen1: if it is purely notification with potentially multiple consumers: use an event (you can add callbacks as desired). if it is interruptable ala middleware look into a middleware framework for this sort of thing.