for a webcrawler that is network io heavy and only needs to disk write a few string values of 0.2 to 4KB in size per page, and hash them with data from just one other page, do you think mongo or redis would be better? or something else?