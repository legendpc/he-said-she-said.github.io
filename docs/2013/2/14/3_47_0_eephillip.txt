I'm cross compiling node for arm, the resulting binary is good to go, but the regular make install calls the tool/install.py script which places quite a few files. What function do these additional files have?