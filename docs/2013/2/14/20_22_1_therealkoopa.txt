What's the best way to get npm to install the dependencies for a bundledDependency? I see there's an issue for it, so it will come sometime. It also seems like a lot of people use a make file to loop through ecah bundledDependency and run npm install on it.