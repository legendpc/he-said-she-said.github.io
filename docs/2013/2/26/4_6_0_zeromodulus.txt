say I have a shared lib, this shared lib is a folder, and in it is a file index.js. I wanna be able to type `var lib = require('./lib/index');` and whatever index.js requires, will be bolted on underneath the variable `lib`, is that possible?