pachet: I guess a quick hack would just be to make sure that i can alias the "type" of the "require" invocation results if i detect that the module writes to module.exports, the "longer" option (where i might have to go eventually anyhow) would be more extensive type inferencer