If I have a function and wish to specify its behaviour using tests in the form "it returns X for input Y", is that a) a good way to write unit tests, and b) is it good to loop over an array of input/output examples to build the test cases instead of coding each one individually?