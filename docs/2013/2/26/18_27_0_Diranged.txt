Is there a way to apply a regular regex exp to a string in NodeJS without using the .replace() syntax? Here's what I want to do… take var 'foo.bar.test.blah' and apply an arbitrary (user supplied) regex expression to manipulate it..