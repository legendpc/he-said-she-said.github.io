extraction should go to User/Application Data/Local if they extract by default which does get you some nice things, but causes issues with multiple versions, keeping single exec is much cleaner but causes problems with nested shell scripts