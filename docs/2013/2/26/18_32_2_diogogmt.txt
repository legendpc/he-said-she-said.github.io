any suggestions which HTTP verbs to use when creating a RESTful API? I usually just go with POST and GET, however, I see that Backbone.js fires a GET, PUT, POST, DELETE depending on the action being performed on the model