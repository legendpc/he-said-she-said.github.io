I'm being a bit naive in asking this - but is there a way to avoid creating an ssh tunnel (via ssh -f -N xxx.xxx.xx.xx) in order to run queries on a remote db?  Perhaps a node module that can create a tunnel and send requests through it?