That specifies how to render a piece of content for inclusion in the HTMLBody region of a document. I could specify other templates and include content from that blog post elsewhere in the document. This is how I render the <title /> tag, for instance