I wanted to use nodejs for writing an executable script (like bash). node can be installed in lots of different paths but it seems I have to specify exactly where it is. like #! /Users/durre/nvm/v0.8.16/bin/node … is there a way around this?