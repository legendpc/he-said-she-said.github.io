Yeoman uses a simple function with connect.static(dir) multiple times to serve multiple directories with the middleware. I'm guessing this makes the node server think that dir1 and dir2 are both in the root of the same server. Correct?