beeant-macbook: I've never had enough SQL to warrant putting it in a separate files (unless it was upgrade/migration SQL). but if you have large blocks of text that you don't want uglying up your code, just load it at runtime when the module is first required.