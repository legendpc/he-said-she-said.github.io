SrPx: if you want to do it all in parallel, you can just keep a counter that when it hits 0, calls some callback. If you want to do it serially, you can have it simply churn through and on the last one, call a callback.