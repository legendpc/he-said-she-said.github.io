In my package.json i have two dependencies on the same private git repo but with different branches.  This seems to be giving npm a world of trouble because when it checks out the second one it already exists.  Is there a different/better way to do this?