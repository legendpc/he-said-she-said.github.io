AAA_awright: So a user has a persistent auth cookie. They visit mysite.com /index  which sees they have an auth cookie with the correct series and token. They are then granted a session cookie which allows them to open the SPA and a socket. Is that the correct way to do it?