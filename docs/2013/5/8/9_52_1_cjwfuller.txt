ningu: I only started Node yesterday :) I was just checking if it would be possible (seems like it would be) and how to do it in Node.  Currently I can proxy the requests and block a page but I can't 'let the page' through e.g. make the Node server do nothing basically