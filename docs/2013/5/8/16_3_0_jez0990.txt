if I want to push or pop with an array, what's the best way to return the array after performing the op instead of the length property - wrap it in my own function? or does something handy that does this exist already?