@Sadtaco so how exactly would a Node/NGINX setup work..?  Let's say I have a network of machines.  Each machine does lots of HTTP/80 requests.  I need all HTTP/80 requests to go somewhere so I can say "this URL is fine" or "no, this URL is NOT fine, you can have this page instead"