also `npm install -g testling` gives you a `testling` command that reads source from stdin such that `browserify test/*.js | testling` will output the TAP from real browsers on stdout and give you a proper process exit code based on the test output