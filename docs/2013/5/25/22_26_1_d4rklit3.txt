is there a different how exec should be used in windows vs mac? I have this line (in cake file) child = exec "NODE_ENV=#{options.env} APP_DATA=#{options.data} node server.js"     will run on mac but not my windows