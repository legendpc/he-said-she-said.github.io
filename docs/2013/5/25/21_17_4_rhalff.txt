weird, I use express.json() but also defined my own function, both set a req.on('data', ...), it seems I can only use one of them, not both. req will be a continues object? a fresh request modifies the current req and is not a new request object or something?