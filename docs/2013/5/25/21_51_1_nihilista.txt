i have a simple app which, when started, reads some configuration from config.js file outputs something to stdout… i'd like to have an option in config.js to run this app as a background process or not. if it should run as a bg process all stdout stuff should write to a file...