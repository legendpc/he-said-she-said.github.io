macbn: actually if you make your middleware mount a specific directory and you pass your own response handler to the static middleware it might not be so bad, take a look at ecstatic or use connect's static server mw, either way