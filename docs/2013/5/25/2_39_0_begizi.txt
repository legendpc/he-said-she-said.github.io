Havvy: I have some code now. I am using ttezels twit api client from github and once a user authenticates it creates a new instance of the twit object and starts their steam. I feel like this is too resource intensive. What would be a better way to start parsing a users stream?