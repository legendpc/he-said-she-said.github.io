I have an express route I want to implement that will have paths like /foo/count, /bar/count, etc. it will be in the same in every controller. basically I want the count handler to call foo() and bar() in each controller, where each controller defines its own foo() and bar().