Callback functions are wonderful for the first part of that implementation, and load balancing those functions is doable, so now we need run-time or compiler-time metadata to help drive which nodes they get distributed to