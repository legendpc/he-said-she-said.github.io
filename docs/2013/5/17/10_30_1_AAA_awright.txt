luminous: Because of a <script type="application/ecmascript"/> or whatever tag that isn't being sent as the declared media-type, here, it's being sent as text/plain. Browsers are supposed to ignore the response entirely, but most just execute the response as script anyways.