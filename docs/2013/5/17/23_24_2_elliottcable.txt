(My code has to work across both Node.js and the browser, and I'd really prefer to use the worker API so that the same code operates identically on both client and server ... but I can implement two styles of threading if there's a good reason.)