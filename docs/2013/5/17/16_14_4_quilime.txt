bashing my head against this one: has anyone had issues connecting to heroku-postgres via localhost with juggledb-postgres? I'm having issues forcing it to use SSL — it seems to be off by default...