geNAZt: yes, it does, because the cache key is based on the string that you pass into require()... so if you have stuff in ./models and require that from ./foo and then require it again from ./foo/bar -- the module in ./foo/bar gets a different instance than the module in ./foo