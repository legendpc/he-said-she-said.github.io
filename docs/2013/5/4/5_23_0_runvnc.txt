if you require('blah') in one place and there is a node_modules folder there but you require('blah') in another file in a different directory that has no node_modules but 'blah' is in the node_modules for your user, then it will be a different object