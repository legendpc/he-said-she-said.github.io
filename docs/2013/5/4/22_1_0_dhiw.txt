are there any performance benchmarks showing static file serving of Node.JS versus nginx or varnish web cache? For certain relative urls you could serve it from the faster one but I'm wondering if Node.JS can be used as a front end for everything, and then use Node.JS as a proxy serve to reach the other daemons and the file store. Just an example