the bigger question... why re-implement something when the OS has great support for and is a super well know? as far as logging, all cron does is run a shell command, so >> the file, or have your cron job itself post its logs to a server or write to 1 of 1000 logging libraries