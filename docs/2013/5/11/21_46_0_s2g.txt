anyone has experience with the `forever` nodejs package? I have a silly problem and can't run the application outside app's folder. I'm trying to do `forever -a /var/www/myapp/app.js` but then relative paths get mixed up and nodejs throws an error that it can't find my `routes` folder