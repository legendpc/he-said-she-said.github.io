foofoobar: for example, 'connection.js' contains `var mongoose = require('mongoose'); module.exports = mongoose.createConnection(uri);`. then you can just require('./connection') and get your connection