Alternatives to requiring it would be using fs to open, read and parse the file. Which is what require is optimized to do, I don't see value in using another method of just reading+parsing+exposing the file when there's one meant to.