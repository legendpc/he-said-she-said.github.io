peterschmidler: Merely being authenticated as a certain user isn't enough to determine they have authorization to do something. For example, you must never allow non-safe (read-write) operations from a Cookie or other header that is automatically submitted with each request.