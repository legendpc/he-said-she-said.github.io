BobAlmighty: regarding the prototype, you can just attach the variables to an object property or as separate properties on `this`, like `this._tick = 0;` inside your constructor, then use `this._tick` in your prototype methods