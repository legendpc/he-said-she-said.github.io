Hi. I'm using express.js to create a small site. Now I want to make am XMLRPC call to a local service and get some data. The client should be initialized in my app.js. How can I use this client variable in a different required file now (var routes = require('./routes').