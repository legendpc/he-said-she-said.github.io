tjfontaine: the code is way too big and the leak is not obvious, it's not even in the v8 heap, I'm either causing a native leak (like doing something wrong with buffers that accumulates) or there's an actual leak in some native code