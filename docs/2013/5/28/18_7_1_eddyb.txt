either in v8, libuv (I only use stdout and stderr, I doubt it's uv), something in node, or the native parts of the few modules I use (then again, node-ffi does work only at the start of the process, so it - or the C library I use it for - couldn't be leaking long after)