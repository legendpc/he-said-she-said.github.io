if I would hook malloc, then that could give me a size and a backtrace. then hook free to remove that object, output the objects, compact the backtraces, and you get a nice tree of function calls, sorted by total allocation size