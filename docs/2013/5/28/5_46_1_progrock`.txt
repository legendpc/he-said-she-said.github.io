hmm, I'm trying to find user in my users collection (mongo) and update the lastLogin field... I'm using findAndMondify, but instead of updating the lastLogin field, its replacing the whole user document with just the latLogin field