JediUnixMaster: the group nodejs doesn't exist, node.js doesn't need/use a specific group for anything. if you're not running node.js as root, you don't need to touch root's settings/groups/etc. at all