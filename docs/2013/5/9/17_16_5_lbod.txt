just looking for info what gets published, im presuming when you publish it's whatever is in your local git repository at that time and it'll npm tag with the version that existed in package.json  i.e. there's no connection to git tags at all