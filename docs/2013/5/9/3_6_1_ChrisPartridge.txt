it parses it, because if you do "npm install some_package", you might have a specific version defined in package.json - so instead of just getting the latest, it will get the version your application requires