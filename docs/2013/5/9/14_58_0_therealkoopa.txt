kenperkins: I'm trying to create a a smart function that hits some url endpoint, which returns either csv or json (but you don't know based on url or response type). If the data is csv I want to .pipe(csvstreamparser), otherwise, do something else.