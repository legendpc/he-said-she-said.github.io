it's a bit annoying on npm how the package pages are cached, so when you click on the "recently updated" ones on the front page, you only see a module that's older (in this case, 7 hours old update) https://npmjs.org/package/rarfile