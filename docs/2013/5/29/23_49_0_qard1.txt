Using one var i potentially can be helpful to reduce memory if, for example; you have hundreds of thousands of concurrent requests, each running multiple loops. i = 1 * 100000 is less than i = 2 * 100000, so will use half the memory. It can add up—albeit, rather slowly.