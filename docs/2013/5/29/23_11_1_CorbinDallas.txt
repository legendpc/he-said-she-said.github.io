all the arguments come through, even the object "dataCache" in question (that's why I put some empty keys in it - to check) but when I alter the object the calling closure doesn't see the changes to "dataCache" that I've made