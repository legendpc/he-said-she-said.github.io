I'd like to play with 'bare' node (using only formaline to parse multipart/form-data). Does it make senese for a request dispatcher to have a list of regular expressions ? or it should be better to have pages organized in a tree ?