levilewis: I see. you could definitely do it in two routes and you could use a function that returns a function to generate the route handler. but if you want it to be one route, why not just pass a query or body parameter?