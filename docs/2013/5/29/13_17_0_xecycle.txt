Does the pool distributed with node-mysql handle lost connections?  It's behaviour seems indeterministic to me --- sometimes it will reconnect, and sometimes not.  Requests to my server will simply hang.