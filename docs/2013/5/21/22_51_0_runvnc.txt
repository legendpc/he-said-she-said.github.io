does this sound like a dumb idea: what if npm had an option --cached that would look in a local folder for a cached copy of a package before hitting npmjs.org.. and then the cache could just expire every day