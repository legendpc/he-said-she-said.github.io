Say, I'm writing a barcode from image discovery library for work.  I'm at the point now where it can find and read code_39 format fairly reliably across some pretty heavy noise.    I could expand it to cover more formats, with better endpoints if there is any demand for it here.