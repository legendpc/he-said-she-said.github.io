Anyway, since a lot if not most of node.js libraries avoid promises and use plain callbacks, I think that a "task manager" that accepts generators which yield callback functions (like the ones you can pass to caolan's async) would be cool.