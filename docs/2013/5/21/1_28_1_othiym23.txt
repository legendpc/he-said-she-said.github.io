ericelliott: there's a harvest cycle that connects to New Relic's servers, as well as a periodic sampler. both are running on a schedule. I should probably unref the periodic schedulers, but that would require a bunch of documentation and would confuse people