spion: I think promises are a very specific API, if you return an object to which you can define callbacks, like readFile('xyz').onsuccess = function(contents){ /* */ } that's called events, or DOM-style events