rvagg: just to be clear -- it's installing the devDependincies in the sub-dependencies. so.. my package depends on substack's package, which depends on some huge package foo in a devDependency. foo gets installed.