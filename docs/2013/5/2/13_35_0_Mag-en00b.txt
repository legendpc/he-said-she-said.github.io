okie dokie new Error() is 18 times slower than new Object(). but yes, still, they are only generated in an error situation which needs to be handled differently. so ofcourse not for flow control, but for just that; error handling