but if you are curious, I'm making it so that there is no admin page persay, it's a clientside JS app on top of the site that pulls from APIs. you edit articles inline, use a fixed position toolbar etc. 