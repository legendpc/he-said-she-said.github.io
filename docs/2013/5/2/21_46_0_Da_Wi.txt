hello. I have a question to a specific module. it's db-mysql. If I make a select on data and want to get a string i get ->'string'<-, where the resulting string contains a ->'<- in front. How to get the real string without the apostrophe?