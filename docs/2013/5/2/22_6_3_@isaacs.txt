AAA_awright: but i'd like to eventually be more on par with Java.  we are for some things (especially startup time) but if you dump a giantass pile of CPU work on the JVM, it'll crank faster than just about anything.