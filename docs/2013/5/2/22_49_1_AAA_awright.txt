My assertion is that in all cases that one uses Buffer for reading data, you can use a Uint8Array for the same use, they do literally the exact same thing so the performance should also be the same other things being equal, yes?