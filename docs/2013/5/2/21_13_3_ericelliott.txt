if I was going to slurp some APIs now and then and inject the results into a bunch of HTML templates and cache the results, I'd probably use request, express, and handlebars to generate the cached HTML, and serve it up with express static server.