paultman, let's put it this way. 1. Node.js has a good native driver supported by 10gen. 2. Most node.js tutorials are based on mongodb, one way or another. 3. Mongodb is very easy to use with node because everything is JSON. Just more intuitive.