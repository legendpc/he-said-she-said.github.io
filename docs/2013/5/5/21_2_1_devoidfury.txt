yes, but the inner bits aren't "returned" like usual. Under the hood, jade is doing: var rendered = ""; rendered += asyncFunc(function(result) {});. You can't do `x = someAsync()` and get the result in place