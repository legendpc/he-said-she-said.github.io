Hi, I have some code that serves a resized image, everything works fine except (in chrome) the connection seems to stay open. my script ends with res.end(data, 'binary') and the browser receives a 200 http code.