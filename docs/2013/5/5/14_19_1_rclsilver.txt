hi, i've deployed node.js 0.9.3 in openshift, i've added a depency (node-xpmp⁻bosh) in package.json, but now, i would like to know how to start the application? openshift seems to start a default nodejs instance, i just want how to add the module in the process