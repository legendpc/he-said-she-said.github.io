Oh! I realize what is the difference: The node.js shortcut is for node.exe which simply runs the node executable, while the command prompt shortcut runs the Windows command prompt in a node & npm environment :) Woohoo!