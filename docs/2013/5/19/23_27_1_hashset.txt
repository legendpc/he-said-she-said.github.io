ningu: right, so something in the jquery + 'post' + res.download() is breaking.  would a ridiculous but feasible workaround be to redirect to a get method with the new filename passed-in the query-string?