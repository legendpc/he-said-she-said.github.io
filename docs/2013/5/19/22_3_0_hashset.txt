ok, let's try something more basic.  I have an express server and fire a $.post(…) client-side, I build a .zip file in the server and I need to send that .zip as a download back to the client.  Can anyone suggest tips on figuring this out?