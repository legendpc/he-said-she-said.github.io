sinclair|nz, 1) a folder (or list of folders) are scanned recursively and all symbols defined (modules, classes, interfaces) are written to an index file 2) references to a symbol get resolved against the index