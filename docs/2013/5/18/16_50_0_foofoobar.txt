Hi. I was asking yesterday here about replacing rendering via EJS+Partials on the server to just send the data by JSON and let the client render it. What is a good way to "render" it on the client then? Include EJS there ?