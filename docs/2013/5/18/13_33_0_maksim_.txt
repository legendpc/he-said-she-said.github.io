Hi, I have this npm package, when its installing one of its dependencies is in a private repo and it is installed as git+ssh://repo path,  I have the https credentials but do not have the ssh ones.. Is there a way to trick it to look into the https one instead of trying the ssh one and failing?