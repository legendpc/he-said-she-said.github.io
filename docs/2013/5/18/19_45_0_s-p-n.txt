from docs: "options can include start and end values to read a range of bytes from the file instead of the entire file. Both start and end are inclusive and start at 0. The encoding can be 'utf8', 'ascii', or 'base64'."  - http://nodejs.org/api/fs.html#fs_fs_createreadstream_path_options