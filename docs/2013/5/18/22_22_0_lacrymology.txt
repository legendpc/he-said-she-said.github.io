I've got a problem with callbacks within a loop. An iteration variable doesn't change between closures. http://pastebin.com/M2gm1aix << see the `playlist` at L2, I need to update it in L24., but the last one in the array gets to all callbacks