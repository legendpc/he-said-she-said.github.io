sinclair|nz: with new streams, you could do something like `var data = []; stream.on("readable", function() { var chunk; while ((chunk = stream.read()) !== null) { data.push(chunk); } }); stream.on("end", function() { do_something_with(Buffer.concat(data)); });'