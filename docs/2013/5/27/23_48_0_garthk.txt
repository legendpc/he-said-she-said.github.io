I was hitting one bottleneck by using object streams with a write per object. Node can only crank that around at ~0.5m writes/sec. It's great if you're throwing around 64KB buffers, but crummy if you're throwing around 90 byte objects. :)