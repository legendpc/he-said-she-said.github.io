I'm looping through a mongo collection with forEach… `comments` is the collection and I'm using `comments.forEach(function(comment) { //do stuff }); but I'm getting TypeError: Cannot call method 'forEach' of undefined at runtime