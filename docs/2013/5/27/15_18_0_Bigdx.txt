nathan7: well ... and how can i "set encoding"? i  want to get the uploaded data with "req.on('data', function(chunk) {...} but chunks is "different" then the source (file), i guess because of some encoding-problem