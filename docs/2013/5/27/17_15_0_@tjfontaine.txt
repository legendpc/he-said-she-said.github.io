r04r: it can be expensive to allocate a bunch of buffers, but if they're small you're going to fall inside the internal caching mechanism, so really just write it like you want it to work and then do profiling to see if that's sensible