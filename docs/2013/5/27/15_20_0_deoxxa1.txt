Bigdx: collect all the chunks up into an array, then use Buffer.concat() at the end to join them together. if you try to convert a random chunk to utf8 you might end up converting half a character or something (because not all characters are exactly one byte).