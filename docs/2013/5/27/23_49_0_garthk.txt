I wrote a chunky, new-stream version of dominictarr's 'through' module which deals with arrays of objects per write but keeps the same API style, which I quite like. It's still too slow. Back to the benchmarks. :)