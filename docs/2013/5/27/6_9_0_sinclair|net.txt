ningu: with WSDL, ive been able to dynamically build client API for several different languages at the click of a button, in fact, its completely automatic, as the builds are based entirely off the wsdl