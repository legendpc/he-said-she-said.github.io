AAA_awright: Yeah, but that's suboptimal because if I have the server behind a reverse proxy and a user POSTs and excessively large request, causing me to close the connection, the other users who are in the middle of a request on that same TCP connection will receive an error in their browser saying the TCP connection was ended prematurely (assuming the reverse proxy doesn't re-attempt the request on a