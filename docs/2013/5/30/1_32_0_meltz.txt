and once I have what I need, I pause the request's socket, send the handle via IPC to one of the workers, create a new socket with the handle, and emit the connection event on the HTTP server, passing in the new socket