so then you'd have a pointer within the text to annotate the start and end of each change… how would track that though? like on a schedule? say 500 milliseconds or 3 seconds? or by events? like X key presses? (I would go with some time interval just off the top of my head)