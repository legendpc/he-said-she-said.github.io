something like (but my example will be wrong, I don't know the details) var proto = require('net').Connection.prototype, originalConnect = proto.connect; proto.connect = function() {console.log('connect', [].slice.call(arguments));console.trace();return originalConnect.apply(this, arguments);};