I've got a package.json with deps to some git repos. usually the username of the current logged in user is used as user for the git ssh dep. Its possible to use a placeholder for that? e.g. npm install gituser=... and use this param inside the package.json?