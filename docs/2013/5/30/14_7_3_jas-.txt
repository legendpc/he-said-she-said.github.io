The latest W3C HTML5 spec includes it as a new element and with the current W3C group working on native crypto support for browsers (exposing the private key, or at least a reference to it) for browser encryption functionality this will come in handy for pushing user details (along with their public key) to a server for certificate signing by a certificate authority leading to easy certificate authentication