i'm new to node and was trying to write a data-loading script (i.e. 'read all the files in this dir, load each line of every file into a database') and it seemed just.. really unwieldy to try to do something like that in node