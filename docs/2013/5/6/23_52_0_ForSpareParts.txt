So, what I'm trying to wrap my head around is whether NoSQL DBs are good/efficient for the same problem space as SQL, where you have all this data that maps pretty cleanly to (what I tend to think of as) a bunch of related data structures you could model as classes. I can certainly imagine modeling the same data in a key/value store, but I have no idea if that would be efficient. Or do you just approach that problem differentl