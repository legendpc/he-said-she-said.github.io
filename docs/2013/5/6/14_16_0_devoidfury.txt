squeeks: you can get the tarball from a source by using 'npm pack', so write up a little script that does that automatically for your package.json => dependencies. Next, a little bash script that does something like "npm install *.tgz"