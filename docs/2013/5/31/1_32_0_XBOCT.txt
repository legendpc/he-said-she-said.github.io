Right now I'm considering doing something like myModule.exports.db = db; from the index module to set the db into the exports, then access the db using exports.db from within the module itself. would that work?