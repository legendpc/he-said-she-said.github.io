samholmes, I guess you can just modify that script, if you start it with nodemon that will be the part listening for changes and reruns your script, which reloads the page through that remote debugging interface.