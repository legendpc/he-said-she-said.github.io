Hello all, how would I structure an application where I have multiple modules that all require the same database module, and that database module should only be loaded once in total for all the modules because I only want one active connection to the DB?