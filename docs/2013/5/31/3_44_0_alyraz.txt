 _.each(availableCampgrounds, function(campground){                 var getPhotoSrc = client.query("SELECT src_url FROM campground_photos WHERE campground_facility_id = ($1)", [ campground.facilityID ]);                 getPhotoSrc.on("end", function(result){                   console.log(result);                   queryCount -1;                   if(queryCount === 0 ) {                     pageRes.render('campgroundResults', {     