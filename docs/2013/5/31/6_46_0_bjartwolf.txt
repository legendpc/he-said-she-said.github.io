In node 0.8 I can just pipe a raw fs readstream or a nodecopter to a spawned ffplay subprocess, and it works perfectly. In node 0.10.x it hangs after a while (10-15s). I've tried to listen to events, pass it thorugh throughstream, force to oldmode etc to track down the error, looked for similar bugs for streams but can't find any