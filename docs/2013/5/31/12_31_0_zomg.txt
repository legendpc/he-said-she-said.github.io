mikejw: if you have async code and want to get a value from it when the processing is done, you need to use a callback to move the value, so essentially you end up with a chain of callbacks with async code