Hello. Would somebody recommend an Express middleware that would serve templates in a particular directory a la express.static but rendering them first with the current templating engine (e.g. Jade) ?