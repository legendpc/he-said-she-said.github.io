fix2fax, could it be the delay is caused by conservative buffer lengths in the server or client? if you want absolutely no delay, make sure none of both use a buffer... or at least a very tiny buffer if not having one at all fails