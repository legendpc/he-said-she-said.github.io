Raynos: how much data are you pushing? i'm shoving small bits of data every 80ms across socket.io and allowing the client to publish to local subscribers (canvas elements). my cpu usage on the client is negligible.