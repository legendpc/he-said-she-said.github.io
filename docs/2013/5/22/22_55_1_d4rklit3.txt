Ok i have an express server, it uses stylus as middleware, and then this browserify thing is a route to build the js file, it also has a get("/*", res.sendfile(index.html)) so anything non-static will load as the index.html