How can I close a socket.io connection and stop the client from attempting to reconnect?  It seems to automatically attempt reconnects when disconnect() is called. And to be clear, I DO want it to reconnect when it times out and such, just not when an actual disconect() is called.