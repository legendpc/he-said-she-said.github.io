Depends on the complexity of what you are doing - but libraries like async.js or some of the function mutators in _ make it much simpler than it would be otherwise. I get a lot of mileage out of function declarations at the bottom, initialization code up top