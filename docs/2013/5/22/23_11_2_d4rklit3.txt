ok i ahve     (source, path)    source being the .coffee file. path being the route for the compiled bundle. could i have an example of how to set it up to watch the source for changes and update the file