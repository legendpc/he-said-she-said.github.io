aandy: Yea, sounds good. But if non-http related connectivity is an option (which it is), is there any reason for me to consider it over websocket? That way I could simply use the built-in net.Socket to work with the client.