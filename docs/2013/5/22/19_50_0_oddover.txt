hi all. I'm fairly new to node.js. I'm having trouble. I'm creating a custom module, and in it, I'm defining a function that returns some data from a postgres database. The issue is getting that data back out to the main program that uses the module.