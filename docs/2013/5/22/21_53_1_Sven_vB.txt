Ritzton, if the docs tell you what "this" will be set to, believe them. if they don't tell you about it, don't rely on what your exploration (using util.inspect) yields, because it might change in future versions.