r1card, also be aware that when two moules require() another third module, they might both get a copy-on-write protected proxy of that module, keeping them from influencing each other at least through the require()d reference.