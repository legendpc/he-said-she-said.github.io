Hi again! Do anyone have suggestions on good way to transform a JSON API result into another JSON result (e.g. remap it)? I guess I could use the .map-function, but manually writing if-statements for each of the possibly present keys doesn't feel like the "best" way :)