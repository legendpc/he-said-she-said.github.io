hey guys. i inherited a project which uses an orphaned npm module for it's build process, which currently has a bug. the fix for said module exists in a PR, but has no response. is there any way to direct npm to override the original module's namespace, and replace it with a custom repo?