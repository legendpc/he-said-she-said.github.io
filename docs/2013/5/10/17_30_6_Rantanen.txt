isaacs: Quick question on NPM - if I screw up first publish of a project, is there a preferred way to fix that? npm unpublish --force, npm publish or just npm publish --force with the same version? >.>