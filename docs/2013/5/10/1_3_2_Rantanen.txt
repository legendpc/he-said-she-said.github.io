Right. I've made the decision to bundle the compiled binaries into the module. Now the next question would be the syntax to embed current configuration string to binding.gyp? The obvious <(configuration) didn't work.