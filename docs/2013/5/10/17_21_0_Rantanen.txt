In that case I'd go with HTTP requests. Easy enough to set up in Node - unless you really worry about scalability in which case go for raw TCP sockets (if you need the reliability) or UDP (if you won't)