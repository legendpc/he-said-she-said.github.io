substack: interesting. Just wondering, browserify --ig returns pretty fast on the terminal, but it is actually still writing the file, right? Because my automation tool automatically refreshes chrome after browserify returns, but I get a 404 error, as in, the HTML file is not there yet. 