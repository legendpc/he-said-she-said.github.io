havvy: for example, in the past, if you wanted to consume a soap web service, you would need to generate a proxy class and compile it with your application, the proxy would be generated off a WSDL document