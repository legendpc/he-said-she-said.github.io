AAA_awright: so every time someone votes I'd need a `pre` save function on the SubmissionVotesSchema that essentially pulls all votes for that particular submission, and then updates my SubmissionSchema and adds the newly calculated `score`