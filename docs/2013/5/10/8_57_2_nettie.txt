jesusabdullah I know this, we select the technology based on the application needs. IF we decide to use node is becayse the app will have high concurrency or bacause it's interactive with slow remote webservices or anything which makes it interesting.