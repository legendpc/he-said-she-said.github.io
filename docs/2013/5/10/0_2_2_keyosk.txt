e problem is, there is no entry in the apache access log for the request that has 'timed out'. So we put an nginx reverse proxy on the same machine as the node to intercept and log the request, and then forward on to apache. here is 2 log entries in nginx :