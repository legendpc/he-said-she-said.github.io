ah, i see this in the docs now.  the problem is that the server that i'm sending to (in theory) will only accept multipart/form-data — so i'm assuming from reading that multipart bit that it would also fail :/