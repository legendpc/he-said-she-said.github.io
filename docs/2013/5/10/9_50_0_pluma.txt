sinclair|node, ko watches when you call observables within a computed observable or binding handler and then re-evaluates them when the observables that were called change. This can be surprising in some cases.