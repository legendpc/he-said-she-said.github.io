adamsmeat: AFAIK npm caches packages you've installed for a different purpose, but the same effect. If there's a new version, it will likely pull down that one instead. Try running npm cache ls / to see if grunt and it's dependencies are in the cache. 