We ran into several issues. One was that in addition to the global application state viewmodel we had these app-specific viewmodels, but we wanted to be able to access both of them. This wasn't so hard with custom bindings (just prevent the default handling and substitute your own context).