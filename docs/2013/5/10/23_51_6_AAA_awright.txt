sinclair|node: While the user may have the permission to delete content, their credentials don't necessarially also carry it. You must not allow deleting content from a cookie, for instance, even if the cookie authenticates the root user.