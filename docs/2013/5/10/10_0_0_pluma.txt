Our biggest mistake wasn't really ko-related. We used curl (cujo's AMD loader) and its plugins extensively. We didn't really distinguish between regular modules and "apps". So if you loaded a module, it could have side-effects just from loading it.