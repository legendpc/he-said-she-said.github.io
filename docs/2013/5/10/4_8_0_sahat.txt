ChrisPartridge: Hmm ok thanks. Then caching is not the issue.  I am using Ratchet UI framework (similar to jQuery Mobile), and every time I switch tabs on the bottom, tab icons flicker on page transition like it's trying to load and render them every single time.