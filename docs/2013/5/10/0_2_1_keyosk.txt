So for anyone that was in here earlier, having a problem making requests from a server I have in node (node is the 'client') against an apache server on a second machine (both machines are on their own private network), a very small percent of my requests are reported as 'timed out'. Now I currently have timeout set for 2 seconds, after first timeout, wait 200 ms, and re-attempt the request. In the majortiy of these cases, upon re-request it succeeds. Th