substack, how can bundle() emit any errors if the error generating code literally throws and error and stops the whole application dead because i can't handle it unless i modify coffee-script compiler