nikkubhai: you don't. you make console.log execute later than it currently does, by doing it inside your callback. also you can't just assign the return value of http.get to a variable and expect it to work.