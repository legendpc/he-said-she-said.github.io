ok, im using expressjs and can easily do NODE_ENV=prod node server.js to set the env. But I want to do it this way cli: node server.js prod, is it ok to alter the process.env.NODE_ENV value within my script so that I can make it dynamic