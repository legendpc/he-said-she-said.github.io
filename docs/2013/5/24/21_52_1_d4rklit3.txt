substack, for instance, coffee-script just has throw error   when there is a syntax error. i want to be able to catch and handle that error. when i do try b.bundle(opts, function (err, src) { })  catch err     nothing happens