I disagree, I don't think that's really a fair comparison at all. The prototype creation essentially creates a single object, assigns its prototype to core.prototype, and is done. The second iterates through an array, checks hasOwnProperty, etc.