caveats: it's java, if that's a deal breaker. it (can) use a lot of memory due to the way data is stored (a row needs to fit in memory, which shouldn't be a problem unless you have a *very* big slug with a log of edits, but you should be aware of it)