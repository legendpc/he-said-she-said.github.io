is it possible to make npm work like this: when install from github, run a script after installation, but run no scripts if install from the registry. that way, compiled files don't need to be committed into the repo, but the repo is still a valid package.