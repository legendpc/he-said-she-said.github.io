Fuzzyma: if you're on node v0.8.x, you can try using req.pause() at the beginning of the middleware stack, just don't forget to resume() it later on. if you're on node v0.10, then it should already be "paused" because of the way streams2 works