kenperkins: new ones, yes. If you're putting together a stream pipeline with both old and new streams, put PassThrough streams between the old ones and the new ones to avoid dropping data. (If you're streaming bytes rather than objects, the performance will probably be fine.)