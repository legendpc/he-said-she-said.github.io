It means that all symbols that you use are actually defined somewhere in the file, instead of just appearing out of nowhere (there are some symbols that appear out of nowhere, like `console` and `process` but that's acceptable to some extent)