I have an extremely noobish question. How exactly does one go about writing an asynchronous function? What exactly is the mechanism that stops the execution and defers execution until a certain event happens?