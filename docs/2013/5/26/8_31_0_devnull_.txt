I'm not sure how the runtime actually handles the execution of multiple variables but since a function in js is an implementation of a lambda it may be implemented in the traditional way but may also be optimized so as not to wrap lambada in lambada for each argument.