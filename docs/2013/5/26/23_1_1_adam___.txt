if i call the callback directly, it would be in a somewhat synchronous fashion because it happens immediately after counter increment, but i would assume that this EventEmitter would push it to the event stack