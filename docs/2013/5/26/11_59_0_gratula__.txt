probably is. i dont wanna use http-proxy for routing, just for proxying (i like the app.all stuff for routing). do u know if there is something kind of like the app.router stuff that can see the websocket requests?