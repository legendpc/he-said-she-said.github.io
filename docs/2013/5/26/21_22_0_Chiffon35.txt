About function like a module : do you know if the module is loaded when the require is parsed, or just when the function is called ? in other words, is there potentially lower performances with module ?