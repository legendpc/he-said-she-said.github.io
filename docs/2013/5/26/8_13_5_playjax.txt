well i'm fairly new to node, so ive probably yet to get into the state of mind where you just "stick everything in a module" the same way you would write a class object in other languages without worrying about the overhead of loading an external file