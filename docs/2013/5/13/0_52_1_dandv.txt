TehShrike1: right, which is why node makes asynchronous. Alright, so how can I stop the stream from parsing the file? What I'm trying to do is read chunks at a time on demand (the client controls the flow) from a CSV file. I haven't seen any CSV module to do this.