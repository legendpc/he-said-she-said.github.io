So DAO would be a Users object that would abstract the handling of a User object. And AR would just have the User object handle all of its own CRUD? And both patterns hang validation on the User object, right?