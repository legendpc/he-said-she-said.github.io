I have a simple db query that in the console works, it just executes a call to the db, returns the data as an array.  if i run it in the shell it works and returns the data but running the same call in my express app it returns as an empty object