He just needs something to identify a function taking a callback parameter, and when found abstract the remaining code after that function call into a separate function which is supplied as found function's callback param