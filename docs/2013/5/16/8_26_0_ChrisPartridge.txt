Stumbler: if it was me, i'd probably do fs.stat with setInterval, if the size doesn't change between 2 intervals, cancel the interval and process it. Not sure how efficient it would be, but probably better than an arbitrary timeout