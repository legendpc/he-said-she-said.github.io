so what I'm doing is from the mini-apps main logic, I instantiate a component by instantiating the object (using prototype functions), I pass the object a div and it loads its html into that div, then it loads its own json and builds itself with that json file