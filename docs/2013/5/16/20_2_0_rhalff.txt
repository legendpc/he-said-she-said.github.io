JosephFerano, ok, so your problem is the application will interact with the web, so it must be run by a webserver, not sure if nodejs is the best option then, because you will have to install it on each and every machine.