if the php script is returning a value, and closing the connection (as per the http get standard) then the timeout won't have fired.  the only reason it would fire is if the php script didn't return an end of data /n/n on it's return in the 5 seconds you've given it