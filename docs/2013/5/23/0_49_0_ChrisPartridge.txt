_d4rklit3: you could also git clone it, edit package.json, change the package reference to the full github tarball URI, and run npm install - it's just the relative github path on the depedency which breaks it on windows