Trying to get on / removeListener to play nice. If I do an "on" I can see it adds a function to _events[key], When I do a removeListener _events[key] gives undefined. But when I run on again I get two functions in _events[key]. Is this expected? Can I somehow avoid it?