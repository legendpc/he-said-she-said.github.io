Hi, I've just installed Nodejs on Ubuntu via `apt-get` (so installed as `nodejs`). And I can't get a symlink `node` working straight up the command line, I have to call directly `/usr/bin/node` to make it run (and of course this folder is in the PATH). Anyone have any idea??