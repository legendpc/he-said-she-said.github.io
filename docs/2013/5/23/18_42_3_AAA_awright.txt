ferno: Then you have a while loop that switches between incoming I/O events. You open a file pointer, then you make a read operation on it, then the program continues. When the data from that read operation is available, you get a "read" event on the switch, and you process it