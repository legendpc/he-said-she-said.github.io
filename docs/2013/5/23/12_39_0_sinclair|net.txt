Havvy: so maybe, all that needs to happen is the entirety of that file needs to be encapsulated in a instantiatable class called V8 instance and have node instantiate one of these at boot, and provide an API to instantiate more?