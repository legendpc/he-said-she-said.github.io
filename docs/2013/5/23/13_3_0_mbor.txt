hi, I use express to serve my public/ dir. how do I serve a JS file outside of public? tried something like app.get('/myfile.js', function (req, res) { res.send(__dirname + '/../myfile.js'); }); but I get "forbidden"