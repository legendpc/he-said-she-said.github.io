ningu: trying to get to the bottom of a case where forEachLimit (concurrency 1) works perfectly, but if I bump it up to 10, my app just exists after some number of iterations, without any of my callbacks firing