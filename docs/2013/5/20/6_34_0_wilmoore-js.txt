w0otness: a simple way to think about it is to realize that when you `require('something')`, you put it into a variable which is "local" to the module. The only way to see that reference in yet another module is to export it.