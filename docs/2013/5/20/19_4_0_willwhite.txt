my `npm info` output doesn't shift like the website but we are occasionally seeing some suspicious messages during `npm install` where npm gets a 404 for the backbone-couch tarball. I was thinking the tarball is just missing from one/some of the servers when I noticed this.