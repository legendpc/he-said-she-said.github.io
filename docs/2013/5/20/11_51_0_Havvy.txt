Given a suitable event system, you could have an object that adds a workflow by adding the necessary event listeners, and give it a tearDown method that then removes the event listeners when the workflow is no longer needed.