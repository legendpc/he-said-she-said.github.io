If I remove that fs.readFule line directly after exec("bash...) everything works pristine. However with that, even in it's own callback, it's trying to read that file first. Never actually executes my mkdir.sh script.