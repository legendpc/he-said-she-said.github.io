HTTP protocol:  If the first communication a clinet sends a server is an HTTP POST, the server (node + connect/express with cookies) creates a session, but does not send the session ID a a cookie to the client - is this a bug, or per http protocols?