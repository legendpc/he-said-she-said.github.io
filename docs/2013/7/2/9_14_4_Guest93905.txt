hallas: so im using a tutorial but im a bit confused. my understanding is that  on a get request the call to get projects is initiated - this is a db query which returns the json and the anon function is to render the page with the json data