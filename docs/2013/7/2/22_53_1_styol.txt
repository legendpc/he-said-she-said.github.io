sent_hil_: welp, the first issue is that your `host` key has a protocol in it (https://) when it should just be "github.com", but the main problem is that you need to use the https module as opposed to the http module to perform that secure request