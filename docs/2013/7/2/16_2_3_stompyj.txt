Question about npm.  If you have a dependency that is pointing to a npm based repo.  Then you switch that dependency to a git repo.  How do you get npm to reload from the git repo, instead of the old npm repo.  It looks like npm install doesn't check