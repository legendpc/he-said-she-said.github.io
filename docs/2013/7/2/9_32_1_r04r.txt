Alright, so regex has characters which have a special meaning. The ? is one of them, and so is . and *. If youw ant to use those characters instead of their special meaning you escape them, which is adding a backslash in front of it