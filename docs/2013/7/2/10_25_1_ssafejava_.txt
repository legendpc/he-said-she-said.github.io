adek: you're going to want to do a loop where you pop() or unshift() a worker ID off an array of worker IDs. pop the first one, send that worker a message & a callback. on the callback, call the whole thing again, popping another ID off the array, until the array is empty