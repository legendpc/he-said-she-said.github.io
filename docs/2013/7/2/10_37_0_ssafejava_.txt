Yep; just keep in mind, string.replace() returns the modified string, but doesn't modify the inputted string. So a line like `var a = "1234"; a.replace("1", "4");` Will do nothing. That one's bitten me a few times