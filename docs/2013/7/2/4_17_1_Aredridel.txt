Just remember that regular expressions are actually a short set of instructions on matching. They read from left to right. Yours was "Start storing into match group 0. Match 'NEWDIR="'. Start matching into group 1 too. Match as much as you can. Then stop matching into 1, and match '"'."