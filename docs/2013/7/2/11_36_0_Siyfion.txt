Say I have a promise in node that checks a message queue, if it finds a message it returns another promise that processes the message, if it doesn't find a message, how can I "escape the chain" of promises? I would normally throw an error, but it's not really an error per-se...?