RalieghFhang so an regular expression is essentially setting up a parser, which strings are sent through, besides match on actual text, you can match on text behind your text or infront of (a lookback and a lookahead)