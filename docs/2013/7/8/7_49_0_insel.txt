how can i interact with with node runing in a terminal window from an other terminal is there something like echo "interact" > /dev/tty1? or a way to push data into the process/queue? or have i to create a interface like telnet or http?