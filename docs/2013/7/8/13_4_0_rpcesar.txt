hitting about 700 MB in a couple minutes and steadily rising (nothing being created in the loop, just a few logs, things created prior though). but the metrics im seeing in the profiler appear too low (if bits) and too high (if bytes)