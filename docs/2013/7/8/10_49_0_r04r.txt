shafox: if you want to use npm to install something globally (-g) use sudo. If it wants sudo when not installing globally you need to fix your permissions. Your directories/folders are likely owned by the wrong user.