i encounter a strange problem. I have an app.js file and use stylus. i took me the whole night to boil it down to two lines. i have one router and a corresponding template directory and another router with a corresponding template directory