and then it translates that to a user, then how it adds it to the request, I don't know, but if you can access user's attributes, your best bet is to try to manipulate the database or see if passport offers some functions of it's own to manipulate the user.