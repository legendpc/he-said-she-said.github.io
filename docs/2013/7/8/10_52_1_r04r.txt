shafox: You are not the only user to the system. Most interesting in this case is the root user. You mentioned starting a project by doing sudo npm install. That runs npm install as root in that directory, creating node_modules as owned by root and then you are in the situation you are now