oren: the design part is based on setting things up so all your user code is being evaluated within the context of a domain, either implicitly through run() or explicitly via binding (which is what intercept) is doing under the hood