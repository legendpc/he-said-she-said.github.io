Multiple returns seems a little confusing to me. var a, b, c = foo(), bar(); // is that a, b contains returns from foo() and c contains returns from bar()? Is it c = foo() and bar() assigns to nothing?