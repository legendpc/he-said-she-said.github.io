say you have 100,000 concurrent users. For each user you query a million records. Assuming caching isn't implemented, the 100,000th user will see "Hello World" immediately, but will get the contents of the query after maybe 20 minutes.