owen1: you want only one domain error handler, and when you're doing d.on("error", next), the presumption is that you're using the default Express / Connect error handler (added to the end of the middleware chain)