generic convention question.  I'm using the "#" prefix in my mocha tests when writing my unit tests:   it("#get_update should update the feed data") for the instance method, "get_update"... What's the convention for Class methods?