liamkeily: what you're describing could be prototype-based. depends if you do something like User.prototype = { coolMethod1: function(){}, coolMethod2: function() } or if you're adding methods manually within your constructor (which would be bad.)