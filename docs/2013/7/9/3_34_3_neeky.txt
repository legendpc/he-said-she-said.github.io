but on a page say http://myapp/user, I want to define the layout with a boostrap template, and define the model and backbone stuff in 'views/user.js'n and combine them into an html file that the clinet renders