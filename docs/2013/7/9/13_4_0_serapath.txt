in my router i do: app.get('/account/', function(req,res,next){ console.log(app.redis);next();}, require(path+'/account/index').init(app.redis));   and it logs to the console: { info: [Function] }, that means it does what its supposed to do