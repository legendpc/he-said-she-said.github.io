using socketio, when several clients try to connect at once, for each one it looks like the handshake authorizes then the transport ends quickly then the connection reestablishes itself. after the reconnects, the connections are stable (don't disconnect). what could be causing this?