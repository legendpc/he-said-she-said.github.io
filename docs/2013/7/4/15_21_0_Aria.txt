I suspect what you REALLY want to do (and this is ugly) is to download the script, analyze for dependencies (or better yet, make it supply its own package.json, so you get versions needed), run npm install, and then run the script.