daslicht, that sounds a bit odd. A Node router is for deciding on a response to a request path. Why would a client need to respond in that way? Are you trying to implement a client side ajax navigation model?