i have a package A that dev-depends on package B which depends on package A, but package B won't install the last stable version of package A from NPM because i guess NPM thinks it's a circular dependency