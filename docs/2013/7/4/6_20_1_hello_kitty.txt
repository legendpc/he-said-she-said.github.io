so lets say i nest db queries 10 deep in respones to /api/get-user -- and then i hit this api 1000x really quickly, and then freeze time and take a look at whats happening.... many requests will be at varying depths through my nested db callbacks right?