socket.write('i write something to the socket', function() { console.log('write done'); });   -- > can i break this 'i write something to the socket' line into two so i can put another portion of the command to the next row?