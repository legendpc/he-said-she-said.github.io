aandy, you can even combine errors. I found this comes in handy with things like parsers. So if you have: `choice(a, b)`, and neither a nor b matches, you can catch both errors, and return a list of errors instead of a single one.