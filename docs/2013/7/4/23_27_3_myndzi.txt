i'm returning a (Q) promise from a function, but it seems to stay pending; if i tack a '.then' to the end of the return statement, it executes, but if i tack a '.then' to the end of the calling statement, it fails to execute