neeky, here's the intended load sequence: 1.) feed_test.js loads mongoose and the Feed model (Feed = require('../app/models/feed')), then I establish the connection to mongoose. Maybe that's the problem.