Could someone tell me a bit about what node will do with a bunch of database-related nested callbacks if my API gets a lot of traffic? Anything that waits for a database probably has a next() in its implementation somewhere, right?