i am having some problems with sending some data via telnet to some client: every command that has a minus sign (-) in it, doesn't work on the box (in example, 'reboot' works, 'rm -rf /flash/' doesn't). any ideas why? i'm using it like this: socket.write('rm -rf /flash/') ... 