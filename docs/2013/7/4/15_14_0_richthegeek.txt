Aria: at the moment, at least, the module gets installed into the location of the script that invoked the daemon (testing script...) and so when the daemon then tries to load the file it doesnt look in the right place