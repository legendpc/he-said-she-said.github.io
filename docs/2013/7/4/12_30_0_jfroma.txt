davidbanham: no. Let's say I've fs.createReadableStream(100mbfile). This stream will emit,emit,emit data and end when it has emited the 100mb. What I want to do is to convert this stream into a 10mb stream, when it ends I want another stream that emits the following 10mb and so on