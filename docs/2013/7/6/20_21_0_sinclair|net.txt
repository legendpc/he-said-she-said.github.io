AAA_awright: so, lets assume you have an express instance, and you want to write an restful api, appex would be a good candidate for that as it can produce meaningful metadata about the endpoints without the developer doing more than writing functions