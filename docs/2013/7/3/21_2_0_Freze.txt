i.e. for light tasks I guess the node.js way is efficient for servers that don't have a lot of horsepower. But why not schedule like an OS in the first place? instead of having the callbacks execute on the main thread were the block