can someone suggest how to select the redis database number synchronously? it doesn't seem to be appropriate for the whole of main to be a callback for one configuration property, and the redis library for node doesn't seem to have a synchronous .select() method