wyled: i don't know either, but therein lies the problem. the 'official' google recommendation, last i read it, was use a headless browser to visit the page and render an html snapshot, which works but it's pretty bloody stupid to expose that kind of backend work to a public url