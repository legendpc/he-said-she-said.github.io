so, i dig through typescript AST, and start looking for these types, so in that instance i mentioned yesterday (i wanted to initialize service types), i look for "all" types that extend Service, and initialize them