sinclair|net: the forked child processes have a communication channel built-in, within the parent you can just: child.send({ some: 'message' }) and in the child you can do: process.send({ some: 'answer' })