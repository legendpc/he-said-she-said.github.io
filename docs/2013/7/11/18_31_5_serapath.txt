ok, so if es6 will come, and specifies tail recursion (actually doable right now, but as u point out, not optimized so i better not use it), ...and with promises... i still dont get why i would need "generators" on a language level