serapath: if you call index.js a couple of times in my example you'll see the problem of relying on settimeout. sometimes it logs "timeout world", sometimes "timeout universe". but the callback is always "callback universe"