Sean3z: If it's because the user has to wait for a particular thing to finish before doing anything else, I'd still do it async and then let the frontend know when the call is done, via a websocket or similar.