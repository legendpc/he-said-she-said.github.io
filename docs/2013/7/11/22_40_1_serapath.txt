so if i want to execute 3 functions which are totally unrelated, which means, they do very different tasks and they could be run in parallel and i need all of their 'return values' (dont care whether callbacks or real returns), how would i do that?