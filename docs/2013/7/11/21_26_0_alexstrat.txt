Hi someone, I would like help on writable stream: I got an underlying socket (in a C++ addon) that has a method ".write" which returns false if it can't handle more data. How do I wrap this in a writable stram ? How to implement "Stream#_write" method given the returned value by "write" ?