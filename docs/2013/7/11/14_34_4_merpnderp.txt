With all the buzz about javascript and memory management, can't that be mitigated by just being more careful about memory allocation? If you have a tight loop creating lots of objects, just reuse the minimal amount necessary?