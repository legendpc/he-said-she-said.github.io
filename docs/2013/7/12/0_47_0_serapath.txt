but even without the options, it would be fine to just have the dependency thing, because i can still make an if statement in the functions that are optional and check whether something has to be done or not