i now have read a bit about async and similar libs and promises and some other kind of approaches. they all do lots of stuff, but the only thing i'm curently searching for is a library, that gives me a way of specifying dependencies between them