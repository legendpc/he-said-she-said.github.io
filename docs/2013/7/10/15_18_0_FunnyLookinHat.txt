Alright - so I can't seem to get Sequelize to return a findAll() with a specified order... I'm trying MyModel.findAll( { order: 'id ASC' }) - and it seems to ( at random ) not actually adhere to it, making me think it's not working at all