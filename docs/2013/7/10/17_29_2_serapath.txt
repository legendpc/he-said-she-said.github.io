hi, i have a general expressjs question. Some modules, like passportjs populate the request object with methods like isAuthenticated(), login(), logout() and the like. In order to have access to certain services inside of handlers