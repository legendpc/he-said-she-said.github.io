With node.js they took the existing language Javascript, the existing interpreter from Google's V8 engine, and put a lot of work into making it work with IO (network, disk, etc) to make it... useful outside the browser