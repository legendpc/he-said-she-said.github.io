when an error is thrown, the offending line is shown.  it appears that the mechanism assumes that the tab character is equivalent to two spaces, which makes rendering with 8 characters very strange.  Is there a way to control that (or just to force the thing to print the entire line)?