ChrisPartridge, I did sudo npm install -g yo grunt-cli bower && sudo npm install -g generator-angular and then what you see here: http://pastie.org/8126906. There were no errors in the earlier steps just warnings about bad package configs.