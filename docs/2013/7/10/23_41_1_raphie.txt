I know it's probably bad practice, but is there a way to require a script so that its defined variables become global? I'd like to reuse a browser-oriented script without modifying it to include lots of module.exports