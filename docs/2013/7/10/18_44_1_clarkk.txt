bewest: thanks for your suggestions. I am really looking for an API that will support any mongodb query, regardless of the collections that exist. Permissions are implemented on the server-side, to prevent unauthorised access to specific collections