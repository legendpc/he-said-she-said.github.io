gabrielstuff: but your app.configure() is redundant in that sense, because all that code is going to run for all environments, yet you're manually saying "when env != test, include the logger", it should really be app.configure('production', function() { //add logger });