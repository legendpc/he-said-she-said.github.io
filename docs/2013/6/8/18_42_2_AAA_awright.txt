ckknight: That makes you dependent on a single template library, which I would call far from streamlined. Maybe some people can, but I can't assume that my template will be available as a file on the filesystem, or that the templates will even be the same format