I'm just getting into node and I've been looking into using expressjs along with mongodb, but it appears that the mongo lib's MongoClient.connect is async, and requires a callback, so how would I use that and store it so that the expressjs app server can use it?