If I ./configure --shared-zlib --shared-zlib-libpath=/opt/some-node-app/embedded/lib && make && ldd out/Release/node, ldd shows that zlib is linked to /lib/libz.so.1 instead of my embedded copy. Could anybody please help me figure out why?