As I can see, all they tell me is how to log in (synchronously) through a website or native app, not asyncronously (every x hours): I tried the registry, but couldnt find anything but simple wrappers that requires a user to actively log in each time