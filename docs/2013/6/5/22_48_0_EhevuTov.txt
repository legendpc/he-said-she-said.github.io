nlloyds, have you tried '-search_paths_first' ? I know that node generally comes with it's libraries it needs locally, compiles and links them during node compilation, so worst case scenario, you could just read the node src Makefiles