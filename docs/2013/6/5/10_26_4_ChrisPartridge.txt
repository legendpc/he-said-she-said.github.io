Trying to find a way to return a buffer in the node-chimera c++ addon, however it uses a QPrinter (http://qt-project.org/doc/qt-4.8/qprinter.html) which only allows setOutputFileName - can anyone see a way around this?