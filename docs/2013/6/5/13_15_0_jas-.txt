spectre: It looks like the NPM source makes use of the 'graceful-fs' module (https://github.com/isaacs/npm/blob/master/node_modules/graceful-fs/graceful-fs.js Line #207) which seems to have a fix applied regarding fails on 'chown'. What version of NPM are you usingA?