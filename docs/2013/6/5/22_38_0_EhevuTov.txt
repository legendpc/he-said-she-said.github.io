nlloyds, sounds like the linker is using your system zlib over your local zlib. Even if you have it setup to use an additional directory for the linker libs, you still probably have to find a way to explicitly tell it to use which one you want.