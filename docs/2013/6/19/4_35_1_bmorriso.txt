well, what happens is...I navigate to url, it'll load "pieces" from each node in the backend, rather than load everything from a single server. Setting it to source, everything loads from a single server