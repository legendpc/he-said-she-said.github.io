oh and in, for example, perl, I can do something that's pretty cool. here is how it goes. sub somefunc { <code> }. then I can pass it by reference somewhere else by using my $var = \&somefunc, and execute it further on with &$var.