...oAuth provider on subsequent re-authorizations when the client sends the cookie id that corresponds to the session id on the server at which point (if implemented correctly) should force the oAuth provider to re-authorize the current token (expiration, valididty etc)