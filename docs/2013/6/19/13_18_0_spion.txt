I still don't understand how can I implement authorization on top of that e.g. client C1 with a signed certificate can access resource A, but not B, while client C2 (with a different client certificate) can access B, but not A.