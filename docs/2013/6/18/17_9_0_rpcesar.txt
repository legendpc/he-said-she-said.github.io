hello, im having problems with a weird crash, in windows, from within fs namespace, probably (not not positively) related to using the "unstable" readline createInterface api. Here is a pastebin of the complete (short) script, which is just meant to emulate the linux "split" command on windows