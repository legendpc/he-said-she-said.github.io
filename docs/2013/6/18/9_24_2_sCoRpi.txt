In a case like this: You doN't expose files directly but a user needs to provides some kind of key which then gets transformed into the actualy file, which then is served up - to prevent direct linkage