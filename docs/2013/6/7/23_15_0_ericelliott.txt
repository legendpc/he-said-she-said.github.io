Purefan, othipm23: Pretty sure I've heard that doing the require from different relative paths can cause a cache miss, which will cause the config to be executed again, possibly returning different values to different modules.