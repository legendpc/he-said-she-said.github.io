so ever since upgrading to node v0.10.X, previously working code to pipe between processes is no loger working. it just hangs after ~900mb (this is video). the suggested fixes (on 'data, and .resume()) do not work...