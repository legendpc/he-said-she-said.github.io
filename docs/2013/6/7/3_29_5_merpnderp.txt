jesusabdullah: that'd be neat to run node.js with IIS. But i really can't think of a problem node would be a solution for us. We write web apps for internal customers. And we've got hundreds of apps and they're nearly all vb webforms.