If I run it all on the same server, I could only accept requests from 127.0.0.1 right? If it were a different server, would I be right in thinking it's not safe to only accept requests from *frontend server IP*, since that could be spoofed?