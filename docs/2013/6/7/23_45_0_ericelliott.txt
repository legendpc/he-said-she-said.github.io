sdwrage: In some cases I've seen 10x improvements over rails and PHP after moving the codebase over to Node -- specifically because the evented model allows you to use server resources more efficiently, rather than waste them in wait states.