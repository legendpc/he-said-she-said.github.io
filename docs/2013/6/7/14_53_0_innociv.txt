How do I match only the foo without "s" at the end? IE "I foos all the foo" with regex?  I only want to match foos that aren't plural. But fooed is fine, and so is foobar, or whatever else.  I'd expect something like /^(foo!s)+$/ but that's not 