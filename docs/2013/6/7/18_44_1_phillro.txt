Mongoose.js question: From within a plugin I'm calling: mongoose.models['model_name'].find(cb); As I need to query a different collection. When I do this, the function executes but the callback is never triggered. It appears there is an issue with the promise. Anyone know/think of a work around?