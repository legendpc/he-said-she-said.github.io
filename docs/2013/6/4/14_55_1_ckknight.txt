sinclair|net: yeah, it just runs through the regular GorillaScript compiler, which has sourcemap support already. One thing I haven't published yet (about to) is a packager that turns a directory tree into a single .js file, that'll have sourcemap support pointing to all the source files. Quite nice