samholmes jadeify looks pretty outdated — it fails on install if you don't have 'less' module preinstalled and it's own example fails when using browserify().use() method, so I can not slap some fast benchmark right now, sorry.