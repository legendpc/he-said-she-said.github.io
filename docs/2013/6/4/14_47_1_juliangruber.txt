werle: the only thing i know about constructors in javascript mainly is that you give them a CapitalName and for not having to do new Foo() but rather Foo() you have to do that if (!(this instanceof Foo)) return new Foo(); stuff