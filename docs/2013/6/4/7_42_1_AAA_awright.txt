What part of this needs an installer at all, then? You should be able to load settings from a configuration file, or similar, or maybe a single form element that writes it if you really want the HTML frontend, what are you anticipating is going to be complex?