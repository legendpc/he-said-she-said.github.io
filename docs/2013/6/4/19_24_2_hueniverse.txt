jesusabdullah: hapi.pack is not clustering. It is about treating multiple http server instances as a single unit. For example offering the same API endpoints over http and https. it abstracts the server configuration which is than passed on to plugins.