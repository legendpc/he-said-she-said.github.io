near on the roadmap is rewriting the trampolines so that a shared impl can be used across all the monads; seems like that should be possible given how alike they are, if you look at Identity, StateT and ContT