yes thats the responsibility of each and every single module. a module has one entry point (usually index.js, otherwise its specified in the package.json) and it decides what it wants to expose to the use of the module