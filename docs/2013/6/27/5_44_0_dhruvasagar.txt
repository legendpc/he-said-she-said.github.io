I see that I can use passport.authenticate as a middleware, but I still don't know how to get a handle on the user object I created within the passport callback, is it passed to / attached to the req for the end point !?