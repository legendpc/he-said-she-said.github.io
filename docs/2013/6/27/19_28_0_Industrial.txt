So I have this Server prototype function that acts as a named and versioned (seaport) dnode interface and may specify dependency Server's/seaport servers so they become callable directly from the implementation.