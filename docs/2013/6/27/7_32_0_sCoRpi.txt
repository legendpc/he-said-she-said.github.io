I execute a find(), but of course the artist isn't inserted yet, so the script inserts a new document. Am I doing something completely wrong? Or is the simplest solution to actually process this type of scanning/indexing just synchronously=