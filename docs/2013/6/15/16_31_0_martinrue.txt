does npm fallback to some other way of contacting the registry? I'm trying to emulate it failing by changing 'registry.npmjs.org' in /etc/hosts to point to some fake IP, and it still seems to find the registry and install dependencies