pretty weird, if i try to do an npm install on a github tarball url it fails "fetch failed" but if i download the tarball myself and do an npm on it locally it succeeds including downloading its dependencies