now it says for the config that is ok. and when I type service nginx start or /etc/init.d/nginx start it will have no output, no logs in /var/log or /usr/local/nginx/logs or any mention of nginx in any other log, and no running process of nginx will appear