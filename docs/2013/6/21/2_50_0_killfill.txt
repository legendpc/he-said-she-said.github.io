tjfontaine: so how does one then calculate the latency of http-client requests?.. was trying to do something like hash[args[1]->fd] = timestamp in the request and use it on the response with timestamp - hash[args[0]->fd]