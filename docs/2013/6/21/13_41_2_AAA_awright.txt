Usually if you have a library like that you have generatePassword(password, iterations) and comparePassword(password, hashed), the former of which produces a string that includes the salt and iteration count