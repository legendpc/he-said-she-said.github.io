I could easily do something like var MyView = ExtendedBackboneView.extend({ initialize: function(){ Backbone.View.initialize.apply(this, arguments); // code... });, but if I ever change what `ExtendedBackboneView` extends (say, I put another superclass in there), I have to change all the code