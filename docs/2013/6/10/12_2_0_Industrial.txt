Anyone use mocha? Say I want an instacne var available for all tests below a certain level (prototype constructor function test, so all moethod tests need this instance to operate on). How do i actually pass the instance to each test?