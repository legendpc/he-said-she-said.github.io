bmatusiak_: That defeats the purpose of defending against session replay & session fixation attacks... each authenticated page should be changing the session id while preserving any existing session data