But if I were doing something else, like proxying a request, I would do something like function(req,res,next){ http.get(options, function(response){res.status(response.statusCode).set(response.headers); response.pipe(res);}); }