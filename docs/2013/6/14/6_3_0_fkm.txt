Hi there. What method would you recommend, to run a node instance as a service? I've found "forever" and haven't had any trouble so far. But I found it somewhat weird that this basic functionality isn't included in node. 