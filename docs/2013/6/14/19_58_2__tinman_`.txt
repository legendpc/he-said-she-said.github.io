ikurei -- a simple way is to restructure your http agent calls so that on next you add the next url to be called, in this way you can keep track of which runs are currently in the queue: "Each time an instance of http-agent raises the 'next' event the agent is passed back as a parameter. That allows us to change the control flow of pages each time a page is visited. The agent is also passed back to other important events such as 'stop'