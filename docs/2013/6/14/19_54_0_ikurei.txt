i'm using http agent and jsdom to scrape a web, capturing the "next" event to iterate through several urls... pretty standard. but each time the callback is called, i don't know which of the urls it's called for