kristian-puccio: Re: my earlier comment about using the logging as middleware, you won't be able to get the function name that way (unfortunately, `next.name` will return 'next'), but you could log the route, params, etc. so you can figure it out pretty easily by looking at the logs