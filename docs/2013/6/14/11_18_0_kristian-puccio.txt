hrm could do that but I don't want to have to add it to each place, rather have it happen automagically. We currently log the function name at the start of each function (we mannually add a log statement) so I'd love to be able to get that for free