owen1: Depends on what you're doing. If you need to read the response body & do something with it, that's your only option (and you can throw it into some middleware to make it easier). Otherwise, the node way would be to pipe the stream 