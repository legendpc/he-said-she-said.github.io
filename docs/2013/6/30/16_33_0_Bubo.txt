for example: you want to query 10 million rows from a database and iterate through all of them. The querying of the data from the database can be done asynchronously, but iterating over it (if done synchronosly) will block it