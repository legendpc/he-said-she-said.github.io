I'm trying out something akin to reference counting. I'm adding to a number whenver i'm creating an async query, and subtracting whenever callback is finished. Closing the dababase on 0 refcount would be wrong though, because the first query could finish before the next one is steup