the callback should be triggered by an event. So if you say "select * from sometable" and then callback, if it takes the database a minute to respond you could be doing a ton of other stuff in that time. 