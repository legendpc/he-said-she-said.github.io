sinclair|net: basically, you have to understand that what node is doing is using libuv to manage a bunch of threads and os-level events behind-the-scenes, and hook that all into a v8 thread. Imagine trying to make all that run cleanly in its own thread.