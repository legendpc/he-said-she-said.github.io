bnoordhuis: also, instead of having a module load another module which loads another module, etc, all modules should be compiled into a single output, such that the entire thing can have policies applied to it