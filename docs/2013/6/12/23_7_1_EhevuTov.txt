for instance, I use node tap to test a library I made, but that's just a simple one program. In this client/server test rather, I need to start the server, start the client, run the test and then kill the server and client