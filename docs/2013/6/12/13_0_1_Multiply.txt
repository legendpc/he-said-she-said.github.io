budz, I'm doing proxying, if the end server doesn't respond, it'll tell the user, and try to start the end point. When it starts, it'll write a simple line to make the browser refresh, and then end the request aswell.