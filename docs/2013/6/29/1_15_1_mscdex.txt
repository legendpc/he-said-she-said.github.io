The_Fly: no, it's just "paused" at that point, but you probably should unpipe first. i am not so sure you can just `res.writeHead(nnn); res.end();` in that kind of state, you might have to just req.socket.destroy();