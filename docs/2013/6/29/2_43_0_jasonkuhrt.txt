msdex: having this all out in the open is getting quite verbose, and instead what I am going to do is create a Duplex that accepts a socket and does the protocol _read and protocol _write transformations within there