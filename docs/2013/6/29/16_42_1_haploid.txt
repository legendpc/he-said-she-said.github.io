my node.js script never exists.  my understanding is this is because the runtime thinks there is a chance a callback will return at some point, but I have no way of determining which callback it exiects to return.  is there any way of debugging this?