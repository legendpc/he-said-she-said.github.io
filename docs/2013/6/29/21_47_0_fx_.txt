Oh, btw, whats the correct way to ignore an arg in a callback, like, an each loop, if you want to ignore the key, do you still say function(key, value) or do you do, function(_, value) or something like that?