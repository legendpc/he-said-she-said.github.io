is there a way to convince npm to install a package as its own subdependency? basically, i have a project that uses itself to compile, and it would be nice to have it use the last stable version (as fetched from npm)