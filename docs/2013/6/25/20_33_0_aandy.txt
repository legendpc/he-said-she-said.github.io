you can set headers to tell what the "real" ip is, if that's what you mean, yes. it's actually built into some node frameworks like connect/express, where it's just a boolean to trust those headers (set by proxies, e.g. nginx, haproxy)