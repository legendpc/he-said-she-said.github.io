jas-: ah, sorry, i meant the tty in the remote box. the ssh cmd itself is no problem (public key). there are variations i can get to "work", e.g. (sleep 1 && echo "pass" && sleep 1 && echo "pass" ) | ssh ... sudo passwd <user>