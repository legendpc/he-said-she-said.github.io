like, I'd think it'd be better to pass a memory location and run a C++ lib on that memory struct, rather than actually passing the values which would have to be checked. I'm just assuming in ignorance, but that's the kind of things I'd like to know to be an issue or not