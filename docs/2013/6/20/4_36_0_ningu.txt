substack: standard formats are still useful though, aren't they? I like to use JSON vs. ad hoc formats when useful, and on top of that, not to use totally random JSON structures if there's any standard practice.