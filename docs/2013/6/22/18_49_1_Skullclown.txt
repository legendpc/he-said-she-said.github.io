isaacbw: the issue with the async part was that I, for example, couldn't simply get all data, put it in a global variable, pass it on. (or, well, maybe not a global variable but one in the scope above it)