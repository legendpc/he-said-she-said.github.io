When writing unit tests. How would test that a variable has require('component') correctly loaded. Is that kind of the stuff you test or just purely test the results of functions?  Cant really imagine that you can mess up ( things like require('express') )