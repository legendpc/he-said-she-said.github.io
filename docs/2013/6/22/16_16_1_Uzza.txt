yes, but why ? I'm clearing cache for npm "npm cache clean" but still some dependencies can not be fetched. I tried it for yeoman, grunt-cli etc, nothing works. I've could install them some time ago on different maschine.