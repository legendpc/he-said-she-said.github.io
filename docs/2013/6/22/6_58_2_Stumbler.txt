what I did is created a server, and just put all data into a string.  every data event, I check for my end of object terminator, and split the string buffer.  calling a new updateObject event, which is parsed elsewhere.