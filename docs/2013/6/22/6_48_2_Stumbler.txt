The only problem with Json over a standard tcp net socket is buffering.  You'll sometimes receive half of a json object in one data event, the other half on the next event, possibly with the first part of a second, depending on how you buffer it