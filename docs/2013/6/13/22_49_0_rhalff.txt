I'm creating this import script for mysql to mongodb and using .insert() with multiple rows at once, but now I'm wondering if mongodb already supports 'streaming' json into the server, without me having to worry to insert to many rows at once.