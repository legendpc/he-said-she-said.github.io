How do I switch to being a production server rather than development? And if I switch the port to port 80, how should I set that up, because I guess running as root is unwise, and www-data user doesn't seem to be able to which I thought it would...