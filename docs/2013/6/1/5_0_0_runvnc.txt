well if anyone is using the aws node sdk or another module that uses https, trying hacking maxSockets on whatever agent they are using to be a big number instead of like 5 or something.  makes a huge performance difference