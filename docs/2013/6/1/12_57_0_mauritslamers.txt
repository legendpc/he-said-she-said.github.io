deoxxa: so, instead of just doing npm install canvas after compiling the necessary libraries, I have to clone the repo and do all kinds of editing, instead of simply copying the resulting binary out :(