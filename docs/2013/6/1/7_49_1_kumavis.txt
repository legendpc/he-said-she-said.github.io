at this time calling a require() in browserify resolves the name via 'browser-resolve' which prepares a pkg-filter from the "browser" overrides in package.json and passes that on to 'resolve' who then ignores the pkg-filter in the case of native modules