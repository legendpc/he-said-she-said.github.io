and I've never seen a customer happy with a default wordpress plugin. They always want it customized. And it's just easier to write your own module than to reverse engineer the plugin. Plus I know that the input is sanitized and crfs protected.