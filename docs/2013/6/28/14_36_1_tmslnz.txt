We were using Symphony CMS for data backend. The thing uses XSLT for view rendering. Man *that* is unmaintanable. There's like 1 dude only on SO who is able to answer Qs of moderate complexity on XSLT.