hi i'm using node-mongodb-native and when i do a db.collection('users').findOne(function(err, res){});… when the connection to mongodb is down this function just hangs and never calls the callback. how do i get it to just report the error to the callback right away or after 1 second timeout?