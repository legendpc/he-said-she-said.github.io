package.json "install" script question: the npmjs.org docs say it's an antipattern but what if i had a node-dependent app that had a npm install followed by bower install? would it be weird to have the install script do bower install?