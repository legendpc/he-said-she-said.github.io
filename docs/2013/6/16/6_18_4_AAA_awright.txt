olalonde: It's about as a generic ORM as you can get though, it works on the principle you have resources, and they map to the database, which from there gets formatted into JSON or HTML or whatever you like