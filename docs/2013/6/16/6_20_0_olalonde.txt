ejexin: with my ORM, you only list attributes. then for each adapter you can optionally map attributes to a database column/type. but so far, it's only assuming that your JS attributes map perfectly to your db columns