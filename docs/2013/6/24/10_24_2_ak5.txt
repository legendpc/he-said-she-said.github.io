RLa: so I have an object I am giving a search method, that is async. It scrapes some webpage and returns the results in an array in a callback that sets properties on the object with the search method <-- does that make sense?