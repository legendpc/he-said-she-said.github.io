sinclair|net: But there is another issue, with serving web pages and webgl content with same application, scaling, for example a GPU would be a much better tool for webgl processing but of course a CPU works better for text processing.