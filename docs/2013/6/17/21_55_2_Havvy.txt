There's the actual socket which gets listened to by an emitter that turns it into string messages which gets listened to and turned into message objects which modules can listen to but is also listened to by the command parser which emits commands if it detecgts them.