id prefer a file too, but I wasnt sure how it would be handled when concurrent clients try to write to it (I know node is single threaded but IO is async so I assumed this would mean more than one can access it at once)