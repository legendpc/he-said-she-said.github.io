And for making the data accessiable to everything(? perhaps a bit limited.) I would use a proxy data source, which caches all the requests and on new requests it passes the new, if any,  and all cached data.