Boggled. Trying to authenticate in Express app. When I call my auth(uid, pwd, log, cb) method, something causes headers to be written… but the authenticator has no references to the response, so can't write headers. WTF?