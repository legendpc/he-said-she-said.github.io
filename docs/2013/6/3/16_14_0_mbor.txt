hi! what's the best way to execute git commands synchronously? I need to switch branch and do some work on it. tried exec-sync but it gives me: Fatal error: Switched to a new branch 'deploy'. seems like it doesn't handle branch switches well