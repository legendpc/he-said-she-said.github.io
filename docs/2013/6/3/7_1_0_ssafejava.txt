olalonde: It lets you set really aggressive caching policies that don't even check for changes to a file (so you don't even get a roundtrip to check ETag, etc.), and then when you update the file, the filename has changed so the browser knows to re-request it