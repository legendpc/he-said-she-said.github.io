aandy: Its fairly straight forward. The database connection works, the cookie (parsing) requirements for express.sessions is present but the custom middleware I am writing always fails when setting the cookie after the .set method is called