Havvy: I guess technically it's RESTful but api.php isn't what I would call "strictly" RESTful because it doesn't reduce the actions down to nomial form, everything is done with POST (iirc) even when you could more semantically meaningful methods like GET or PUT.