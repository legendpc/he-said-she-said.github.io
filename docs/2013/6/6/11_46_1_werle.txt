if you were to define it on the `exports` object then you would need to require that module and possibly enumerate through each property on the exports object. for (var method in object) { if ('function' === typeof) object[method]schema.method(method, object[method]) }