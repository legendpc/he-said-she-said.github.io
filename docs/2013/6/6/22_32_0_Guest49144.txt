I'm pretty sure my node host is running my server on two different machines with two different times, which means that Date.now() is inconsistent. Is there a good way to get a reliable timestamp in node?