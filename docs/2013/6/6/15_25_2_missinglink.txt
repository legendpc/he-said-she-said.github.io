randomlurker. I see, then yes pachet is correct. var foo = require('foo'); Object.keys(foo). The only object added to your scope is 'foo' nothing is added to the global namespace and object.keys will give you any properties of foo