hrm... those all knowledgeable in http, I am writing an API that allows me to acquire a resource on a network, in the event that the API can't fulfill the request, what would be the proper status code?