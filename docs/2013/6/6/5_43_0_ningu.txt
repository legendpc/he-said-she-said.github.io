so... ruby webapps don't support concurrency out of the box, and must be run under passenger in order to serve real world traffic. and there is no nginx+passenger package in ubuntu, so you have to compile nginx with passenger support built in. what a pain.