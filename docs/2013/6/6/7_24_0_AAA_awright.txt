Havvy: REST tends to work best with URIs like </class/id>, where class is a namespace and id is a number or some similar long-lived identifier. Though again, that's not a requirement, REST only says they're opaque.