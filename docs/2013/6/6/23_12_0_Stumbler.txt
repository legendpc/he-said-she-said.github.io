I'm sure there's a better way than what I do, but since no one else is responding…  console.log( object );   often shows the output of the object, but in cases where it doesn't, I often write a for loop   for( var attribute in object)    and console.log(attribute)