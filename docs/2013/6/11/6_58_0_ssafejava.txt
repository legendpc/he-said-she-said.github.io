scathen^C: The easiest way to do this (imo) is just to set up a route for a .js file, e.g. 'bootstrap.js' where, when called, returns JSON with all the data you would normally request upon loading the app