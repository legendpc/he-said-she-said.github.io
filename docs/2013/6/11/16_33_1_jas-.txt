olalonde: technically without "nohup" the commands simply ran one after the other... the use of nohup forced the first curl request into the background logging to the nohup.out file while the second request ran in the foreground logging to the "log" file