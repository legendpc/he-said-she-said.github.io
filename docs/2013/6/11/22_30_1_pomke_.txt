The big advantage is, all of the reendering load is distributed to your users machines, so we can run with minimal inf. We have 3 API servers behind an ELB, and a moderately sized mongo cluster, for a fairly sizable number of users.