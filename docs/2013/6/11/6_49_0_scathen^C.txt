hey all. I am setting up a rest api, and I want to use a url like this /api/users/<id>/objects <- a post to this address would create an object for user(id). My question is, does this mean I need to pass the <id> to the web page, so that the page can build the correct <a> link?