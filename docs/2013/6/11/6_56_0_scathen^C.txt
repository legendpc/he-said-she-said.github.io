ssafejava: thanks. i have one idea. As it's a single page app, I could call some base api path like /api/ which will return the currently logged in user/app info. Then when the app loads after the user has logged in, it calls this url once, and uses that to setup the client model....sound ok>?