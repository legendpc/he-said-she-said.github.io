the quirk I do see often that is somewhat showstopping is when library writers define AMD compatibility inside their modules. that actually breaks code for me depending on how they did it. I usually strip out the AMD code & use requireJS's `shim` config