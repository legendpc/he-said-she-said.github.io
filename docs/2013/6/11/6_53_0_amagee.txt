Is there a library that does things like fs.readFileSync but that has nicer / cross-platform exceptions? Ie. I'm currently going to do something like:  try { var s = fs.readFileSync("..."); } catch(e) { if (e.code === "ENOENT") ... }, but that's not very pretty.