scathen^C: However I would suggest bootstrapping initial data into the main site template. This prevents a lot of awkward code where you need to make sure that data is actually loaded before proceeding. And it really speeds the page up by removing the extra round trip