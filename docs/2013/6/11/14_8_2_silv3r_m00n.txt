in a typical apache+php setup, there are lots of apache processes that consume huge memory, php on the other hand connects to database on every request, i think since node.js "keeps running" and it should offer considerable performance gain