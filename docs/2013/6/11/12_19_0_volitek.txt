Okay so I have two lines, the first is console logging something, the second is using that same something in a for loop, and it complains it's undefined despite logging it.       console.log(tables[game]);