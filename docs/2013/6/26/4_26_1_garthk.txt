In bin/server: config = require('config'), AppClass = require('../'), log = require('bunyan').createLogger(…); var app = new AppClass(log, config); app.start(function(err) { if (err) process.exit(3); });