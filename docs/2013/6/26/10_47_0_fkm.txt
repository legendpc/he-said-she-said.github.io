theCode: MySQL itself or node-mysql? I didn't know MySQL could change the output format and the only thing in node-mysql I could find was: "typeCast: Determines if column values should be converted to native JavaScript types." But having the original DateTime string doesn't help me either.