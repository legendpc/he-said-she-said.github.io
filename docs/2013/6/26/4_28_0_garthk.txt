adamvandenhoven: … then in lib/index.js, you want module.exports = function AppClass(log, config) { … this.app = express(); … } and then AppClass.prototype.start = function(callback) { … server.listen(port, address, function(err) { if (err) { log.fatal(err, "bugger!"); callback(err); } else { callback(null, server); } }