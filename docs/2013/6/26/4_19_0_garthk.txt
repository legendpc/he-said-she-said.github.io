adamvandenhoven: the biggest pain is getting the argument to t.plan() right. I tried sub-tests with smaller plans, but never managed to get it working. I might try substack's tape module to see if it helps.