adamvandenhoven: most of my Vagrant testing involves the full Node application, plus the Puppet stuff. I'm also not pushing to NPM, so I've got something running 'npm pack' and then re-linking the .tgz into puppet-modules/${module_name}/files/ so Puppet can pick it up and deploy it.