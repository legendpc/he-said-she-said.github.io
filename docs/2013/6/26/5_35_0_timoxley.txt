AAA_awright I guess I could do that. I'd need to build an interface around that, these things were originally designed to run as regular services, so at the moment they only have a command line interface