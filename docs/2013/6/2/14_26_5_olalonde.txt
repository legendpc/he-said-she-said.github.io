wathek: if you have the results, you just send them. otherwise, you can add a callback function to an array and your check script should make sure to call all callbacks in the array when its finished… something like that