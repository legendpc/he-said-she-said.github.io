r1card: i'd assume requireTrimmedWhitespace() would tell me i'm a terrible person if there was whitespace, and enforceTrimmedwhitespace() would just be passive-aggressive and trim the whitespace on its own