any of you use Q? .all() returns an array of resolved messages, since I don't really care if one or two fail I switched to allResolved(), but the later is returning an array of promises, is that a bug or by design?