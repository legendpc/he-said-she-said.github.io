aandy, How does that script actually work? It sets a node.js server up on port 9001, and then on incomin clients it also sets up a gstreamer-tcpserver up on port 9001. How can it set up 2 servers on the same port?