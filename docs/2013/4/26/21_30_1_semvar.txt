package.json semver question. Is there community wisdom as to best use cases whether to put the ">" next to your dependencies semvers or the default "~" which is placed there when doing a npm install -S <package> ?