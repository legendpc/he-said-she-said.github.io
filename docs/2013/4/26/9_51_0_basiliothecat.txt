SrPx: wouldn't dependency injection kind of way do it best here? e.g. you iether pass that 'world' as a parameter into constructor function or set it via methods/properties from the main driver program...