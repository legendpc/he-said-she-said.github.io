If I have a start.js file, which require's mymod.js. mymod provides fro example "MyMod()" object as the module.exports. Is it possible from the start.js to give MyMod a callback to be executed? Because every time I give it a callback, it's never called