@leonvv the node server is governing the process the FreeBSD machine is running. Different api calls initiate different actions, web can be used for that as well but is primarily for reading the status of different activities on the server and the console is to perform tasks as well, the same as the API but locally