I have an algorithm to do that when given the point to insert, and the existing points, but matching the unique points to what way they should be in has proven difficult as you cannot compare coordinates and the routing engine uses a different level of precision than OSM.