Prmetheus, you could write #!/usr/bin/node, except that using /usr/bin/env is a bit more flexible in that whatever command comes next (after the space, e.g. /usr/bin/env PROGRAM) is looked up using the executing user's PATH variable