I installed jslint locally. To use it conveniently I found jslint-reporter. But should I install the reporter local or global? I get confused as to when global install is OK and when not. From grunt I read you install grunt locally and grunt-cli globally. Is it the same with jslint-reporter?