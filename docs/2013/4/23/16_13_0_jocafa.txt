when you use "this.foo = 3" inside a function, it will assign 3 to the "foo" property of the instance of the constructor.  if the constructor was not called using 'new', then "this" will (generally) be the global scope