hmmmm… but don't you run the risk that some javascript files get updated to the new version of node, while the old version is still running, those files get required, and then stuff breaks, corrupting data?