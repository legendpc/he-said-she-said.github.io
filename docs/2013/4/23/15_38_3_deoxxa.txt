geNAZt: the second one is a special case from back in the "old days" of js where people didn't realise functions were just regular things like any other variable, and it will assign the function to a global variable `x'