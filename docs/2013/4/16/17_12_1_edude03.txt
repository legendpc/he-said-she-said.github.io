Hello everyone, I'm trying to get my head around map with async callbacks so if I have something like var a = array.map(function(e) { e.success(function() { return someVal}); my problem is of course the success callbacks fire after map is done