anybody know how to add a per-part header (content-type) when generating a multipart/form-data request via the request library?  (which in turn uses form-data for this, and afaict, i would have to generate my own header.  which seems … crazy.)