instead of require('../../path/to/script').useMethods(), I'd like to be able to have cleaner code like require('global/popups').useMethod(). Which would be independent of where the accessor script is too.