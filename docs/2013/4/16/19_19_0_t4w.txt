Question: I run a site on node.js that uses cluster to spawn the server over multiple cores, and up until today it worked fine. Now, however, it seems every process besides the main one is throwing errors about something already being bound to the address, even though it worked fine in 0.9.*