ChrisPartridge: what if you got more than 1 query though that the template needs? rendering from inside makes sense i maybe if you only have one? but in many cases you need 1-4 queries to return a page