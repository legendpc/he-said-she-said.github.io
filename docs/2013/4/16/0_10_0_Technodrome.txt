ChrisPartridge: i just wish i could call a query function, and have it return an array, like all normal query api's  , i don't know how to send my data to a template, i guess bind it to a variable outside of the function and then send it 