kokotron: I have a (currently broken) planet rendering engine in three.js. But I create heightmaps on the GPU and having trouble mapping them to the polar coords (and probably a couple of other issues). But that's all on the browser and I only use node for socket.io requests.