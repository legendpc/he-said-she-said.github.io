I'm looking at the ECMAScript spec for when to inject a semi-colon in: `"abc"\n+"def"` and it looks like a semi-colon *isn't* inserted because the `+` of `+"def"` isn't a restricted production like `++`