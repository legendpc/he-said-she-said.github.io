On v0.8.23, we were making a call to request.setTimeout; It says it will wait for the socket, then call setTimeout on the socket. But while load testing we saw not all request were getting the TImeout set, eventually causing the the app the become completely unresponvie