well, this one idea is to have a platform where we provide server + db and we generate like a restful API for you; and we'd develop a lib to generate some of the code for your server + android app, since a lot of this stuff can get repetitive 