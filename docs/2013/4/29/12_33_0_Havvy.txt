`new` is a function invocation type (like function call and method call) that binds `this` to a new object that has a prototype of the function's prototype property (not to be confused with the function's prototype)