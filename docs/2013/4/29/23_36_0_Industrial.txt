substack: Say I would want to build a framework for hierarchical components in the browser that each have their own css/html/models/controllers/yadda packed up and can be composed to create the pages. I want to configure these components with nested js objects much like css property overrides. So a sub-component inherits its parent component's settings. The thing is, I want to be notified by events when stuff changes in the confi