maybach, that's a lot of different components. So you need a template engine (handlerbars), framework (express), authentication middleware (passport), session management (express does that), session storage