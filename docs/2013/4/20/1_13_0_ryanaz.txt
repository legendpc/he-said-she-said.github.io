mscdex:  cool I think that's what I have right now basically :    app.use(function(req,res,next){ if ((req.method === 'POST') && (req.headers['content-type'].match(/^multipart\/form-data;/))) req.pause(); next(); } );