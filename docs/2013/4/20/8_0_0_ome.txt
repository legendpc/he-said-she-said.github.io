Forexample, I can use 'once' to bind a handler and at the end of the handler bind it again, that way all other Events are ignored until this event is handlered properly, but I want exactly the opposite of this, when ever a new even gets triggered, all previous handlers should halt.