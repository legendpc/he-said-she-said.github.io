var conn = net.connect(23,"myhost"); conn.on("data",function(data){ 	console.log(data.toString("utf8")); }); // that is just printing a weird string. I was expecting it to print the output I got on the terminal (asking password etc) ?