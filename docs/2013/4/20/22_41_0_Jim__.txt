Okay thanks! First I just watched a nice video on Node.js, and I am wondering if there is a way to serialize access to a common resource, such as a database for example. If i build a piblic facing web service that access a back end database, I need to limit concurrent connections...