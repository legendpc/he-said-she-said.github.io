preck7 That is a valid reason, by the way, you might like this linux Distro, GoboLinux or something like that, it's has a very clean  filesystem hierarchy, everything related to a program will go to a single directory.