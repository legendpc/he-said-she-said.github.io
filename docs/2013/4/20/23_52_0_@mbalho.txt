Industrial: i find streams in the browser most useful for when you either want to .pipe things into each other, like pipe a file upload into a websocket, or when you want to start reading something before waiting for it to finish downloading (like a huge CSV or something)