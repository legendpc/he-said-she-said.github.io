and `my way` is not a bit more restrictive than the original. i would like to get rid of the `module` variable as a global, and that is only achievable if I can get a reference to the real global scope. so that i can a call the module property of that 