I have two programs. One compiles stuff from src/ to build/ and then watches src/ for changes to compile those to build/ aswell. stylus, handlebars, jade, livescript, ets. normal files are just moved.