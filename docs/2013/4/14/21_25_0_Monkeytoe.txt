so in linux whenever you install a node module globally with -g... you can run it by just typing its name? so for instance this module is called node-gyp ... if you were to do a npm install -g node-gyp ... you could go to any directory on the system and type node-gyp and it would execute it?