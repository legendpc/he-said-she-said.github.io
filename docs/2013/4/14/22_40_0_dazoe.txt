I have a list of callback functions, IE: blah(function(){ blah2(function(){ blah3(...)})}); But i need a way to make sure that 1st function doesn't run till the last function is finished. the reason being is the last function will change things for the 1st function.