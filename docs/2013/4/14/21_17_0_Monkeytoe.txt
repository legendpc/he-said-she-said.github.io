yeah I added the node-gyp bin folder to the env path... only then every time I would type node-gyp it would open in visual studio instead of executing it... so I removed it from the path... maybe I should re add the path and try node node-gyp ?