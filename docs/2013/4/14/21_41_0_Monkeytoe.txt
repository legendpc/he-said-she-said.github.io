ah bingo! there is a main bin folder... program_files>nodejs>node_modules>npm>bin   notices there is now a node-gyp-bin folder there... so instead of program_files>nodejs>node_modules>npm>node_modules>node-gyp>bin I set it to that one.. and it still worked