is it that streams are only meant to work with bufs & strs? e.g., if a parser gets a str and outputs an ast, which will be used in later stages. it's not appropriate to implement each stage as a steam?