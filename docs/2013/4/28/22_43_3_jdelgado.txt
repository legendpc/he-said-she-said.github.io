kevireilly: Yeah, what I personally do, is I take the data, run a hash on it with a certain key, they append it to the cookie - so it contains the data and the hash. Each page then takes it and makes sure the data matches the hash.