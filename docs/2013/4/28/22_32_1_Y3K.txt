dypsilon: I see... It's a bit "weird" (most likely different) to what I'm used to but I think it works too. I guess that if I want to remove a package or the whole thing just deleting the directory is fine, right?