i'd like to run a node script that runs another command and then exits when the other command is done. what do i do for that? child.exec? won't node exit before the callback if the command takes a while?