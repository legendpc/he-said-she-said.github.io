npm question: lets say I've got installed a module. I noticed inside the module directory all deps are installed too. so one of the deps I need to change. Whats the usual workflow? Install the changed dep without -g option and then overwrite the specific directory of the dependency?