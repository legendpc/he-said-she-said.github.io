ningu: oh yeah, it's fine, but you shouldn't just be doing .on("data", function(chunk) {}) to consume data anyway - you should be constructing a writable stream and .pipe()ing to it if you want to take advantage of backpressure etc etc