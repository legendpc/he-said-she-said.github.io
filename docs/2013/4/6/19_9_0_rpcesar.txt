@mbalho, that replicates the feature, but it doesent quite solve the actual problem I think. after that section of logic, it then needs to reduce the list based on whether its a folder or not (to only load folders), and perform a few other iterative features