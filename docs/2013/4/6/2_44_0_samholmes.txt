Here's what I never got. A load balancer takes incoming HTTP requests and sends them off to non-busy nodes to handle the requests, and gets the response back from the nodes. But isn't the load balancer a bottleneck? How can you scale any more than what the load balancer can handle?