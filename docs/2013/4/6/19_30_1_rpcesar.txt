ok, so lets see if I learned anything here. The general concensious is to say goodbye to underscore, replace with lo-dash, use native varients whenever possible and stop industrializing the usage of these normalization libraries in server side code.