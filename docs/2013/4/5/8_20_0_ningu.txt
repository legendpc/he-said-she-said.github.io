boof: no, he's saying to use php instead of node. not sure if I agree. :) but from your description of the app it doesn't sound like node would be particularly better or worse than anything else. however, node is good at handling large numbers of requests concurrently so I guess that's useful for everyone