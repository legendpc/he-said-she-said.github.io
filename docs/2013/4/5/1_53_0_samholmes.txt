I'm thinking that foo(callback) is incorrect. callback(foo()); is better because now everything conforms to this idea that function return a value at some point (with immediate or later). I'll ponder this idea more, later.