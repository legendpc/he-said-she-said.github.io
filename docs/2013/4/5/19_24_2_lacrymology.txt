gluxon: ok,but what if bar was a function too, the example was poorly designed. what's the difference between doing this.bar = function() { return 'bar'} and <constructor>.prototype.bar = function() { return 'bar'}