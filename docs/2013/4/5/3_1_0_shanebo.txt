ChrisPartridge I tried that but it through an error that the "name" field wasn't valid.Problem is the project isn't a node_module itself, I'm just needing npm to install all dependencies. Does that make sense?