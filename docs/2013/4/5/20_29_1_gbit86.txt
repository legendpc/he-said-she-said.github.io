these files belong? I guess the entire "http-auth" folder that is copied from github belongs inside my nodejs application folder as well and that require('http-auth') does not reference an actual binary installed, but a folder?