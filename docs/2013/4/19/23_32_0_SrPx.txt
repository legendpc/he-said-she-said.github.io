I'm trying to login on my server via SSH using node.js, but it's not working properly - I guess because ssh asks for password etc. exec('ssh my_domain',function(err,stdout,stderr){ console.log(stdout); }) // nothing happens (freezes)