radiodario: thanks, that's what I'll probably do.  It's definitely something related to git submodules, after pulling from origin npm install --save is now working as expected.  Does npm log any useful output (other than the usual) anywhere?