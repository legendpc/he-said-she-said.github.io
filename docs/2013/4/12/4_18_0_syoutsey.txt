ningu: that's what the jsonp is sending back to the client, regardless. i'm loading in the jsonp from a <script> tag and it's including that first check. so i'd like to prevent the server from sending it, rather than the client from parsing it