here is an actual question: after attaching the node debugger to a process I always get "can't request backtrace now" when I type bt. This despite the fact that the process is using 100% CPU, so it sure seems stuck in a loop, and I'd love to know where (: