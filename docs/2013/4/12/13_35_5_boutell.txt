sinclair-work: the only "heavy work" I seem to be running into is image processing, which should be done with existing optimized tools written in C or whatever anyway, and that's what folks do; they use the node-imagemagick module, which manages imagemagick processes