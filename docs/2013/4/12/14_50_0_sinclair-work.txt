totallymike: you have a threadpool servicing http requests, http request is accepted on one of these threads, and if that thread requires heavy lifting, it pushes that work out to a background worker, (thread), while that work continues in the background, the original thread used to accept the http request is returned to the thread pool