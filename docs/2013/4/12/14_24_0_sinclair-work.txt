totallymike: a lot of the nodejs http stuff is happening in the core libs, not in js, but it only handles the raw low level http stuff, you still need to build stuff over that, i just don't think node should be doing that