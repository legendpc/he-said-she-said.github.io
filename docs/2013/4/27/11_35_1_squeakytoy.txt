robertj, a simple node app that reads directories, files, file contents, etc. And it should be possible to have plugins that hooks into each "data layer", such as when a file is found, it can notify plugins that are interested, etc.