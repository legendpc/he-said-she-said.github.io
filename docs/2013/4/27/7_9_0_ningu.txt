Batandwa: sounds like an issue with one of the native bindings used by bower or one of its modules. from that error I can't really tell what it might be. but you can look at what it compiled when you installed it