right now I don't assign any client ids, each request is independent, like REST, except it doesn't follow the REST standards of GET/PUT/DELETE because everything is a query for data with a JSON request object