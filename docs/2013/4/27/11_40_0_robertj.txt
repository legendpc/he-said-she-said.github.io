ponders the logistics of figuring out which blocks he needs to keep. BackutSets have rules that determine when a file comes/goes. Files have a set of blocks. Blocks go when no longer required by a file...