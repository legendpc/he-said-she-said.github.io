if I'm using app.use(express.directory(dir)); to serve a static directory, is it possible to pre-emptively intercept the requested URL to direct the server prior to serving the content? I'd like to only statically serve certain file types.