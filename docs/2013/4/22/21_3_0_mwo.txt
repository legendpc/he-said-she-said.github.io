Hi all.  I'm trying to get node-gyp running under Mac OS X 10.8.3 with Xcode 4.6.2, and it appears that xcode-select no longer exists, or rather is now just a link to xcrun, which doesn't have the -print-path option.  Anyone else seen this?