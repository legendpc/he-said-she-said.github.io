Prometheus, bash doesn't do anything else with the file, it is up to the internals of node.js at that point. node.js uses stdin/out/err which the calling shell understands, that's how they communicate