Hadaka, I assuming that it's a 'globa' package, the npm install script should take care of it. if it's not a global package, you shouldn't be really touching anything outside node_modules/YOUR-PACKAGE-NAME