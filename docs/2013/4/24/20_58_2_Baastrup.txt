Hello. I'm using sockjs to communicate between a server (node) and a client (browser). But I also need to send data from another server (node) to the first server. What would make sense here? Using POST for the server to server communication?