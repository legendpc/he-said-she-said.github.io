deoxxa1: i have put 'console.log("putting to log " + ipaddr);' after the 'logstream.write(data.toString());' line, and it only outputs 'putting to log 192.168.1.101' all the time; other ip isn't echoed, ever