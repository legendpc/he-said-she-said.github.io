r1chard: PHP and Node.JS are very different under the aspect that the Node.JS has a "always up and running" vm, where PHP is essentially started up for every request. Thus: Node.JS can become faster if you prepare a lot of things and just keep them in memory.