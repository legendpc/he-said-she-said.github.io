locoloco, for logging purposes you can *try* to use x.constructor.name, on the off-chance that object was created through a constructor, which isn't guaranteed to be the case (or really unlikely in some libraries)