the `require` function is a module locator, which finds the 'async' module by name and returns it as a value. Typically you would assign it to a variable with the name name to then use in your program.