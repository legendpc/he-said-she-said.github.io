the stack/scope overhead and perf degredation from such pollution in your typical JS engine (including V8) is noticeable if you're redundantly adding function stacks/etc to handle async cases for no reason