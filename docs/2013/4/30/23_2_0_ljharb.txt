Industrial: then instead of requiring it, perhaps you should use fs to walk through the paths? and as for browserify, it seems like you would want to generate an explicit list of paths on the server, and then have that config be available to the frontend.