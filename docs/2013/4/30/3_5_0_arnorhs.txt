i'm planning on testing JSON API endpoints, and figured that combining mocha + should + http request might be a good blend.. also there might be something nice in using json xpath along with that to check if the data is correct