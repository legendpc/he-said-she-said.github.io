iggyZiggy you can probably solve that without changing your code, but by maintaining a load-ballancing proxy and spawning new server instances, rerouting the new request to it and let the old ones on the old machines, so they can go down gracefully