it will install 2 routes, /oauth/twitter/start (where it communicates with the other server then redirects the user) and /oauth/twitter/end (which is the callback url) where it completes the communication then calls the "back" function