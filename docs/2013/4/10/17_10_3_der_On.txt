currently I have a scenario where a user uploads a file, which is async, and can go wrong, if uploaded it has to be moved, then a thumbnail must be generated then a record has to be saved in the database. This leads to absurd nesting