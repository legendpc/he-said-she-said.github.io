i.e.) i can auth to the github api via oauth and see the token in my cookies… I can go to a different browser and authenticate using that cookie.. but what if that site accidentally exposes my token?