I shouldn't have two different variants of the same codebase that are potentially not representing the same features (IE, if someone fixes something in the CS and commits without rebuilding) just because somebody wants to read JS