So, npm's tarball has a lib/ directory. If I install inform-shared anywhere else, it has a lib directory in node_modules/inform-shared, but if I install from `npm install .` on inform-daemon- the lib for inform-shared is missing