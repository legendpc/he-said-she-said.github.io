just the ws connection. im actually running in to the problem in my test suite. inside of the setup function i am opening the ws connection, then inside the tests, this.ws.send("anything") is failing because the connection hasnt been established