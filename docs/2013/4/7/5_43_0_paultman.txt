yeah, and rather then big objects like the user object, I can lazy load them, add them to cookie, and on repeated calls, if some attr is in the cookie, use it, else check db, and add it to cookie (for future requests)