deoxxa: it's not too much more work to do if(!(Buffer.isBuffer(a) && Buffer.isBuffer(b) && a.length === b.length)){return false}for(var i = 0; i < a.length; i++){if(a[i] !== b[i]){return false;}}return true;