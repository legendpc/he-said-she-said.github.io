deoxxa: by default .net (as a example) stores state inproc (in memory to the local machine) however, it is trivial to change it from inproc out to several network storage mechanisms, like a dedicated state machine, or SQL state etc