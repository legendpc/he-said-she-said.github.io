if I go require('./foo.js'), and then in foo.js I put "exports = {key: value)", is there any kind of special internal behaviour around the export object in a file you require() that I could be replacing accidentally?