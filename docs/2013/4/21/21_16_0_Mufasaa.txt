@jayk thanks for that.  Now that that's settled, have you ever done something along the lines where node listens to two ports simultaneously? One say for API calls and the other for server-server comms  