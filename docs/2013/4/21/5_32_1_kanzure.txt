well that's silly.. npm tries to read your git repository if package.json species that there's a remote git repo. except that i'm still writing my first version and haven't committed yet. so now npm fails for everything.