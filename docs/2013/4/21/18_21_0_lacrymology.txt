RLa: the setup is complicated. I'm running some tests, right? I have `mosto`, which does this.driver = new playlist_driver, and sets some functions as listeners of some events playlist_driver sends. It sends them when something happens on a pub/sub channel. Do you follow?