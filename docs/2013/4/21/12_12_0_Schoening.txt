Coz I need to create a sort of gameloop to check some variables.  I though I could just put in an IF statement, because I had heard node has its own loop and so I though it would run over the code again. But that was ofc wrong