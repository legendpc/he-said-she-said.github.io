boutell: the other thing is that node-http-proxy is still using streams1 - it could be a scenario where the compatibility isn't quite compatible.  I just finished cleaning that up in another piece of software that handled incoming requests.