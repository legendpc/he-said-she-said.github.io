i need a way of "parsing" urls so that if i type in, for example, "google.com", it resolves to a fully qualified URL of "http://google.com". would it be safe to presume that it will always be http and just modify the string, or do i need a lib for this?