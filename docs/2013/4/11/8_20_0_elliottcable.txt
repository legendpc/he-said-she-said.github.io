(all of my code is written to run in both node.js and the browser, with extremely small “loaders” to load it as a CommonJS module node-side into the server, and throw it onto a global object client-side)