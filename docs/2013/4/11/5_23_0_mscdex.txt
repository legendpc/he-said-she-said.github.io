niftylettuce: if my assumption is correct, then all you need to do is make sure you have `key` and `cert` set as your default key and cert for non-SNI clients and an `SNICallback` for SNI-capable clients