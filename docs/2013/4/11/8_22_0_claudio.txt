gues, I can't sleep. I need to be asssured about a stupid thing. Node is single-threaded, so can I be sure that if I have a (quite) complex global structure to update on every socket connection, every connection will find that structure as it was left by the socket before it?