let's simplify: if each socket connection would take one synchronous operation which lasts 1 second... the next connection (which is immediately following) what will do? Wait to enter the main node loop?