Is there a way to set what the default exit code of a node process is going to be? Rather than forcing it to exit right now with a given code via process.exit? I would like to let other callbacks do their magic, but still flag that there's a problem.