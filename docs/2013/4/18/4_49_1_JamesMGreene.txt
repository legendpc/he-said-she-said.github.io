If I am using the new ReadableStream interfaces and I want to read X bytes manually at first but then want to read everything else, what's the best way to do that?  I'm thinking start off with a `reader.pause().read(X)` and then switch over to `reader.on('data', fn).resume()`. Sound about right?