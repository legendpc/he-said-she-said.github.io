aye, in particular I'm looking at a chef cookbook that grabs the "latest" node unless a version is specified. but they've only set the most recent version number as a default so I'd have to update this cookbook each release.