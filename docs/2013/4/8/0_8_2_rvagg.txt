writeBufferSize controls the incoming log file size, by default it's 4MB, but if you have a large amount of data coming in all at once then it may be more efficient to increase that and let it buffer more on write and then handle compaction later when it's not so busy