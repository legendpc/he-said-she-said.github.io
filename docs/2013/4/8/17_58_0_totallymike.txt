it's easy to convert/change later; most of the edge cases you run into will be things that are language-agnostic, so converting to a different language later (as long as your specs are well-defined) doesn't have to be so costly