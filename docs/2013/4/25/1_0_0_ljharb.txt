AAA_awright: that's not entirely correct. the parent repo contains nothing except the SHA of the submodule, and gitignores that subtree. the submodule itself contains all the history and .git objects that it holds, and they are not cloned when the parent repo is cloned.