i'm using this for reading every file (there are many of those files): new lazy(fs.createReadStream(value)).lines.forEach(function(line) { ......... every time node passes this line, i'm counting "running" +1... every time after that an "pipe" event for lazy emits, i'm counting "running" -1