hi - is anybody familiar with the 'phantom' node bridge to phantom.js? I'm having difficulty working out an equivalent of evaluate() when the code to be run in the simulated browser is async and can't return a result immediately