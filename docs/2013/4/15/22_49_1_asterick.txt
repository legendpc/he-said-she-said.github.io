Question: I'm opening ~60HTTP requests simultaniously in node using http.request(...), and it appears that the module is requesting the individual elements sequentially rather than all at once (this is a load test script).  Is this normal behavior?