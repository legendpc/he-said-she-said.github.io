actually stupid question for me, how do you guys send query results to a template, the mysql module for instance, queries are functions in themselves, not exactly just like an array returned in say Python ? 