so I'm setting a GUC parameter with set_config() immediately followed by an UPDATE which has a trigger that references the config value. but there seems to be a race condition whereby sometimes the config value is available and sometimes it's not.