Hello guys. I have a few questions about node. What kind of web apps would need node? For example if you want to make a simple blog for yourself, would you use nodejs or would you go for Django/Rails (which would be faster I think (?) )