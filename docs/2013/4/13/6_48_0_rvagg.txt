when you're wanting to scale up to having multiple hosts handle the load then you'd need a different method (tho I have been toying with the idea of a distributed worker-farm that can work off many hosts with a similarly simple API)